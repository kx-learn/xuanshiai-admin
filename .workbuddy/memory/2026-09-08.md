# 2026-09-08

## 关注粉丝 wechat-fans 重建
用户截图：关注粉丝页（用户管理卡片）。`/wechat-fans` 原为空 ListPage，侧边栏「公众号 → 关注粉丝」入口已存在。
- 重写 `src/app/(admin)/wechat-fans/page.tsx` 为纯前端 mock：
  - 面包屑 `首页 / 公众号(下拉) / 关注粉丝`。
  - `.fan-notice` 须知框（淡蓝带 i 图标 +「须知」+ "这里的粉丝是指关注了您的公众号的粉丝，昵称是指该粉丝在您的系统平台中注册的账号昵称。"）。
  - `用户管理` 卡片（`admin-card`，标题为「用户管理」非「关注粉丝」）：筛选工具栏左侧「请选择分组」下拉 +「按昵称搜」下拉 +「请输入」输入框 + `搜索` 蓝钮 + `找到粉丝：0人`；右侧两个蓝钮 `管理粉丝分组`（Users 图标）/`同步公众号粉丝`（RefreshCw 图标）。
  - 表格列：全选 checkbox | ID | 昵称 | openId | 是否关注 | 关注时间 | 分组；空态用 `Inbox` 图标 +「暂无数据」。
  - 受控 state：group/searchType/keyword/selected（selected 全选逻辑基于 fans 长度）。
  - 后端无接口，`fans: FanRow[] = []` 模拟空列表。
- `globals.css`：末尾新增 `.fan-*` 样式（须知/图标/正文，`.wechat-fans-page .admin-card-body` padding 微调）。
- 表格样式手写（非 DataTable，因需首列全选 checkbox）。
- `npx tsc --noEmit` 通过。

## 侧边栏"官方服务"合并 tab
用户要求把「工单反馈」和「软件授权」合并成一个可展开的侧边栏菜单「官方服务」，下属两个子项；面包屑改成 `首页 / 官方服务 / 工单反馈（软件授权）`。

### 改动
- `src/components/AdminLayout.tsx`：
  - `utilityLinks` 移除「工单反馈/软件授权」两项，只保留「应用中心/婚创学苑」。
  - 新增常量 `serviceGroup: Group = { label:"官方服务", icon:MessageCircle, items:[{工单反馈→/system-feedback},{软件授权→/system-empower}] }`。
  - `activeGroup` useMemo 增加匹配 `serviceGroup.items`，使当前子页命中时该分组自动展开（openGroups 初始化含"官方服务"）。
  - 侧边栏底部 utility 区在 `utilityLinks` 渲染后追加一个可展开分组（同 groups.map 的展开/收起逻辑：按钮 toggle openGroups、子项 Link、当前页高亮 blue）。
  - import 移除不再使用的 `CircleHelp`。
- `src/app/(admin)/system-feedback/page.tsx`、`system-empower/page.tsx`：面包屑中间的「系统管理」改为「官方服务」。

### 注意
- 顶部 header 第 114 行仍保留 `<Link href="/system-feedback">工单反馈</Link>`（顶部导航项），未动。
- 两个页面本身（工单反馈表格 / 软件授权信息）内容未改，仅调整了面包屑层级。

## 官方服务两页 UI 对齐截图
用户给软件授权、工单反馈两张设计图，要求页面做得一模一样。

### 软件授权 `system-empower/page.tsx`（重写）
- 去掉原来的 授权信息+系统信息 双卡片与「授权模块列表」表格，改为**单卡片**：卡片 header「软件授权」+ body 左侧键值列表，右侧留白。
- 键值字段（无冒号，label 灰色固定 100px）：授权版本(高级版·蓝)、授权机构(宜春爱)、运营区域(江苏省 南京市)、区域保护(无)、授权域名(www.xuanshiai.com, xuanshiai.163kyun.com)、授权期限(2027年04月30日 还有233天授权到期)、授权产品(《163K相亲系统》SaaS)、授权协议(未上传·链接)、客户经理(小丽)、技术维护(崔工)、产品官网(两个 http 链接)。
- 用新 class `.license-info/.license-field/.license-label/.license-value/.license-strong`（新增于 globals.css）；`a` 蓝色带下划线，`.license-strong` 蓝色加粗。旧 `.license-grid` 样式保留不再引用。

### 工单反馈 `system-feedback/page.tsx`
- 表格列改为截图 6 列：工单ID | 反馈类型 | 反馈时间 | 状态 | 答复时间 | 操作（去掉 工单标题/提交人，提交时间→反馈时间，新增答复时间）。
- 按钮文字「提交工单」→「+ 提交新的反馈」。
- 须知框 `.service-notice` 样式本身已符合截图，未改。
- DataTable 空态（`components/ui/table.tsx`）：英文 "No data" 改为 **`暂无数据` + 灰色 Inbox 图标**（lucide `Inbox`，全局生效）。

### 类型检查
`npx tsc --noEmit` 通过。

## 软件授权字体与间距微调
用户反馈字体大小/行间距仍与截图不一致，且官网要改为超链接。
- `globals.css`：`.license-info` gap 18→22px、max-width 560→640px；`.license-label` width 100→88px、颜色 #8f99a9→#9aa1ad；`.license-value` 颜色 #333→#303133、line-height 1.5→1.6。
- 官网行 `system-empower/page.tsx:20` 本就用 `<a href target=_blank>` 两个 http 链接，`a` 样式 `.license-value a{color:#3658f7;underline}` 生效，未改代码。
- 注意：本页所有 label 均为 4 个汉字，label 列宽对齐即可，value 列自然对齐。

## 软件授权·授权版本/授权协议改普通文字
用户对照截图发现「授权版本·高级版」和「授权协议·未上传」应同为黑色普通文字（无蓝色、无下划线），仅「产品官网」为蓝色超链接。
- `system-empower/page.tsx`：`授权版本` value 去掉 `<span class="license-strong">`，改 `<>高级版</>`；`授权协议` value 去掉 `<a href="#">`，改 `<>未上传</>`。
- 产品官网仍保留两个 `target=_blank` 超链接；`.license-strong` `.license-value a` 样式保留（仅官网链接命中）。

## 婚创学苑 operate-center 全面重建 + 顶部导航跳转
用户给 11 张截图（分享老师/最新推荐/婚恋创业/红娘话术/线下活动/引流获客/红娘销售/红娘服务/资料下载/用好系统/专属课程），要求逐一还原样式；并让顶部导航栏「婚创学苑」点击后跳转到左侧导航对应页。
- 后端**无**婚创学苑接口，全部用前端模拟数据还原 UI。
- 重写 `src/app/(admin)/operate-center/page.tsx`（原为空的课程网格）为一整套客户端组件：
  - 面包屑 `首页 / 婚创学苑`；白色 `.wc-shell` 大卡。
  - `.wc-notice` 须知框（浅蓝💡）、`.wc-banner` 紫渐变（标题+副标题胶囊）。
  - `.wc-tabs` 11 个 tab（active 蓝色+底部下划线）；tab 切换时清空筛选/搜索。
  - `.wc-filter` 教师筛选行（头像+名字(数量)复选框，点击 toggle），`.wc-search` 关键词搜索（Enter 或按钮）。
  - 内容区 3 列网格：`分享老师` tab → TeacherCard（头像+名字+简介+分享胶囊）；`专属课程` → 空态（暂无数据+底部提示）；其余 tab → CourseCard。
  - CourseCard 支持 6 种内容类型图标封面：video(播放+时长角标)/pdf(红P)/ppt(橙P)/word(蓝W)/download(蓝下载)/link(绿链接)；顶部「所有正式用户/免费体验」标签；「必看」红标；老师小头像+点赞数；右上角发布时间。
  - 顶部教师徽章、头像用渐变圆+名字首字占位（无真实图片）。
- `src/app/globals.css`：新增全套 `.wc-*` 样式于末尾。
- `src/components/AdminLayout.tsx:126`：顶部导航「婚创学苑」从 `<button>` 改为 `<Link href="/operate-center">`；左侧 `utilityLinks` 的婚创学苑本就高亮 `/operate-center`，跳转即对应。

## 婚创学苑·分享老师卡片改左图右文布局
用户对照截图：每个老师卡片**左边是一张方形照片**，右边为名字/简介/「分享(N)」胶囊。当前是「上头像+下简介」上下布局，需改左右布局；暂无真实图片，左侧照片位先留空占位。
- `operate-center/page.tsx` TeacherCard：外层 `wc-teacher-card` 改为 `display:flex`，新增 `.wc-teacher-photo`（92×92 圆角方）放占位灰底+人物轮廓图标（无图片先空着），右侧 `.wc-teacher-card-body`（名字/简介/分享胶囊）`flex:1`，分享胶囊 `margin-top:auto` 贴底右对齐。
- `globals.css`：`.wc-teacher-card` 改 flex 布局；删除 `.wc-teacher-card-head`（不再用）；新增 `.wc-teacher-photo`/`.wc-teacher-photo-placeholder`/`.wc-teacher-card-body` 样式。

### 类型检查
`npx tsc --noEmit` 通过。

## 应用中心 plugin-center 重建 + 查看详情弹窗
后端无应用中心接口，全部前端模拟。用户给 2 张截图：应用中心列表页 + 点击「查看详情」后的弹窗。
- 面包屑改为 `首页 / 应用中心`（去掉原「系统管理」）。
- 重写 `src/app/(admin)/plugin-center/page.tsx`（原来是空插件网格）：
  - 列表页：`.admin-card` 内 `.app-list`，每条 `.app-item` 横向布局：左侧 `.app-icon`（88×88 紫色渐变方块，两行白字如「互选/活动」），右侧 `.app-main`：`.app-title-row`（应用名+`.app-cat-tag`「独立模块/运营工具」+`.app-purchased-tag`「已购买」绿标签）+`.app-desc`（描述 clamp 2 行）+ 右下角 `.app-detail-btn`（绿色描边「查看详情」pill）。
  - 详情弹窗 `DetailModal`：`.app-modal-mask` 遮罩 + `.app-modal`，头部 X 关闭 +「查看详情」标题，body 左侧大图标（`.app-icon-lg` 128×128）+ 右侧名称/分类/「应用介绍」/「详细介绍」。Esc 或点击遮罩关闭，用 `useEffect` 锁 body 滚动。
  - 示例 6 个应用：互选活动/商家联盟/互动消息/多门店系统/销售匹配库/朋友圈（含 iconText、渐变、分类、已购买、描述、详细介绍）。
- `globals.css`：末尾新增全套 `.app-*` 样式（列表/图标/标签/按钮/弹窗遮罩及内容）。
- 无现成 dialog/radix-dialog，只装了 `@radix-ui/react-slot`，手写弹窗。

## 应用中心详情弹窗改右侧抽屉
用户：详情弹窗应从**右边弹出**，样式**上下贴着边缘**（占满视口高度）。
- `globals.css`：`.app-modal` 改为 `position:fixed; top:0/right:0/bottom:0; height:100%; width:min(760px,100%)`；从居中弹窗改成右侧滑入抽屉，加 `@keyframes app-drawer-in`（translateX(100%)→0）滑入动画。`.app-modal-mask` 去掉 flex 居中（退化为纯遮罩）。`.app-modal-body` 改为 `height:calc(100% - 57px); overflow-y:auto`（头部固定、内容内部滚动）。
- JSX 结构（header 左 X + 标题，body 左大图标+右信息）无需改动，仅 CSS 改定位。

### 类型检查
`npx tsc --noEmit` 通过。

## 公众号菜单配置 wechat-menu 重建
用户给 3 张截图：菜单配置页（左侧手机模拟微信菜单 + 右侧须知/表单）。侧边栏「公众号 → 菜单配置」已指向 `/wechat-menu`，原页为空 ListPage。
- 重写 `src/app/(admin)/wechat-menu/page.tsx` 为纯前端 mock 编辑器（无后端接口）：
  - 面包屑 `首页 / 公众号(下拉) / 菜单配置`。
  - **左侧 `.menu-phone` 手机外壳**：顶部状态栏（`·····WeChat` + `1:21 AM` + `100%` 电池标）、导航栏（`‹ 返回` + 右侧人形 SVG）；中部 `.menu-phone-screen` 二级菜单列表（`activeTop.subs` 渲染，当前项 `.selected` 蓝字）；底部 `.menu-phone-foot` 一级菜单栏（`≡` 键盘图标 + 3 个一级菜单 col，每 col 上为 `+` 按钮下为菜单名，当前选中蓝字）。
  - **二级菜单悬浮操作**：`.menu-sub-row:hover .menu-sub-actions` 从右上弹出 `↑/↓/×` 三个 `.menu-act-btn`（SVG，上移/下移/删除），`e.stopPropagation()` 防触发选中。
  - **点击联动**：点二级菜单行 → `setActiveSubId`；点一级菜单 → `switchTop` 重置为该列 subs[0]。右侧表单 `.menu-form` 用 `activeSub` 显示其 菜单名称 / 菜单类型（radio：跳转链接/发送消息/跳转小程序，默认 url）/ 跳转地址（type!=url 时 disabled）。
  - `+` 给当前一级菜单追加空子菜单（`addSub`）；`同步菜单` 蓝钮校验一级≤3、每个二级≤5 后 mock 弹窗。
  - 须知 `.menu-notice`：`i` 图标 + 8 条带编号文本，红色片段用 `renderNoticeLine`（`<span class="menu-red">`）注入（不超过3个/不超过5个/128字节/256字节/3字节/已认证）。
- `globals.css`：末尾新增全套 `.menu-*` 样式（手机外壳/状态栏/二级行/悬浮操作/一级菜单栏/同步按钮/须知/表单/radio）。
- 复用 `.admin-card` / `.admin-card-header` / `.admin-card-body`。
- `npx tsc --noEmit` 通过。

## 公众号菜单配置·尺寸精确修正
用户第三次反馈手机尺寸不对（此前两次从 470 已收窄到 330）。进一步对齐 `.menu-*`：
- **手机外壳宽度**：`.menu-left` / `.menu-phone` 由 `470px` 收窄为 `330px`（窄竖屏比例）。
- `.menu-phone-screen` 右侧内边距 `22→14px`；`.menu-sub-row` 宽度 `55%→56%` 再回调居中。
- **二级菜单长条**：高度 `62→64px`、字号 `15→16px`。
- **键盘图标改「圆圈+9点+上方收起箭头」SVG**（viewBox `0 0 26 32`：顶部 `M8 3 L13 8 L18 3` 箭头 + 外圆 cx13 cy20 r10 + 9 个圆点），并去掉 `.menu-key` 的外圈边框圆（原 `border:1px #999; border-radius:50%`），仅居中显示。

## 公众号菜单配置·点击联动改「右侧显示当前菜单信息」
用户反馈：点击底部一级菜单不应切到其二级菜单（更不应把右侧设为其第一个二级），而应在右侧直接显示该一级菜单本身的信息；悬浮二级菜单显示 ↑↓× 图标。
- `TopMenu` 增加 `type`/`url` 字段（一级菜单也有类型+跳转地址）。
- 状态约定 `activeSubId === -1` 表示「当前编辑对象是该一级菜单本身」，否则为二级 id；初始 `activeSubId = -1`。
- `switchTop(id)` 只 `setActiveTopId(id)` + `setActiveSubId(-1)`（不再改成 subs[0]）。
- 新增 `editingMenu` = `activeSubId===-1 ? activeTop : (activeSub ?? activeTop)`；右侧表单全部改用 `editingMenu` 与 `updateMenu`（updateMenu 按一级/二级分别更新 TopMenu 或 subs），删除原 `activeSub` 空态分支（一级菜单恒有数据）。
- 悬浮图标 `.menu-sub-actions` 由 `top:-44px; right:0` 改为 `top:-34px; left:0; right:0; justify-content:center; gap:22px`（行上方水平居中），字体/按钮略放大。
- `npx tsc --noEmit` 通过。
- **底部 `+`**：`.menu-plus` `font-size:32px→36px`、`font-weight:300`、`padding:24px 0 20px`，颜色 `#444`。
- **一级菜单名**：`.menu-top-name` `font-size:15→16px`。
- `npx tsc --noEmit` 通过。

## 公众号菜单配置·手机布局修正
用户截图指出手机模拟布局与截图不符，修正 `wechat-menu/page.tsx` 与 `.menu-*`：
- **手机更宽**：`.menu-left` `flex:0 0 470px`、`.menu-phone` `width:470px`。
- **二级菜单靠右贴边**：`.menu-phone-screen` 改 `align-items:flex-end; justify-content:flex-end`（顶部留白、紧贴底部），`.menu-sub-row` 宽 `55%`、高 `62px`、白底黑字，相邻项压线 `border-top:0`（去掉圆角/阴影，无 gap）。
- **底部菜单栏**：`.menu-phone-foot` 白底；第一格 `.menu-foot-key` 窄 `64px` 内含圆形九宫格键盘图标（`.
menu-key` 边框圆 + SVG 9 圆点）；其余 3 个一级菜单等宽格，每格上为**不带边框**的大号 `+`（`font-size:32px`），下为 `.menu-top-name`（上下有细分隔线，选中蓝字加粗）。
- **状态栏/导航栏**更贴近：状态栏纯黑 `#000`、导航栏 `#404040`，字号加大。
- `npx tsc --noEmit` 通过。

## 小程序参数配置 miniprogram-config 重建
用户给 2 张截图：小程序参数配置页（配置表单 + 授权卡片）。后端无小程序配置接口，纯前端模拟。
- 重写 `src/app/(admin)/miniprogram-config/page.tsx`（原为 ListPage 空表格）为一整套配置表单：
  - 面包屑 `首页 / 小程序(下拉) / 参数配置`。
  - `.mp-notice` 须知框（浅蓝底 i 图标 + "须知"标题 + ICP 说明文字）。
  - `小程序配置` 卡片：是否开启（自定义 `.mp-switch` 圆形开关，on 时蓝底+右白点+左"启用"文字）、*appId / *appSecret（`.mp-input` 输入框）、状态栏背景色（`.mp-color-swatch` 色块，点击唤起隐藏 `<input type=color>`）、小程序码（`.mp-upload` 上传块）、分享封面（上传块+`.mp-clear-btn` 清空+`.mp-tip` 尺寸提示）、实名认证功能开关、`确定提交` 蓝色按钮。
  - `小程序授权` 卡片：`立即授权` 蓝色按钮 + `.mp-authorize-tip` 提示框。
  - 字段几乎全是受控 state（enabled/appId/appSecret/barColor/qrState/coverState/realName）。
- `globals.css`：末尾新增全套 `.mp-*` 样式（须知/卡片/行/label/开关/输入/色块/上传/提示/按钮）。
- 复用 `.admin-card` / `.admin-card-header` / `.admin-card-body`。

## 小程序参数配置布局对截图修正
用户再次截图指出布局有区别。对照两张截图逐项修正 `miniprogram-config/page.tsx` 与 `.mp-*` 样式：
- **按钮贴左**：`确定提交`/`立即授权` 之前放在 `.mp-row` 里带空 `.mp-label`（96px 缩进），导致不贴左。改为 `.mp-field`（flex）里直接放按钮（无 label），flex 从 0 起靠左。
- **提示条整宽淡蓝**：`留空默认为首屏截图…` 和授权提示由「行内 `.mp-tip` 小字」改为整宽淡蓝条 `.mp-tipbox`（`width:100%; max-width:980px; border:1px #d6e0fb; background:#f5f8ff; padding:12px 16px`），带圆形 `i` 图标。分享封面条的 tipbox 用 `.mp-upload-group .mp-tipbox{align-self:stretch}` 撑满控件区。
- **上传按钮改深灰**：`.mp-upload-btn` 增加 `variant`（`dark`: 深灰 `#4f5a69` 白字，`light`: 白底灰边黑字）；小程序码用 dark、分享封面用 light。去掉原白底描边观感。
- **去掉行分隔线**：`.mp-card-body` 内不再用 `.mp-row`（原带 `border-bottom`），改用 `.mp-form`+`.mp-field`（`padding:15px 0`、无分隔线），截图表单行无明显横线。
- **开关文字区分**：`CheckSwitch` 增加 `label` prop（默认「启用」），「是否开启」传「开」、「实名认证功能」传「启用」。
- **输入框宽度**：`.mp-input` 320→250px 贴合截图；`.mp-label` 仍 `flex:0 0 96px`；`.mp-card-body` padding 改 `4px 24px 24px`。
- `npx tsc --noEmit` 通过。

## 公众号参数配置 wechat-config 重建
用户给 2 张截图：公众号参数配置页（公众号配置 + 安全验证）。侧边栏「公众号 → 参数配置」已指向 `/wechat-config`，原页为空 ListPage。
- 重写 `src/app/(admin)/wechat-config/page.tsx` 为纯前端 mock 表单：
  - 面包屑 `首页 / 公众号(下拉) / 公众号参数配置`。
  - `.gzh-notice` 须知框：`i` 图标 +「须知」+ "以下内容的配置正确与否将影响到您平台中与公众号的相关功能的正常。"
  - `公众号配置` 卡片：*公众号微信号 / *appId / *appSecret / *apiToken / *消息加解密密钥（`.gzh-input` 输入框，`flex:1` max-width 480px）；加密模式三选一 radio（明文/兼容/安全，选中蓝点，安全默认）；二维码（`.gzh-qr` 88×88 内嵌运行时生成 SVG 伪二维码，下方`↑ 上传二维码`+`↻ 生成二维码`两个 `.gzh-icon-btn`）；`确定提交` 蓝钮（用空 `.gzh-label` 占位，对齐输入框左缘，非贴左）。
  - `安全验证` 卡片：`上传文件`（`.gzh-file` 选择文件按钮 + 文件名，隐藏 file input 只收 `.txt,.html`）；`.gzh-tipbox` 淡蓝提示条"请上传.txt或.html文件"；`确定提交` 蓝钮。
  - 受控 state：wxNo/appId/appSecret/apiToken/secretKey/mode/qrFilled/fileName。
- `globals.css`：末尾新增全套 `.gzh-*` 样式（须知/卡片/字段/label/输入/radio/二维码/按钮/文件上传/提示条/提交按钮）。
- 复用 `.admin-card` / `.admin-card-header` / `.admin-card-body`。
- `npx tsc --noEmit` 通过。

## 公众号菜单配置·二级菜单固定同级面板
用户截图指出：会员中心/红娘中心/联系红娘/私人订制是**同级菜单**，点击底部「社交活动/服务中心」时中间二级菜单不应消失。原实现二级列表随 `activeTop`（一级菜单）切换，切到空子菜单的一级会清空。
- `wechat-menu/page.tsx`：二级菜单拆为独立 state `subMenus`（4 个同级项），与一级菜单解耦。
  - `addSub()` 不再接收 topId，改为向 `subMenus` 追加并选中。
  - `removeSub` / `moveSub` 改为操作 `subMenus`（交换同级别顺序）。
  - `updateMenu`：`activeSubId===-1` 改 `topMenus`，否则改 `subMenus`。
  - 中间列 `menu-phone-screen` 渲染 `subMenus`；底部 `+` 绑定 `onClick={addSub}`。
  - `switchTop(id)` 只 `setActiveTopId`+`setActiveSubId(-1)`，中间列表不受影响。
- `npx tsc --noEmit` 通过。

## 公众号菜单配置·底部栏拆两行 + 二级菜单列大小
用户截图要求菜单位置/大小与图片一模一样。关键差异：**底部栏不是一列上下排 `+`+菜单名，而是两行**——上一行是「`+` 行」（3 个等宽格子均分整宽，不预留键盘位），下一行才是「键盘图标 + 3 个菜单名」。
- `wechat-menu/page.tsx`：`.menu-phone-foot` 内拆成 `.menu-plus-row`（`topMenus.map` → 仅 `.menu-plus` 按钮，均分整宽）+ `.menu-name-row`（`.menu-foot-key` 键盘列 + `topMenus.map` → `.menu-top-name`）。
- `globals.css`：
  - 删旧 `.menu-foot-col`/`.menu-key` 外圈边框。
  - `.menu-plus-row` flex；`.menu-plus` `flex:1`、`border-left:1px #ececec`（first 无）、`padding:26px 0 24px`、`font-size:40px`、`color:#888`。
  - `.menu-name-row` flex、`border-top:1px #ececec`；`.menu-foot-key` `flex:0 0 62px` 垂直居中；`.menu-top-name` `flex:1`、`border-left:1px #ececec`、`padding:26px 0 24px`、`font-size:16px`。
  - 二级菜单列：`.menu-sub-row` 宽 `56%→44%`、高 `64→56px`、字号 `16→15px`；`.menu-phone-screen` `min-height:320→300px`、右侧内边距 `14→12px`。
- `npx tsc --noEmit` 通过。

## 公众号自动回复 wechat-autoreply 重建
用户给 3 张截图：自动回复页（三 tab：关注回复/关键词回复/消息回复）。原为 ListPage 空表格。
- 重写 `src/app/(admin)/wechat-autoreply/page.tsx` 为多 tab 自定义页：
  - 面包屑 `首页 / 公众号(下拉) / 自动回复`。
  - 三个 tab（`关注回复`/`关键词回复`/`消息回复`）+ 主内容区，选中蓝字+蓝色 3px 下划线。
  - `关注回复`：淡蓝须知框「当用户关注公众号时，系统会自动回复以下信息，但根据腾讯官方要求，只能发送前2条」+ 全宽虚线按钮「+ 添加关注回复」+ `回复内容` card（标题右侧 radio「回复全部/随机回复」；数据行：文本「Hi，欢迎来到宣智爱❤一个真实、有趣、优质的脱单社交平台。」+ 右侧编辑/删除图标）。
  - `关键词回复`：两个并排虚线按钮「+ 添加关键词回复」「⚡ 触发关键词统计」+ `回复内容` card（标题右侧搜索框「请输入关键词」+ 蓝色搜索按钮，无数据）。
  - `消息回复`：淡蓝须知框「当用户输入关键词以外的内容时，系统会自动回复以下信息，但根据腾讯官方要求，只能发送前2条」+ 全宽虚线按钮「+ 添加消息回复」+ `回复内容` card（radio「回复全部/随机回复」，无数据）。
  - 受控 state（activeTab/replyMode/keyword/followReplies），addReply/removeReply 操作关注回复列表。
- `globals.css`：末尾新增全套 `.ar-*` 样式（tab/下划线/须知/添加虚线按钮/卡片/radio/搜索框/内容行/编辑删除图标/空态）。
- `npx tsc --noEmit` 通过。侧边栏「公众号 → 自动回复」入口本就指向 `/wechat-autoreply`，无需改动。

### 自动回复·编辑按钮生效
用户反馈关注回复行的「编辑」图标点击无反应（此前 `EditIcon` 按钮未绑 `onClick`）。
- `wechat-autoreply/page.tsx`：新增 `editingId`/`draft` state 与 `startEdit`/`saveEdit`/`cancelEdit`；行渲染时若 `editingId === r.id` 则显示 `.ar-edit-box`（`input` 编辑内容 + 「保存」「取消」按钮），否则显示原文案 + 编辑/删除图标，编辑按钮 `onClick={startEdit(r.id, r.content)}`，删除 `removeReply`。
- `globals.css`：`.ar-*` 末尾新增 `.ar-edit-box`/`.ar-edit-input`/`.ar-edit-actions`/`.ar-edit-save`/`.ar-edit-cancel` 样式。
- `npx tsc --noEmit` 通过。

## 公众号模板消息 wechat-template 重建
按两张截图重建 `/wechat-template`（此前是空 ListPage），侧边栏入口已指向该路径。
- `page.tsx`（重写）：面包屑 `首页/公众号▾/模板消息`；`.tm-notice` 须知框「i 图标 + 须知」，5 行文案；`.admin-card` 卡片标题「模板消息」，工具栏左侧仅「请选择分类/全部分类」下拉。
- 表格列：序号 | 模板标题 | 关联会员 | 推荐使用模板编号 | 推送事件 | 消息接收人 | 对应公众号平台模板 | 开关 | 操作。
- 数据行 2 条（奖品兑换成功通知/兑换会员、账户资金变动提醒/财务），「对应公众号平台模板」未选时显示蓝色「选择模板」链接（点击打开抽屉），已选则显示编号；「开关」蓝底白字「开」toggle，可切换为灰色「关」；「操作」蓝色「配置」。
- 「模板标题/关联会员」两列为窄竖排（`writing-mode: vertical-rl` + 字间距）。
- `TplModal`：右侧滑入抽屉（复用 `app-drawer-in` 动画，`top/right/bottom:0` 贴边），标题「选择模板」+ 右上 `×`；body 内 `.tm-modal-notice` 淡蓝须知「如果选择模板清单中没有这个模板，应该在这里添加。」+ 右侧蓝色「+ 添加新模板」按钮；下方 `.tm-tpl-box` 表头「模板/选择」+ 模板行（编号 + 蓝色「选择」按钮），点击「选择」回填到对应行的 platformTpl 并关闭抽屉。
- `globals.css`：末尾新增全套 `.tm-*` 样式（须知/表格/竖排单元格/蓝色链接/开关/抽屉/弹窗须知/模板列表/选择按钮）。
- `npx tsc --noEmit` 通过。

## 小程序参数配置·上传图片/清空图片修正
用户截图澄清：「上传图片」不是独立按钮，而是**叠在预览图底部的一条深灰覆盖条**；「清空图片」是清空小程序码 + 分享封面这两张上传图。
- `miniprogram-config/page.tsx`：`UploadBox` 移除 `variant` 与独立的 `.mp-upload-btn`，改为预览区 `.mp-upload-preview` 内 `position:relative` 底部叠加 `.mp-upload-overlay`（绝对定位 `left:0/right:0/bottom:0`，深灰底白字「上传图片」，点击上传）。
- `清空图片` 点击当前同时 `setCoverState("empty")` + `setQrState("empty")`（清两张图）。
- `globals.css`：`.mp-upload-preview` 加 `position:relative`；新增 `.mp-upload-overlay`（`#5a5a5a` 深灰白字，圆角下 3px，hover 加深）；删除 `.mp-upload-btn` 及 dark/light 变体；`.mp-upload-group` 内 `.mp-upload` 宽度锁定 88px。
- `npx tsc --noEmit` 通过。

## 公众号消息群发 wechat-send 重建
按两张截图重建 `/wechat-send`（原为 ListPage 空表），侧边栏「公众号 → 消息群发」入口已指向该路径。
- `page.tsx`（重写）：
  - 面包屑 `首页 / 公众号(下拉，children 含 6 个公众号子页) / 消息群发`。
  - `.sd-notice` 须知框：`i` 图标 +「须知」标题，6 行文案（基于微信公众平台模板消息的群发系统…/不受条数限制…/支持按粉丝分组群发…/自定义链接…/自由编辑字段文字内容颜色变量…/使用步骤：1、新建群发 2、编辑模板内容 3、发送）。
  - `.admin-card`：自定义头部 `.sd-card-head`（左 `.sd-card-title`「已创建的群发消息」+ 右 `.sd-add-btn` 蓝底白字「+ 添加新模板」，flex 两端对齐）。
  - 表格列：ID | 模板标题 | 对应公众号平台模板 | 更新时间 | 操作；初始空态显示 Inbox 灰图标 +「暂无数据」；`pickTpl` 后往表格添加一行（title「新建群发」、platformTpl 为所选编号、updatetime 当前时间），「操作」= 蓝色「配置」。
  - `SendDrawer`：右侧滑入抽屉（复 `app-drawer-in`，top/right/bottom:0 贴边），标题「新建群发消息」+ 右上 `×`；`.sd-modal-notice` 淡蓝须知「切勿滥用和违规使用本功能，微信模板消息违规说明」；`.sd-tpl-box` 表头「模板/选择」+ 模板行（编号 + 末尾问号圆圈图标 `/` + 右侧蓝色「确定」按钮），点击「确定」回填编号并关闭抽屉。
- `globals.css`：末尾新增全套 `.sd-*` 样式（须知/卡片头/添加按钮/表格/链接/抽屉/弹窗须知/模板列表/确定按钮）。
- `npx tsc --noEmit` 通过。

## 模板消息·“配置”编辑弹窗
用户截图为模板消息页点「配置」后弹出的居中「编辑配置」弹窗。此前「配置」按钮无 onClick。
- `wechat-template/page.tsx`：
  - 新增 `ConfigField` 接口与 `initialConfigFields`（first/keyword1-4/remark 六个字段，内容与颜色块初值 `#000`）。
  - 新增 `EditConfigModal` 组件：居中蒙层弹窗（`.ec-modal`，flex column），标题「编辑配置」+ 右上 `×`；
    - body 顶部 `.ec-notice` 淡蓝须知「切勿在微信推送模板中配置违反微信规则的内容或链接，微信模板消息违规说明」。
    - 左右两栏 `.ec-columns`：左 `.ec-form`（`模板ID` 区块标题 + `名称` 行（name 初值=该行 title）+ 字段行 `first/keyword1…/remark`，每行 = 标签 + 输入框 + 黑色颜色块（隐藏 `<input type=color>`）+「插入可用变量」（在字段后追加 `{{变量}}`）+「增加一行」（新增 keywordN 行）+「链接类型」radio（网页/小程序/空，蓝色圆点）+ `*链接地址`（`{{SiteUrl}}/subpages/gift/exchange`，选「空」禁用））；右 `.ec-preview`「消息预览」灰底卡片（first 无前缀、keyword 显示 `keywordN: 值`、remark 无前缀，实时联动）。
    - footer：左「取消」右「确定」（蓝色）。
  - state 加 `configOpen`/`configRowId` + `openConfig(id)`；「配置」按钮 `onClick={() => openConfig(r.id)}`。
- `globals.css`：末尾新增全套 `.ec-*` 样式（蒙层/弹窗/header/body 滚动/footer/须知/两栏/表单行/颜色块/按钮/radio/预览卡片）。
- `npx tsc --noEmit` 通过。

## 平台配置·基本配置 platform-config-basic 6-tab 重建
用户 12 张截图：平台配置页面所有 tab。`/platform-config-basic` 原为空 ListPage，侧边栏「平台配置 → 基本配置」入口已指向该路径。
- `page.tsx`（重写为纯前端 mock）：
  - 面包屑 `首页 / 平台配置(下拉) / 基本配置`；顶部 `.pcfg-tabs` 6 个 tab：基本配置/运营模式/信息登记引导页配置/基本资料登记配置/私密信息登记与展示配置/筛选功能配置（选中蓝字+3px 蓝色下划线）。
  - **基本配置**：左右两栏 `.pcfg-basic-wrap`（左 `.pcfg-basic-form` 字段列 + 右 sticky `.pcfg-preview`「分享效果预览」卡片，联动 shareTitle/shareSummary）。字段：平台名称/平台宣传标语/PC端Logo/PC引导页图/抖音二维码/首页分享标题/首页分享摘要/推送消息摘要(+淡蓝 tip「公众号推送消息的摘要内容」)/首页分享图片(+云端素材)/微信一键登录页LOGO(+云端素材)/登录页标语(+恢复默认，tip 透明PNG 446x34)/默认籍贯地/默认现居地(下拉)/自定义默认头像设置(男/女会员缺省头像，上传+从云端素材选择)/确定提交。
  - `UploadBox`：96x96 预览图（空态占位文字，filled 渐变蓝底字）+ 底部 `.pcfg-upload-overlay` 深灰「上传图片」条（叠在预览图底部）；可选右侧「云端素材」按钮；下方 `.pcfg-tipbox` 淡蓝提示条（圆形 i 图标）。
  - **运营模式**：`.pcfg-radio-wrap` 3 个 radio（全平台线上牵线+引导线下模式[蓝点]/全平台纯线下模式/指定会员为纯线下模式）+ 淡蓝说明条（居中 i 图标）+ 两个 CSS 手机样机 `.pcfg-phone` 示意 + 红色虚线箭头 SVG `.pcfg-mode-arrow` + 确定提交。
  - **信息登记引导页配置**：`.pcfg-notice` 须知框 + `.pcfg-table-guide` 表格（ID|标题|描述|是否展示|排序），行内「标题/描述」为 `.pcfg-input-inline` 输入框、「是否展示」为 `.pcfg-switch` 开(蓝)/关(灰)、「排序」为 `.pcfg-sort-btn` 上移/下移蓝色链接（首行无上移、末行无下移）。
  - **基本资料登记配置**：须知框 +「资料登记页统一副标题」输入框 + 表格（排序|资料登记字段|引导文案|加入到会员注册流程），`flow` 三分支：`required` 显示「必选项」文字、`on` 显示蓝「是」开关、`off` 显示灰「否」开关（26 行字段按截图）。
  - **私密信息登记与展示配置**：`.pcfg-table-private` 表格 7 列（ID|数据项目|是否加入到会员注册流程|是否展示在编辑资料|引导文案|是否展示在会员详情页|排序），29 行私密字段，「民族」首行加入注册=开其余关，每行 3 个开关 + 上移/下移。
  - **筛选功能配置**：须知框 + `.pcfg-table-filter` 表格（筛选条件|开通/关闭|排序|使用权限），18 行筛选条件全「开通」蓝钮、「使用权限」为原生 `<select>`（所有人可用/需完善资料可用/仅VIP会员可用）。
- 通用组件：`Switch`（on=蓝底白字 off=灰底白字，可传 onText/offText/width）、`SortLinks(index,total,onMove)`、`Notice(text)`。
- `breadcrumb-config.ts`：`groups` 新增 `platformconfig`（label 平台配置 href /platform-config-basic，children 7 个平台子页）。
- `globals.css`：末尾新增全套 `.pcfg-*` 样式（tab/卡片/两栏/字段/label/input/select/上传组/上传overlay/云端按钮/tipbox/恢复默认/avatar组/提交/预览栏/radio/mode-notice/手机样机/须知框/表格列宽/开关/排序链接）。
- `npx tsc --noEmit` 通过。

## 云端素材 cloud-material 新建
用户 12 张截图：云端素材工具页（顶栏「云端图库」入口）。后端无接口，纯前端模拟 UI。
- 新建 `src/app/(admin)/cloud-material/page.tsx`：
  - 面包屑 `首页 / 运营工具 / 云端素材`；`.cm-app` 白色大卡。
  - `.cm-head` 头部（左「云端素材」标题 + 右 `×` 关闭）。
  - `.cm-banner` 紫渐变横幅「婚恋行业图片素材库」+ 右侧 4 个圆形 reward 徽章（国内首家/原创设计/海量更新/直接可用，icon+标题+副标题）。
  - `.cm-body` 左右布局：左 `.cm-nav` 竖排 12 个分类（最近更新/全部素材/平台轮播图/平台图标/线下获客/红娘朋友圈/红娘海报/活动封面/活动招募/人物头像/装修设计/公众号，active 淡蓝底蓝字）；右 `.cm-main`。
  - `.cm-toolbar`：左 `|` 紫色竖线+分类名，tabs.length>1 时显示子 tab 胶囊行（active 紫底白字）；右搜索区（「按标题关键词/ID搜索」标签 + 输入框 + 蓝色搜索按钮 + 紫色描边「改图技巧」+ 紫色实心「提交需求」）。
  - `.cm-grid` 5 列素材卡片：图片区（渐变+emoji 占位，aspect 4/3）+ 分类名(ID:递减ID) 居中 + 底部分割「查看 / 下载」两按钮（蓝色文字，SVG 图标）。
  - 分类→卡片：每 album 用 cardLabel/emojis/hue/startId 生成 count 张递减 ID 卡片；「公众号」empty=true 显示空态（Inbox 图标+暂无数据）。
  - 交互：点左侧分类切换 activeKey/重置 tab；点子 tab 高亮；搜索(Enter/按钮)按 ID 或标题过滤；「查看」弹 `.cm-preview` 居中预览弹窗（渐变大图+emoji+分类名(ID)+关闭按钮，点遮罩关闭）。
- `AdminLayout.tsx:126` 顶栏「云端图库」由 `<button>` 改为 `<Link href="/cloud-material">`。
- `globals.css`：末尾新增全套 `.cm-*` 样式（头部/banner/reward/nav/分类标题/tab/搜索/网格/卡片/空态/预览弹窗）。
- `npx tsc --noEmit` 通过。

## 云端素材改全局弹窗（非独立页面）
用户反馈：云端图库是**点顶栏跳出来的弹窗**（蒙层居中、背景变暗、带 × 关闭），不属于任何大菜单，不应是点击菜单后的独立路由页。
- 删除 `src/app/(admin)/cloud-material/page.tsx`（不再占路由）。
- 新建 `src/components/CloudMaterialModal.tsx`：把原页面内容抽成 `CloudMaterialModal({ open, onClose })`，去掉面包屑；`.cm-mask` 全屏遮罩（`position:fixed; inset:0; z-index:300; rgba(20,25,45,.45)`，纵向可滚动）内嵌 `.cm-dialog` 弹窗（`max-width:1240px; border-radius:10px; box-shadow 大`）。open 为 false 时 `return null`；重开时重置 preview。点击 × / 遮罩（复用预览遮罩）关闭。
- `AdminLayout.tsx`：新增 `cloudOpen` state；顶栏「云端图库」由 `<Link href="/cloud-material">` 改回 `<button type=button onClick={()=>setCloudOpen(true)}>`；在 `<main>` 后挂载 `<CloudMaterialModal open={cloudOpen} onClose={()=>setCloudOpen(false)} />`。
- `globals.css`：新增 `.cm-mask`/`.cm-dialog`（含 `.cm-dialog > .cm-head` 白底、`.cm-dialog > .cm-banner` 顶边、`.cm-dialog .cm-body{max-height:calc(100vh-200px); overflow-y:auto}`）弹窗壳样式。
- `npx tsc --noEmit` 通过。

## 云端素材·改图技巧详情抽屉 + 提交需求反馈抽屉
用户 6 张截图：点「改图技巧」弹「详情」右侧抽屉（富文本教程步骤 1-6），点「提交需求」弹「添加反馈」右侧抽屉（反馈表单）。
- `CloudMaterialModal.tsx`：新增 `detailOpen`/`feedbackOpen` state；「改图技巧」按钮 `onClick={()=>setDetailOpen(true)}`、「提交需求」按钮 `onClick={()=>setFeedbackOpen(true)}`；open 重开时同时重置两个抽屉。在 preview 预览弹窗后渲染两个右滑抽屉。
  - **详情抽屉 `.cf-detail`**（宽 900px，复用 `app-drawer-in`）：`.cf-head` 左 `×`+「详情」标题；`.cf-body` 滚动，`.cf-article-title` 居中「云端素材库使用、改图小技巧」；6 个 `.cf-step`（步骤 1-5 各带 `.cf-illus` 插图占位（渐变底+底部标签），步骤 4 后附口令词示例 3 行、步骤 6 常用提示口令 5 行 + `.cf-ps-note` 注：图片文字仅供参考）。
  - **反馈抽屉 `.cf-feedback`**（宽 760px）：`.cf-head` 左「添加反馈」+ 右「取消」/「确定提交」按钮；`.cf-feedback-body` 表单——反馈类型 radio（BUG/使用咨询/需求建议/故障诊断/其他，自定义圆点蓝点）、内容(required)富文本链接编辑器（`.cf-editor-toolbar` H/B/T₁/T₂/I/U/S/字体/… 工具栏 + `.cf-editor-text` textarea「请输入正文」）、手机(`*13285288888`)/微信/QQ 输入框。
- `globals.css`：新增全套 `.cf-*` 样式（遮罩淡入/两个抽屉/头部/关闭/按钮/正文/文章标题/步骤/插图占位/口令行/表单行/label/radio/编辑器/工具栏/输入框）。
- `npx tsc --noEmit` 通过。

## 平台配置·导航配置 platform-navconfig 重建
用户 8 张截图：导航配置页（侧边栏「平台配置 → 导航配置」）。`/platform-navconfig` 原为 ListPage 空表格，面包屑 `首页/平台配置(下拉)/导航配置` 已正确。
- `page.tsx`（重写为纯前端 mock）：
  - `.nv-notice` 须知框（`i` 图标 +「须知」标题 + 3 行文案，末行含 `常用链接地址` 蓝色链接）。
  - `.admin-card`「导航配置」卡片，内 6 个 `.nv-block` 可折叠分区。
  - **前 5 区**（头部主导航/头部主导航二/底部导航/首页专用导航/会员中心导航）：`.nv-block-head` 标题+右侧折叠箭头（collapsed 旋转 -90deg）；区块体 `.nv-styles` 5 等分 `风格1~5`（每风格 = 竖排彩色圆角图标 `.nv-style-glyph`（彩色底+glyph 符号占位）+ 图标名 + `.nv-style-name` 风格名 + 底部「一键切换」蓝钮）+ `.nv-items` 导航项列表。
  - **导航项行 `.nv-item`**：名称输入框 + 路径输入框(灰字) + 「更改图标」按钮（含 🎨 色块）+ 蓝色「确定」+ `.nv-switch` 圆形开关(蓝=显示/灰=隐藏，可点切换) + `.nv-tip` 状态提示（底部导航「游客/普通会员/VIP会员状态下显示的图标」带 `i` 圆圈）+ `↑上移/↓下移`（首行禁上移、末行禁下移，可真实交换顺序）+ 右侧 `›`。
  - **第 6 区「更多图标样式自由换装」**：`.nv-table` 表格 5 列（图标位置/当前图标/图标名称/最佳尺寸/操作），20 行数据（首页 11 + 资料页 9），当前图标=彩色 `nv-glyph-box`，部分行操作含「更换颜色」(蓝)+「恢复默认」。
  - 交互：`.nv-block-head` 点击折叠/展开；`.nv-switch` 切换 show；上移/下移真实交换 items；名称/路径输入受控。
- `globals.css`：末尾新增全套 `.nv-*` 样式（须知/卡片/分区折叠头/风格预览/彩色图标/一键切换/输入框/更改图标/确定/开关/提示/移动链接/图标表格各列）。
- `npx tsc --noEmit` 通过。

## 平台配置→平台布局页（4 tab 完整页）
侧边栏「平台配置→平台布局」指向 `/platform-page`，重写该路由（原为空 ListPage）。

### 改动
- `src/app/(admin)/platform-page/page.tsx`：重写为「须知框 + 4 tab（手机端首页/会员资料页/红娘团队展示页/电脑端）」大页面，`.pl-` 前缀。
  - **手机端首页**：首页布局设置 5 行（轮播图片/图标导航/数据统计/红娘喜讯/文字导航，开关(开·关)+上下移+「配置图片/配置导航›」链接）；数据统计样式选择 6 卡；文字导航样式选择 10 个彩色「最新」胶囊；红娘喜讯样式选择 4 卡；默认会员展示样式 6 个手机样机卡(双列/列表/大图/简介/简约/醒目)；委托红娘模板 3 卡；文字导航栏配置（自定义显示名称输入+选择活动分类下拉(活动指定分行)+开关+上下移）；首页置顶/VIP会员显示数量「最多 N 人」+提示；三个排序规则 radio（按排序值/随机）+提示；数据统计栏 4 基数字段+提示；允许用户自由切换样式 6 胶囊；确定提交。
  - **会员资料页**：会员资料页设计效果图「点击查看」；会员的推广红娘信息 radio(显示/不显示)+效果查看；VIP宣传弹窗 radio(开启/关闭)+点击查看+提示；布局配置「自定义显示名称」10 行（Ta的认证/个人资料/自我介绍/更多信息/Ta的语音/理想另一半/红娘说(隐藏)/爆灯嘉宾/收到的礼物/联系方式，显示开关+上下移）；底部按钮（主按钮功能 线上牵线 radio、登录前后文案输入、次按钮功能 爆灯/申请约见/打招呼 radio、显示文案）+提示+确定提交。
  - **红娘团队展示页**：预览红娘团队展示页面效果「点击这里」；导航文案 2 输入；页面标题；分享图标 radio(默认/云端素材库)+红色「红娘团队」缩略图+提示；分享描述；头部轮播图（上传+最佳尺寸 700×240，2 张渐变图各带下移/上移/删除）；加入红娘/红娘管理按钮文案；确定提交。
  - **电脑端**：电脑端引导页模板 5 个紫色海报缩略图+「自定义」+确定提交。
- `globals.css`：末尾追加全套 `.pl-*` 样式（tabs/区块/布局行/radio 样卡/手机样机/文字胶囊/导航行/数量输入/提示框/数据统计输入/切换胶囊/轮播/电脑端缩略/提交）。复用已有 `.nv-switch/.nv-move/.nv-input/.nv-ok-btn/.nv-notice/.pcfg-radio`。
- `npx tsc --noEmit` 通过（无后端，纯前端模拟）。

## 平台配置·权限配置（power-config）
- **重写** `src/app/(admin)/power-config/page.tsx`（路由 `/power-config`，原空 ListPage，侧栏+面包屑均指向）。单页多卡片（非 tab）：
  - 面包屑「首页 / 平台配置 / 权限配置」+ `nv-notice` 须知（VIP会员/会员/用户 3 行定义）。
  - **卡片1 平台浏览展示权限**：会员登录默认显示 radio(异性/同性●/所有)；未完善资料浏览量限制 迷你输入[2]+「页」+选中 radio note「每页显示10人…」；进入其他会员资料页权限（内联小标题「未完善资料:」radio 禁止/允许●）；已脱单会员展示 radio(展示/隐藏●,仅在"官宣"列表中显示)。
  - **卡片2 会员资料页内容查看权限**：照片视频查看权限（非VIP可浏览照片[1]张/视频[0]个）；语音/红娘说/择偶要求/个人资料/自我介绍/更多资料 查看权限 radio(所有会员/仅VIP)。
  - **卡片3 会员状态更改权限**：顶部蓝底 Info（这里是指会员自己在"会员中心-状态设置"…）；委托红娘/完全私密 radio(所有会员/VIP-银卡/VIP-金卡/VIP-钻石会员●/完全私密多一个"不允许会员自己设置")；暂停服务/我已脱单 radio(允许/不允许●+note「(会员操作时弹出提示"请联系您的红娘")」)。
  - **卡片4 会员线上牵线权限**：右上「图文解说」蓝钮（点击展开 Info）；没有实名认证是否允许发起牵线 radio(允许/禁止●+note)；没有签署承诺书 radio(允许/禁止●+note)；发起线上牵线次数限制 迷你输入[0]+「次/天（0表示不限制）」+行内 hint「i 此功能对于"介绍到结婚为止"的收费模式非常关键」；没有实名认证的是否允许被牵线 radio(禁止●/允许)+缩进蓝底 Info（设置为"禁止"…）。
  - **卡片5 其他权限配置**：实名时强制上传身份证照片 radio(不强制上传●/强制上传+note)；会员中心展示专属红娘 radio(显示●/隐藏)；同一身份证号允许多个账号 radio(允许/不允许●)；强制用户上传头像/照片 radio(开启●/关闭)+缩进 Info；是否允许会员自行修改婚况/学历 radio(允许/不允许●)+缩进 Info。
- 结构：复用 `.admin-card`+自建 `Card`（头部标题/右上 tools）、`Row`(label+body)、`Radio`(pcfg-radio+note)、`MiniInput`、`Info`(indent 缩进到内容列)。41 个 radio 全部用 useState 可点切换。
- `globals.css`：末尾追加全套 `.pm-*` 样式（页面/卡片头/标题/右上工具钮/行/行 label/行内说明 note/内联 label/内联单位/迷你数字输入/行内 hint/蓝底 info/提交行）。
- `npx tsc --noEmit` 通过（无后端，纯前端模拟）。

## 平台布局·开关统一滑块式
- `platform-page` 首页布局设置的开关原用 `.nv-switch`（圆点固定+文字），与主流滑块式（`.pcfg-switch/.tm-switch/.mp-switch`：text 状态文字+knob 滑动圆点）不一致。
- 第一次尝试：自建 `.pl-switch`（text absolute 定位）导致开字与圆点重叠。
- 最终做法：`src/app/(admin)/platform-page/page.tsx` 的 `Switch` 组件直接复用 `.mp-switch`（on 显示 label 文字 + knob 滑动到右，off 仅 knob 在左无文字），与小程序/公众号开关 100% 一致。
- `globals.css`：删除无效的 `.pl-switch` 段（未遗留死代码）。
- `npx tsc --noEmit` 通过。

## 内容配置页·3 tab（文案配置/自定义页面/会员中心）
- 重写 `src/app/(admin)/platform-content/page.tsx`（原空 ListPage），面包屑用 `getBreadcrumb("平台配置","内容配置")`。
- 文案配置：行式表单 label 居右灰字，含平台首页弹窗(mp-switch"开启"+蓝底tip"同一IP每1小时只弹出一次")、非会员查看弹窗、相亲会员注册弹窗、相亲会员协议(长合同)、VIP-挚爱专享、加入服务红娘/推广红娘/推广红娘中心介绍(富文本 cf-editor+渐变海报 Banner)、全民推广红娘宣传口号(*必填)、线下约见服务介绍(textarea)、线下VIP提示(textarea)、资料推广描述、确定提交。
- 自定义页面：关于我们(富文本+指纹Banner)、私人定制名称(input)、私人定制描述(富文本+Banner+4条目 icon/title/desc)、相亲防骗提醒(input+富文本+Banner)、"小心有以下行为的用户"(竖排左栏+蓝底警示条列表)、确定提交。
- 会员中心：nv-notice 须知(页面效果蓝链)+表格(状态名|自定义状态名文案限6字|描述文案)5行=公开展示/委托红娘/暂停服务/已经脱单/完全私密，input/textarea 可编辑，确定提交。
- `globals.css`：末尾新增全套 `.cc-*`（tabs/行表单/blue-tip/编辑器/渐变banner/textarea/条目列表/warn 警示/表格/输入框）。复用 `.mp-switch/.cf-tool/.nv-notice/.nv-ok-btn/.cf-input`。
- `npx tsc --noEmit` 通过（纯前端 mock）。

## 平台配置·基础数据 (/platform-base 主数据页)
- 重写空 ListPage 为长滚动单页：面包屑+nv-notice 须知+29 张分区卡片（审核不通过原因模板/会员标签/客户来源/客源状态/客户意向/民族/结婚意向/职业/学历/婚姻状况/情感状态/收入状况/购房情况/宗教信仰/性格标签/爱好标签/喝酒/吸烟/择偶要求-喝酒/吸烟/购房/婚姻状况/自白模板/跟进模板/举报分类/商家分类/单位分类/线下VIP套餐/人格类型MBTI）。
- 每卡片=分区标题+胶囊列表（每个胶囊=`<`+名称(+emoji)+✎编辑icon+✗删除icon+`>`，淡蓝底圆角）+末尾「+添加分类」蓝胶囊钮。交互：✗ 从列表移除（setData filter）、✎ 打开抽屉预填、+添加 打开空抽屉。
- 「添加分类」抽屉（app-modal 右侧滑出）：头部 × + 标题 + 右上取消/确定提交；表单 label 居右 —— *分类名称(input)、模板内容(textarea placeholder"请输入")、分类图标(+40*40 上传框 + 〓从云端素材选择)。
- 数据用 `t(label, emoji?)` 生成 Tag(id自增)；`sections` 为初始 seed，state 驱动增删编辑。
- `globals.css`：末尾新增全套 `.bd-*`（wrap/notice/group/section/inner/title/pills/pill(arrow,emoji,edit,del)/add-btn/modal 表单(label/req/input/textarea/icon-upload/icon-cloud)）。复用 `.app-modal/.admin-card/.nv-notice`。
- `npx tsc --noEmit` 通过（纯前端 mock，无后端）。

## 平台配置·收费配置 (/platform-payconfig 长滚动单页)
- 重写空 ListPage：面包屑+须知(微信商户号/财务管理-收入明细)+12 张卡片(资料审核费/实名认证费/婚姻状况查询/牵线有效期/单次线上牵线费/线上牵线套餐10行/线上VIP会员3类型/资料置顶6套餐/推广红娘+合伙红娘入伙费/会员爆灯/资料推广)+底部确定提交。
- `Board`(标题+右上图文解说钮)、`Row`(label+input+单位)、`Tip`(蓝点提示)、`Radio`(复用 pcfg-radio)。卡片头标题左带 3px 蓝色小竖线。图文解说钮为淡蓝底蓝字。
- 表单行：label 居左(宽150)+数字框+.caret 说明。蓝底 info 框 `pay-bluebox`+圆形 `pay-dot`。
- 线上牵线套餐 10 行：名称/牵线N次/收费N元/mp-switch 开关。线上VIP 3 类型：名称/有效期N天/收费N元/送N次牵线/权益描述链。资料置顶 6 套餐：置顶N天/收费N元。合伙红娘 3 级别收费，分列。
- `globals.css`：末尾新增全套 `.pay-*`（wrap/board/board-head/title(::before 蓝竖条)/tut-btn/board-body/row/row-label/row-controls/num/unit/tip/tip-dot/bluebox/dot/vip/table/tr/td/td-label/inline/center/col/link/submit/submit-btn）。复用 `.admin-card/.nv-notice/.pcfg-radio/.mp-switch`。
- `npx tsc --noEmit` 通过（纯前端 mock，无后端）。

## 系统管理·二级/三级混合菜单 (AdminLayout 侧边栏)
- 用户提供截图：系统管理下「系统配置/广告管理/系统日志」为二级直接项；「电话外呼/短信系统/后台账号」为三级父级，各自可折叠展开子项（下拉箭头）。
- `AdminLayout.tsx`：
  - 类型升级：`type MenuItem = { label; href?; children?: Item[] }`，`Group.items` 改为 `MenuItem[]`。
  - 新增 `findInItems(items, pathname)` 递归 helper（叶子用 href 匹配，父级命中任一子项即视为命中该父级）。
  - `activeGroup` 改用 `findInItems(group.items, pathname)`；新增 `activeSub`（命中的父级或叶子）。
  - 新增 `openSubGroups` 三级展开状态 + `toggleSub` + `useEffect`（当前路由所在父级自动展开）。
  - 渲染：父级=button+ChevronDown/Right+高亮(`bg-[#f0f5ff] text-[#3658f7]`)；三级子项缩进 `pl-[70px]`；二级叶子仍 `pl-11`。
  - serviceGroup 无 children，其 items 标注 `as Item[]`，渲染 `href={item.href!}`。
- 系统管理菜单结构改为：系统配置/广告管理/电话外呼(外呼平台·外呼状态·呼叫记录)/短信系统(签名配置·通知配置·短信群发·发送记录)/后台账号(添加账号·账号管理·权限分组)/系统日志。
- 新增页面 `system-setting-admin-user-add/page.tsx`（添加账号表单：登录账号/昵称/密码/确认密码/所属角色单选，面包屑 `getBreadcrumb("系统管理","添加账号")`），避免菜单指向 404。
- `npx tsc --noEmit` 通过。

## 系统管理菜单·key 警告排查（已修复，非最终代码问题）
- 用户报 React "unique key prop" 警告 + `<Link href=undefined>`。查 `.next/dev/logs/next-development.log`：两错误同源，均发生在 HMR 中间态——菜单数据先改为 parent 项（无 href），渲染逻辑后改，热更新瞬间浏览器以"新数据+旧渲染"跑了一帧，电话外呼/短信系统/后台账号被旧 leaf 逻辑渲染成 `<Link href=undefined key=undefined>`。
- 最终代码 parent 项渲染为 button（不走 Link），所有 map 的 key 有效；`npx tsc --noEmit` 通过；curl SSR 各页面无新错误。
- 结论：浏览器 overlay 为 stale，刷新即可消除。经验：多步 Edit 改共享组件时，数据结构与渲染逻辑应在同一批次提交，避免 HMR 中间态污染用户浏览器。

## 系统管理·系统配置 (/system-setting-basic 多 tab 长页面)
- 重写空 ListPage 为 7 tab 单页：基本信息/注册访问/文件存储/支付配置/图片水印/海报配置/自定义区域，面包屑 `getBreadcrumb("系统管理","系统配置")` + 手动追加当前 tab 名（四级：首页/系统管理/系统配置/{tab}）。tab 激活蓝色 + ::after 下划线。
- 组件化：`Switch`(复用 .mp-switch，on 显"开启")、`Radio`(复用 .pcfg-radio)、`Info`(蓝底信息条 .sy-info)、`Row`(label 居右 132px)、`UploadBox`、`Editor`(工具栏 + 内容区，高>200 时 .sy-editor-scroll 滚动)。
- 基本信息：经营主体/域名绑定(https://前缀+输入框+SSL证书钮)/域名备案/公安备案/运营区域/客服电话/客服微信/客服微信二维码(64px 假二维码+上传小钮)/电脑尾部(编辑器)/后台LOGO/红娘工作台LOGO/用户协议/隐私政策(编辑器滚动)+确定提交。
- 注册访问：是否开启浏览/登录安全增强/是否开启注册(开关)/IP限制类型(黑名单·白名单 radio)/IP设置(textarea)/注册短信验证码(开关)，均有 Info 须知。
- 文件存储：顶部须知框(七牛) + 七牛accessKey/secretKey/空间名称/上传地址/远程地址/私有队列 input。
- 支付配置：内部子tab(微信支付配置·支付宝配置)。微信：是否启用/appid/商户号/证书类型(平台模式·公钥模式 radio)/公钥证书ID/微信支付公钥(textarea)/APIv2/apiV3 密钥/p12证书上传+data说明/显示排序。
- 图片水印：左表单(水印开关/水印类型 radio/水印文字/水印字体下拉/缩放比例 slider+Info/字体大小 slider/文字颜色色块/旋转角度/透明度 slider/填充尺寸 宽高px+Info)+右侧效果预览(渐变占位 .sy-wm-photo+斜向水印文字)。
- 海报配置：须知框 + 表格(海报名称/是否永久二维码(永久绿·临时红 .sy-tag)/扫码强制引导关注公众号(行内 Switch))，10 行数据，state 驱动开关。
- 自定义区域：头部(区域数据管理 + 右侧云端数据迁移下载链接 + 添加省份蓝钮) + 空状态(图标+文案)。
- `globals.css`：末尾新增全套 `.sy-*`(wrap/tabs/tab/body/form/row/label/req/ctrl/input/textarea/domain/info/info-i/link/qrcode/upload/upload-sm/p12/editor/editor-tools/tool/content/scroll/subtabs/subtab/table/th/td/tag(perm,temp)/wm/wm-form/wm-preview/preview-title/wm-photo/wm-text/select/range/color/empty/region/region-head/title/add-btn/empty-text/save-btn)。复用 `.mp-switch/.pcfg-radio`。
- `npx tsc --noEmit` 通过；SSR 输出"正在验证管理员身份"占位(客户端 hydrate 后显示业务，AdminLayout authReady 校验所致)，无编译错误。

## 系统管理 › 广告管理（/system-setting-adconfig）
- 重写为自定义表格页（弃用 ListPage 骨架）。面包屑 4 级：首页/系统管理(可下拉)/广告管理/全部广告，用 `AdminBreadcrumb` 手写 items。
- 结构：`adcfg-head`(左侧"广告管理"标题 + 右侧蓝色"+ 添加广告"钮) + `adcfg-table`(广告编号/广告标题/广告类型/平台/状态/操作) + `admin-pagination` 右分页(‹ 1 ›)。
- 数据 13 行 mock（h5_index/active_h5_index/active_mp_index/.../video_mp_index），类型分 轮播图/图片，平台分 H5端/小程序端；状态用 `.mp-switch` 开关，行状态 state 驱动；操作列"编辑"绿色链接钮。
- 表头列宽固定：code 22% / title 34% / type 14% / platform 14% / status 10% / op 6%；`adcfg-table` min-width 900px 横向滚动；行高 58px 顶部分隔线。
- `globals.css`：末尾新增全套 `.adcfg-*`(page/head/title/add/add-plus/body/table/th/td 列宽系列/td-title/td-op/edit/switch)。复用 `.mp-switch/.admin-pagination/.admin-card`。
- `npx tsc --noEmit` 通过；页面 200 正常。

## 系统管理 › 电话外呼 › 外呼平台（/outbound-call-platform）
- 重写为自定义表单页（弃用 ListPage 骨架）。面包屑 4 级：首页/系统管理(可下拉)/电话外呼(纯文字)/外呼平台(当前)，用 `AdminBreadcrumb` 手写 items，电话外呼仅为三级分组父级显示。
- 结构：顶部通知条 `.obc-notice`(浅蓝圆角，蓝色圆点"!" + "通知"加粗标题 + 两行正文) + `.obc-card`(头"外呼平台"+表单)。
- 表单(复用 `.sy-row/.sy-label/.sy-ctrl/.sy-input`、`.pcfg-radio`，label 靠右 132px，行 align-items:center)：
  - 外呼平台服务商：radio 单选(建讯通讯 默认选中/厚朴通讯/云客)，state 驱动。
  - 建讯外呼账户名称：input placeholder "请输入建讯外呼账户名称"。
  - 建讯外呼呼叫中心地址：input placeholder。
  - 录音下载地址：readonly input，value "https://node-api-gz-hunlian01.puntel.com"，灰底灰字。
  - 保存：蓝色 `.obc-save`，缩进左对齐 label 宽度。
- `globals.css`：末尾新增全套 `.obc-*`(page/notice/notice-i/notice-body/notice-t/notice-desc/card/head/body/form/radio-wrap/input/input-readonly/actions/save)。复用 `.sy-row/.sy-label/.sy-ctrl/.sy-input/.pcfg-radio/.admin-card`。
- `npx tsc --noEmit` 通过；页面 200 正常。

## 系统管理 › 电话外呼 › 外呼状态（/out-call-list）
- 重写为「统计卡片 + 表格空状态」页（弃用 ListPage 骨架）。面包屑 4 级：首页/系统管理(可下拉)/电话外呼(纯文字)/外呼状态(当前)。
- 顶部通知条复用 `.obc-notice`（"须知"标题 + 3 行正文）。
- 5 个统计卡片 `.oc-stats`(grid 5列)：外呼服务商/外呼账号/坐席数量/所有坐席通话总时长/本月通话总时长。每卡=顶部[彩色圆图标(lucide User/Armchair/Clock/Phone，外呼账号无图标) + 灰色小标签] + 下方大字值(捷讯通讯/未开通服务/0/0秒/0秒)。图标色：蓝#3658f7/橙#fa8c16/紫#722ed1/红#f5222d。
- `.oc-card` 头"外呼状态"+右上蓝色"+ 添加坐席"钮；12 列表头(坐席工号/坐席状态/外呼号码/绑定服务红娘/呼出次数/接通次数/呼出通话总时长/平均时长/本月呼出通话总时长/当天呼出次数/当天呼出时长/操作)，min-width 1280px 横向滚动；空状态行 lucide `Inbox`+"暂无数据"。
- `globals.css`：末尾新增全套 `.oc-*`(page/stats/stat/stat-top/stat-icon/stat-svg/stat-label/stat-value/card/head/title/add/add-plus/body/table-scroll/table/th/empty/empty-icon)。复用 `.obc-notice*`/`.admin-card`。
- `npx tsc --noEmit` 通过；页面 200 正常。

## 系统管理 › 电话外呼 › 呼叫记录（/out-call-record）
- 重写为「通知条 + 搜索栏 + 表格空状态」页（弃用 ListPage 骨架）。面包屑 4 级：首页/系统管理(可下拉)/电话外呼(纯文字)/呼叫记录(当前)。
- 顶部通知条复用 `.obc-notice`（"须知"+一行正文）。
- `.rec-card` 头"呼叫记录"(标题左对齐，无右钮)；搜索栏 `.rec-bar`(浅灰底)：
  - 左：radio 相亲会员(默认选中)/客源线索，复用 `.pcfg-radio`。
  - 右工具条：下拉"所有坐席"(`.rec-select` 自定义箭头) + [下拉"按昵称搜"+ 输入框"请输入"] + 蓝色"搜索"按钮 + 日期范围(开始日期→结束日期，`.rec-input` date)。
- 9 列表头(状态/呼出信息/通话次数/呼出坐席/呼出开始时间/挂机时间/通话时长/录音回放/本次通话小记(同步到服务跟进))，min-width 1080px 横向滚动；空状态行 lucide `Inbox`+"暂无数据"。
- `globals.css`：末尾新增全套 `.rec-*`(page/card/head/title/bar/bar-left/toolbar/select-wrap/select/behavior/search-group/input/search-btn/range/date/range-arrow/body/table/th/empty/empty-icon)。复用 `.obc-notice*`/`.pcfg-radio`/`.admin-card`。
- `npx tsc --noEmit` 通过；页面 200 正常。

### 签名配置页 `/sms-signature`（2026-09-08）
- 由 ListPage 骨架改为表单页：面包屑 首页/系统管理(可下拉)/短信系统/签名配置；通知条(须知，两行正文+相关政策蓝链)；卡片头"短信签名"。
- 表单一行：label"您的短信签名"靠右 + 灰底只读输入框"南京信达宜管家"；下方蓝色 info 条(.sign-info 圆图标+「需要修改请对接系统服务商为您申请」)。
- `globals.css`：末尾新增 `.sign-info`/.sign-info-i。复用 `.obc-notice*`/`.sy-row/.sy-label/.sy-ctrl/.sy-input`/`.admin-card`。
- `npx tsc --noEmit` 通过；页面 200 正常。

### 通知配置页 `/sms-notices`（2026-09-08）
- 由 ListPage 骨架改为「通知条 + 搜索栏 + 长滚动表格」：面包屑 首页/系统管理(可下拉)/短信系统/通知配置；通知条「只有启用状态下的通知项目系统才会发送短信」。
- 卡片头「通知配置」+ 左侧搜索输入框(请输入关键字搜索)+蓝色「搜索」按钮。
- 5 列长表格(编号/短信通知场景描述/短信内容示例(加宽 min480px)/通知对象/状态)，min-width 1480px 横向滚动；68 行 mock 数据，编号含 1~74 跳跃(30/31/32/33/50/51/52...)及 1041/1042/1151/1293/1294/1347~1359/1439/1463/1506。
- 通知对象列双态：普通行显示灰字(前台会员/任意用户)；管理员行显示标签组(admin× / 8× / 7×admin×)+「请选择管理员」下拉(单管理员进显示灰色幽灵「请选择要通知的管理员」)。
- 状态列复用 `.mp-switch`(.on 时显示「启用」)+toggle 交互。
- `globals.css`：末尾新增全套 `.notif-*`(page/card/head/bar/search/search-input/search-btn/table-wrap/table/col-id/col-scene/col-content/col-target/col-status/td-*/target-text/admin/tags/tag/tag-x/select/select-label/select-ghost)。复用 `.obc-notice*`/`.mp-switch`/`.admin-card`。
- `npx tsc --noEmit` 通过；页面 200 正常。

### 短信群发页 `/sms-group`（2026-09-08）
- 由 ListPage 骨架改为「通知条 + 卡片头 + 表格空状态」：面包屑 首页/系统管理(可下拉)/短信系统/短信群发；通知条「本系统整合开发了腾讯短信的群发功能，可以对指定的用户群发各种合规短信内容，提升平台与用户的连接」。
- 卡片头左「短信群发」+ 右上 3 个蓝色按钮：+创建群发任务 / 短信充值(圆图标) / 提交群发模板(文件图标)。
- 8 列表头(任务名称/创建时间/发送对象/手机号码清单/任务状态/发送统计/发送明细/操作)，min-width 1100px 横向滚动；空状态 lucide `Inbox`+"暂无数据"。
- `globals.css`：末尾新增全套 `.grp-*`(page/card/head/title/actions/btn/btn-plus/btn-ico/table-wrap/table/th/td/empty/empty-icon/empty-text)。复用 `.obc-notice*`/`.admin-card`。
- `npx tsc --noEmit` 通过；页面 200 正常。

### 发送记录页 `/sms-record`（2026-09-08）
- 由 ListPage 骨架改为「通知条 + 统计卡 + 空状态」：面包屑 首页/系统管理(可下拉)/短信系统/发送记录；通知条(须知)3 行正文(本系统接入腾讯云短信接口...第 1、2 条发送失败场景扣除条数说明)。
- 4 统计卡(带分隔边框圆角容器)：当前线路：腾讯云专线 / 短信余量：9410条(蓝字)+「在线充值」蓝钮 / 发送成功：1585条(绿字) / 发送失败：434条(红字)。
- 卡片头左「发送记录」+ 右上蓝色文字「错误码查询」；空状态 lucide `Inbox`+"仅限超级管理员（admin）查看"。
- `globals.css`：末尾新增全套 `.srec-*`(page/stats/stat/stat-line/stat-label/num/num-blue/num-green/num-red/recharge/card/head/title/query/empty/empty-icon/empty-text)。复用 `.obc-notice*`/`.admin-card`。
- `npx tsc --noEmit` 通过；页面 200 正常。

### 添加账号页 `/system-setting-admin-user-add`（2026-09-08）
- 由简易表单重写为截图表单：面包屑 首页/系统管理(可下拉)/后台账号/添加账号(4 级，用 AdminBreadcrumb，`getBreadcrumb` 结果中插入"后台账号"段)；卡片头"添加账号"，表单 max-width 640px。
- 字段：分组(下拉 select，placeholder"请选择分组"，选项 管理员组/红娘组/客服组)/账号(placeholder"3-28字符，最多14个汉字")/密码(password)/姓名(placeholder"4-16字符，最多8个汉字")/手机(空输入)。
- 手机下 + 定时锁定下各一条蓝色提示条 `.sign-info`(lucide `Info` 圆i图标)：`接收短信通知和后台登录`；`开启定时锁定后，到了时间后该账号自动锁定`。
- 定时锁定行：`.mp-switch` 开关(默认 off，on 时内部 label"开启"+) + 右侧静态文字"开启"；底部 `.addacct-actions` 内"确定提交"蓝钮。
- `globals.css`：末尾新增全套 `.addacct-*`(card/title/form/row/label/req/content/input/select/info/switch-row/switch-text/actions/btn)。复用 `.sign-info`/`.mp-switch`。
- `npx tsc --noEmit` 通过；页面 200 正常。

### 账号管理页 `/system-setting-admin-user`（2026-09-08）
- 由 ListPage 骨架重写为自定义表格页：面包屑 首页/系统管理(下拉)/后台账号(下拉/添加账号·账号管理·权限分组)/账号管理(4 级，复用 AdminBreadcrumb)。
- 卡片头"账号管理"；筛选栏(浅灰底 flex)：状态段按钮组(全部[选中蓝]/正常/已锁定，`.au-tab-active`) + 下拉"全部▾"(定时锁定筛选) + "分组：不分▾" + 手机号输入框(placeholder"请输入手机号") + "搜索"蓝钮。
- 表格列：复选框/用户名/姓名/用户组/手机号/绑定微信/添加时间/一般锁定/定时锁定/操作；2 行 mock(admin 无删除钮)。绑定微信有值绿色"已绑定"，空值灰"-"；一般锁定绿色"正常"▾(可点切换)；定时锁定灰"未开启"；操作"编辑"蓝 + "删除"红。
- 分页底色按钮 `‹ 1 ›` 右下角。
- `globals.css`：末尾新增全套 `.au-*`(card/head/title/filter/tabs/tab/tab-active/fd/fd-label/fd-chevron/group/group-label/phone/search/table-wrap/table/th-check/td-check/wechat-bind/wechat-none/lock/lock-chevron/timed/actions/edit/del/pagination/page-btn/page-num)。复用 `.admin-card`。
- `npx tsc --noEmit` 通过；页面 200 正常。

### 权限分组页 `/system-setting-admin-group`（2026-09-08）
- 复用账号管理的 `.au-*` 样式：面包屑 首页/系统管理(下拉)/后台账号/权限分组(4 级)；卡片头左"权限分组"+右上蓝色"+ 添加用户组"按钮(`.au-head-between`/`.au-add`)。
- 表格列：用户组名/当前用户/操作；3 行 mock(超级用户组=admin、shushu，一般用户组/一般运营组=灰字"无当前用户"用 `.au-wechat-none`)；操作=编辑/权限管理/删除(蓝蓝红)。分页 `‹ 1 ›` 右下。
- `globals.css`：新增 `.au-head-between`/`.au-add`/`.au-add-plus`/`.au-group-table`/`.au-group-name`/`.au-group-users`。
- `npx tsc --noEmit` 通过；页面 200 正常。

### 系统日志页 `/system-setting-admin-log`（2026-09-08）
- 面包屑 3 级：首页/系统管理(下拉)/系统日志(`getBreadcrumb("系统管理","系统日志")`)，卡片 `.au-card` 同款无头。
- 7 个 tab 数据驱动(`TABS` 数组，`useState(active)`)：后台登录日志/后台密码修改日志/会员删除日志/客源线索删除日志/账号删除日志/会员CRM手机号修改日志/客源线索手机号修改日志。tab 行 `.syslog-tab` 蓝色下划线。
- 每 tab 可配 `notice`(须知通知条，复用 `.obc-notice*`)、`filter`(input 单输入 / range 管理员下拉+日期范围 / rangeInput 再加"被删除的账号/手机"+搜索)、`columns`、`rows`(纯 mock)、`showPagination`。
- 表格列差异：前两个 tab=ID/账号/时间/IP地址；后五个=ID/操作人/账号·会员·客源/操作时间/IP地址。target 列 `white-space:pre-line` 支持多行(账号删除含 `(ID: xxx)`、CRM手机号改动含`将手机号：X 修改为：Y`)。空 tab 显示 `Inbox`+"暂无数据"。
- `globals.css`：末尾新增全套 `.syslog-*`(card/tabs/tab/tab.active(::after 下划线)/notice/filter/select-wrap/select-holder/select/select-chevron/daterange/date/date-ico/date-text/date-arrow/input/input-w/search-btn/search-ico/table-wrap/table/td/target/empty/empty-icon/empty-text/pagination/page-btn/page-num)。
- `npx tsc --noEmit` 通过；页面 200 正常。

### 基本配置页表头对齐修复 `/platform-config-basic`（2026-09-08）
- 用户反馈"表头和列对齐"：原 4 张表(信息登记引导/基本资料登记/私密信息登记与展示/筛选功能)用 `display:flex` 的 div 行(`.pcfg-tr`)，每行独立按内容分配列宽(`flex:1 1 auto`)，表头行与数据行(尤其含 300px 输入框/下拉框的列)列宽对不上。
- 改为真 `<table>` + `table-layout:fixed`(4 张表全部改：`<thead><tr.pcfg-tr-head><th.pcfg-th>` / `<tbody><tr.pcfg-tr><td.pcfg-td>`)。`w-id 54px / w-show 120 / w-switch 100 / w-sort 150`，无宽列自动平分剩余。
- 表内 `.pcfg-input-inline` 与 `.pcfg-select select` 强制 `width:100%; box-sizing:border-box` 铺满列，杜绝溢出撑宽。
- `npx tsc --noEmit` 通过；页面 200 正常。


## 红娘课堂弹窗（MatchmakerClassModal）
- 顶部导航「红娘课堂」蓝色按钮（AdminLayout.tsx 顶部 ml-auto 区，bg-[#3d5cf1]+Bell）原本无 onClick，将其改为 `onClick={() => setClassOpen(true)}`，渲染 `<MatchmakerClassModal open={classOpen} onClose={() => setClassOpen(false)}/>`。
- 新增 `src/components/MatchmakerClassModal.tsx`：右侧 Drawer。标题「红娘课堂」+「关闭」钮；banner 紫图标+「资深行业老师分享婚恋运营经验」；搜索框+蓝色「搜索」钮；6 tab（红娘话术/线下活动/引流获客/红娘销售/红娘服务/收藏）；列表每条= 标题(可选红「必看」徽标/红字)+日期·已看过/未看过·♡未收藏+右侧圆形头像+名字(班长/君君/刘欢欢/曾晨)；底部「共 N 条」+分页。点击某条 → 面板加宽 `.mkc-wide`(522→1180px) 并在右侧出现视频播放器(黑底渐变画面+字幕「从业 8 年的红娘」+底部控制条 ◀ 进度条 0:03/7:01 音量 全屏)。
- 数据 mock 于组件内 `tabs` 数组（5 tab 各 9 条 + 收藏空态）。样式前缀 `.mkc-*` 追加到 globals.css 末尾。
- `npx tsc --noEmit` 通过。

## 更新报告弹窗（UpdateReportModal）
- 顶部「更新报告」灰按钮（AdminLayout.tsx translate-x-2 区，bg-[#3d455f]）接上 `onClick={() => setReportOpen(true)}`，渲染 `<UpdateReportModal open={reportOpen} onClose={...}/>`。
- 新增 `src/components/UpdateReportModal.tsx`：右侧 Drawer「系统更新消息」。搜索框+放大镜图标；6 tab（全部/功能升级/细节改进/新功能/BUG修复/其他更新）；列表每条= 分类(灰)+标题(可红字)+红点●+时间；底部「共 N 条」+分页。点击某条 → 面板加宽 `.ur-wide`(522→1180px) 右侧出现详情（标题/分类·时间/分隔线/正文/蓝色「＋ 提需求建议」按钮 + 复制内容）。
- 点击「提需求建议」→ 打开 `.ur-feedback` 全屏「添加反馈」抽屉（深蓝头部 #2f3350 + ✕ + 取消/确定提交；反馈类型 radio 默认「需求建议」+ 富文本编辑器 + 手机(默认 13285288888)/微信/QQ）。复用了 CloudMaterialModal 里的反馈样式思路但独立实现。
- 数据 mock 于组件内 `items` 数组按 category 划分，`catCount` 存各 tab 总条数（全部639/功能升级255/细节改进391/新功能16/BUG修复169/其他更新4）。样式前缀 `.ur-*` 追加到 globals.css 末尾。
- 注意：字符串内不能用英文双引号，会 TS1005，中文引号要改「」。`npx tsc --noEmit` 通过。

## 用好系统弹窗（UseSystemModal）
- 顶部「用好系统」灰按钮（AdminLayout.tsx translate-x-2 区，bg-[#3d455f]）原本是无 onClick 的 `<span>`，改为 `<button onClick={() => setUseSystemOpen(true)}>`，渲染 `<UseSystemModal open={useSystemOpen} onClose={...}/>`。
- 新增 `src/components/UseSystemModal.tsx`：**左侧** Drawer（注意：与红娘课堂/更新报告的右侧 panel 不同，`.us-panel` 用 `left:0`）。标题「用好系统」+「关闭」钮；banner 紫图标 +「教您用好婚恋系统」；搜索框+蓝色「搜索」钮；列表每条= 徽标（红「必看」/橙「新上」渐变）+ 标题 + 日期；底部「共72条」+分页 `‹ 1 … 4 ›`。点击某条 → 面板加宽 `.us-wide`(522→1180px) 右侧出现图文详情。
- 详情区 `.us-detail`：顶部封面图 `.us-cover`（紫渐变 + 紫色横幅「销售红娘匹配库」+ 白色大字副标题「线下面谈开单神器」），下方正文段落（以小标题 `h` 分组）。第一条约 3 段正文按截图 mock，其余条目为占位短文。
- 数据 mock 于组件内 `docs` 数组（10 条，含徽标必看/新上）。样式前缀 `.us-*` 追加到 globals.css 末尾（`.us-badge-red`/`.us-badge-orange` 渐变徽标）。
- `npx tsc --noEmit` 通过。

## 充值弹窗（RechargeModal）
- 顶部「充值」灰按钮（AdminLayout.tsx translate-x-2 区，bg-[#3d455f]）原本是无 onClick 的 `<span>`，改为 `<button onClick={() => setRechargeOpen(true)}>`，渲染 `<RechargeModal open={rechargeOpen} onClose={...}/>`。
- 新增 `src/components/RechargeModal.tsx`：右侧大 Drawer（`.chg-panel` 宽 1180px）。左上角 ✕ +「充值」标题；蓝色须知条（💡须知：避免短信/人脸核验余量不足...）；4 张余额卡（短信9410条/人脸核验786条/电子合同150条/婚姻状况1次）；四类充值套餐组（短信余量充值/人脸核验余量充值/电子合同余量充值/婚姻状况权威数据查询余量充值），每组含 ①费用标准注释 + 3 个套餐（名称 + 橙色 ¥价 + 蓝色「充值」按钮）。
- 点击某套餐「充值」→ 弹出居中 `.pay-panel`「在线支付」小弹窗（340px）：标题「在线支付」+ 右上 ✕ + 200px SVG 二维码 + 「请使用“手机支付宝”扫码并支付」。
- 数据 mock 于组件内 `balances`/`groups` 数组。样式前缀 `.chg-*`/`.pay-*` 追加到 globals.css 末尾。
- `npx tsc --noEmit` 通过。

## 顶部搜索框·快捷导航下拉
- 顶部搜索框（AdminLayout.tsx placeholder「输入会员昵称/手机号/编号/姓名」，xl 显示）用 `<div className="relative hidden xl:block" onMouseEnter/onMouseLeave>` 包裹，新增 `navOpen` state。
- 悬浮显示 `.quicknav-panel` 下拉卡片：标题「快捷导航」(17px 加粗) + 3 列网格 12 个快捷入口按钮（客源线索/会员管理/牵线管理/会员匹配/线下会员/约会管理/会员分区/活动报名/推广红娘/服务红娘/收入明细/短信充值），每个为浅灰底圆角按钮，hover 变蓝 (`bg:#3658f7`)，点击跳对应路由。
- 面板默认 `opacity:0; visibility:hidden`，`onMouseEnter` 时加 `.quicknav-visible` 显示（含顶部小三角 `::before`）。
- `quickNav: Item[]` 常量 + 样式前缀 `.quicknav-*` 追加到 globals.css 末尾。
- `npx tsc --noEmit` 通过。

## 红娘工作台改外链
- 顶部「红娘工作台」由站内 `<Link href="/crm/home">` 改为 `<a href="https://www.xuanshiai.com/crm/user/login?redirect=/home" target="_blank" rel="noopener noreferrer">`，新窗口打开。
- `npx tsc --noEmit` 通过。

## 顶部个人中心下拉
- 顶部「头像+账号名」个人中心按钮（AdminLayout.tsx ml-auto 区，含 CircleUserRound+ChevronDown）用 `<div className="relative flex shrink-0 items-center" onMouseEnter/onMouseLeave>` 包裹，新增 `profileOpen` state。
- 悬浮显示 `.profile-panel` 下拉（宽 216px）：3 个菜单项（绑定手机/绑定微信/修改密码，`font-size:16px` 深色，hover 浅灰背景 `#f5f7fb`）+「退出登录」按钮（原 onClick 的 logoutAdmin 移到这）。
- 绑定手机/绑定微信暂跳 `/system-feedback`，修改密码跳 `/system-setting-basic`（占位路由）。菜单带顶部小三角 `::before`，默认 `opacity:0`，悬浮加 `.profile-visible` 淡入。
- 样式前缀 `.profile-*` 追加到 globals.css 末尾。
- `npx tsc --noEmit` 通过。

## 绑定手机→编辑账号页
- 个人中心下拉「绑定手机」原跳 `/system-feedback` 占位，改为跳 `/system-setting-admin-user-edit`（AdminLayout.tsx profile-item）。
- 新增 `src/app/(admin)/system-setting-admin-user-edit/page.tsx`：「编辑账号」页。面包屑 首页/编辑账号（2 级）。表单：分组下拉(默认超级用户组)/账号(shushu)/密码(placeholder 不修改请留空)/姓名(shushu)/手机(15996394511+接收短信通知提示)/定时锁定(mp-switch off)/微信(未绑定+「立即绑定」蓝钮)/确定提交。复用 `.addacct-*` 样式 + 新增 `.addacct-wx-row/.addacct-wx-unbind/.addacct-wx-btn`。
- `npx tsc --noEmit` 通过。

## 修改密码弹窗（ChangePasswordModal）
- 个人中心下拉「修改密码」原 `Link /system-setting-basic` 占位，改为 `<button onClick={() => setChangePwdOpen(true)}>`（AdminLayout.tsx）。新增 state `changePwdOpen`，渲染 `<ChangePasswordModal open={changePwdOpen} onClose={...}/>`。
- 新增 `src/components/ChangePasswordModal.tsx`：居中 Modal `.chgp-panel`(460px)。标题「修改密码」+ 右上 ✕；3 个密码字段（旧密码/新密码/确认密码，均带 `*`，placeholder 请输入旧密码/新密码/确认密码，右侧👁 eye 切换可见）；底部右侧 取消(白)/确定(蓝)。用 `PwdField` 子组件管理每行 visibility 状态，`onClose` 时遮罩点击关闭。
- 数据本地 state，确定暂不提交。样式前缀 `.chgp-*` 追加到 globals.css 末尾。
- `npx tsc --noEmit` 通过。

## 头部搜索→资料管理页
- 顶部搜索框（AdminLayout.tsx）改为受控 `searchKeyword` state。输入框 Enter 或点击放大镜图标 → `router.push('/love-user-list?keyword=<查询>')` 并关闭快捷导航。放大镜由 icon 改为 `<button>` 包裹。
- `love-user-list/page.tsx`：新增 mount `useEffect`，读取 `window.location.search` 的 `keyword`，若有则 `setFilters({...initialFilters, search:k})` 和 `setDraft(同)`，触发 load 重新拉取（资料管理→会员资料页，智能搜索框回填关键词）。
- 路由指向 `/love-user-list`（会员CRM→资料管理）。`npx tsc --noEmit` 通过。
