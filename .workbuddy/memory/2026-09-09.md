
## UI配置项清单逐项核对（财务管理及以后全量接线完成）
- 按 docs/ui配置项清单-财务管理及以后.md 逐项核对：16 个未接页面全部接线，产出 docs/UI配置项清单-逐项核对结果.md（逐页状态：✅真实数据源/🟦配置域存储/⚪占位及原因）。
- 本轮新接 16 页：财务合同4页（econtract_records/templates/seals/config）、外呼2页（outbound_seats/outbound_call_records）、短信2页（sms_broadcasts/sms_send_records）、后台账号3页（真实接口 GET/POST/PATCH /admin/matchmaker/accounts，分组权限注入新账号）、系统日志页（login-logs + 新增 audit-logs 接口按 action 前缀）、公众号6页（wechat_mp/fans/menu/replies/templates/broadcasts）、小程序页（wechat_mini）。
- 后端：admin_config.py 新增 16 个配置域；新增 GET /admin/matchmaker/audit-logs（business_audit_log，join 账号名）；wechat_mini.realname_enabled 接入 submit_realname（关闭→403 实名功能未开放，默认开行为不变）。
- 注意：showConfigToast 第二参是 "error"/"ok" 字面量不是 boolean；pickAndUploadImage(file, onDone, onError) 是回调式；ListPage keyword 字段需 key:"keyword" 才走 keywordValue 受控。
- 全量验证：tsc --noEmit 通过；后端 pytest 301 passed（4 个既有失败与改动无关，stash 已验证）。

## 财务管理接线（范围：系统配置+4个真实数据页）
- 菜单：财务管理分组 9 子页；经用户确认本次只处理 5 页（系统配置 finance-config、收入明细 system-finance-order、资金流水 system-credit-history、余额提现 system-cashout-history、统计报表 finance-statistic），合同相关 4 页（合同/模板/印章/合同配置）后端无电子签模块未处理。
- 后端（E:\houduan\xuanshiai）：① services/finance.py + schemas/finance.py + routes/finance.py 新增按日聚合 `GET /admin/finance/daily-report`（payment_order 按 pay_time 分组，status=1 收入、status=3 退款）；② 三个后台列表接口补时间范围/账户过滤 Query（orders/ledger/withdrawals，含 withdrawals 的 account_id）。
- 前端：ListPage.tsx 增加可选 keywordValue/onKeywordChange（本地关键字受控过滤，向后兼容）；finance-config 接 finance 配置域（6 个可控键行式编辑：payment_mode/withdrawal.enabled+min_amount/refund.manual_review/free_payment.enabled/e_contract.enabled，扁平↔嵌套映射，800ms 防抖保存）；三个列表页搜索键对齐后端（order_no/user_id/status/account_type/account_id/start_time/end_time）；cashout Tab 联动 status 真实筛选；statistic 接新日报接口。
- 审计：4 个数据页查真实表真实生效；finance 配置域仍无运行时消费（支付真实走 settings.wechat_payment_mode，提现/退款无开关判断）。文档：前端 docs/财务管理-按菜单层级配置总结.md；后端 docs/财务管理-后端执行逐项审计报告-2026-09-09.md。

## 系统管理纯配置页接线（与平台配置同套机制）
- 范围确认：仅接 5 个纯配置页（系统配置 7 Tab / 广告管理 / 外呼平台 / 短信签名 / 短信通知配置），业务记录页与后台账号、日志本次不接。
- 前端新增/改造：lib/platform-config.ts 增加 asNumber；5 个页面从静态 mock 改为 useConfigDomain 自动保存 + 提交按钮落库，图片位真实上传（复用 /admin/common/upload）；globals.css 增加预览与区域列表样式。
- 后端：services/admin_config.py 新增 10 个配置域默认：sys_site/sys_access/sys_storage/sys_payment/sys_watermark/sys_posters/sys_region/sys_ads/sys_outbound/sys_sms。均仅为管理端配置占位，无运行时消费（审计确认）。
- 文档：前端 docs/系统管理-按菜单层级配置总结.md；后端 docs/系统管理-后端执行逐项审计报告-2026-09-09.md（含 P0-P3 让配置真正生效的接入建议）。

## 平台配置总结改为"按菜单层级"视角
- 用户要求按后台菜单等级（一级分组 → 二级子菜单页面 → 页内 Tab/分区）总结，而非原功能六级分层。
- 已重写 docs/平台配置-接口接线与分级配置总结.md：新增"一、菜单等级结构"（平台配置组 → 7 个子页面树）、"二、菜单等级配置总结"按 7 页逐页列 Tab/卡片 → namespace → 配置内容；保留页面↔配置域速查、改动文件清单、保存行为。

## 添加反馈抽屉 ✕ 按钮修复
- UpdateReportModal "添加反馈"抽屉头部 `.ur-feedback-head`：原用内联 `<svg class="ur-feedback-x">` 直接嵌在 `.ur-feedback-title` 内，与「添加反馈」文字挤压重叠显示异常。
- 修复：改为独立 `<button class="ur-feedback-close" onClick={onClose}>`（lucide `X` 图标），与标题分离；`.ur-feedback-x` 设 `display:none` 废弃，新增 `.ur-feedback-close`（20px 按钮 + 16px X，白色，hover 变浅灰）。
- `npx tsc --noEmit` 通过。

## 财务管理菜单改三级嵌套
- AdminLayout.tsx 第64行「财务管理」由平铺 9 项改为二级/三级混合嵌套（复用 system 组的 children 渲染逻辑，无需改渲染）：
  - 2级：系统配置(/finance-config)/收入明细(/system-finance-order)/积分明细(/system-credit-history)/余额提现(/system-cashout-history)/统计报表(/finance-statistic)/电子合同(父级可折叠)
  - 电子合同 children：合同管理(/e-contract-list)/模板管理(/e-contract-template)/印章管理(/e-contract-yinzhang)/合同配置(/e-contract-config)
- `npx tsc --noEmit` 通过。

## 概览首页重构（home/page.tsx）
- home/page.tsx 由数据驱动(adminEndpoints.dashboard + fallback)改为**纯静态 mock**（不再 import DashboardCharts/PendingReviews/adminEndpoints），按截图还原。面包屑仍用 AdminBreadcrumb（首页/概览）。
- 布局自上而下：问候行(`😊 {greeting()}，shushu!` + 右侧「录入线索/录入资料/发布活动」蓝钮) → 7 统计卡(平台用户746/公众号粉丝0/客源线索9/会员总数640/红娘团队1/线上总收入4126.8元/线下总收入0，各带彩色文形icon+本月+N) → 待审工作(11项:会员承诺17/房产认证0/学历认证0/其他认证0/资料待审88/活动报名9/待牵线3/约见申请3/提现申请5/账号注销4/投诉举报0，每项「去处理」) → 中部4卡(会员增长趋势折线图7/15天tab/单位:人 + 会员登记来源占比环形图登记总数640人 + 会员男女占比环形图男女相差172人/男406人63%/女234人37% + 光荣榜) → 底部3卡(耗材余量「实名786次/短信9410条/合同150次/情况1次」+在线充值钮 / 系统更新列表 / 使用帮助列表)。
- 数据均 mock 于组件内常量：greeting()按当前小时返回凌晨/上午/中午/下午/晚上好；sourceData/genderData/memberTrend/honorList/materials/updates/helps。
- 复用 recharts(已安装)。样式前缀 `.ov-*` 追加到 globals.css 末尾(7列统计卡/11列待审网格/2.1fr-1.4fr-1.4fr-0.8fr 中部/1.4fr-1.4fr-1fr 底部，含 1280/720 响应式降级)。
- 「在线充值」→/sms-group、「系统更新更多」→/system-setting-admin-log、「使用帮助更多」→/use-system(占位路由)。`npx tsc --noEmit` 通过。

## 财务管理系统配置页（finance-config/page.tsx）
- 由空 ListPage(表格+搜索 + dataSource:[]) 重写为**纯静态表单页**，按截图还原（面包屑 AdminBreadcrumb = getBreadcrumb("财务管理","系统配置")）。
- 页面结构：「支付配置」标题卡片 `.fin-card` → 字段列表：
  - 积分名称=金币(输入框) / 积分比例 1元=[10]积分 / 余额名称=余额 / 余额提现(开关默认开) / 提现手续费(单选:不扣手续费 | 提现金额>=[100]元,扣手续费) / 手续费费率=1(%)
  - 蓝色信息条：按提现金额计算，四舍五入…(`.fin-info` 复用 sign-info-i 图标)
  - 「提现方式」分组标题 `.fin-sec-title`：自动提现到微信零钱(默认关)+单笔最小[1]/最大[500]+`fin-badge`蓝色提示；大段说明信息条；人工转账到微信/银行卡/支付宝(默认开)+单笔最小[1]/最大[1000]+「最小为1元,0为不限制」徽标
  - 「充值套餐」分组：6 套餐(1→10 /200→2200 /300→4000 /400→6000 /500→7500 /600→9000 积分) 每行 [金额]元 充值 [积分]积分
  - 底部「确定提交」蓝色按钮 `.fin-submit`
- state 管理：balanceWithdraw/feeMode/autoWechat/manualWechat/manualBank/manualAlipay；其余输入 defaultValue 静态。开关复用 `.mp-switch`。
- 前缀 `.fin-*` 追加到 globals.css 末尾：`.fin-card/.fin-title/.fin-form/.fin-row/.fin-row-nowrap/.fin-label(128px)/.fin-content/.fin-inline/.fin-muted/.fin-gap/.fin-input(240px)/.fin-mini(76px,.fin-mini:disabled)/.fin-fee-options/.fin-radio(accent-color蓝)/.fin-info/.fin-sec-title/.fin-amounts/.fin-badge(淡蓝块)/.fin-actions/.fin-submit`，含 720px 响应式降级(行内字段纵向堆叠)。`npx tsc --noEmit` 通过。

## 财务管理收入明细页（system-finance-order/page.tsx）
- 由空 ListPage(endpoint,/api/backend/admin/finance/orders + columns 编号/订单号/会员ID/商品/金额...)重写为**纯静态 mock 列表页**，按截图还原（面包屑 = getBreadcrumb("财务管理","收入明细")）。
- 顶部两行 tab 按钮 `.finord-tabs`(全部/积分充值/.../短视频打赏 + 短视频红包/社区缴费/.../线上收款，共23个，默认「全部」蓝色实心圆角)。
- 9 统计卡 `.finord-stats`(grid 9列，首卡带顶蓝边框 active)：总收入 4126.8元(首卡) / 今天(2026.09.09) / 昨天 / 本周 / 上周 / 本月 / 上月 / 今年 4126.8元 / 去年，其余均 0元。
- 筛选条 `.finord-filters`：全部支付状态▾/全部支付方式▾/按下单时间▾(原生select+内嵌svg箭头) + 开始日期→结束日期(date) + 「按订单号搜」输入框 + 搜索(蓝实心) + 导出EXCEL(蓝描边)按钮。
- 表格列：平台订单号/支付时间/下单时间/付款会员(名字+(ID:xxx)两行)/支付事项/支付方式/支付金额/状态(未支付橙)/已支付回执/操作(改为已支付蓝链)。首行 FO87752484855981917 红色高亮(`.finord-row-active` 左侧红条 #ff4d4f + 浅红底)。
- 数据：seedRecords 20条(截图真实：Lemon ID790/O_o♡ ID760/出现1 ID54/芸希老师 ID35/陌 ID788/途遇觅老师 ID767/是静香本人没错 ID790；事项多为推广红娘入伙费99元、VIP会员299元、送礼物90元)，buildRecords 补齐到 TOTAL=211 → 分页 11 页(20条/页)。
- 分页 `.finord-pagination`：paginationWindow() 生成 1..5 … 11 窗口，当前页高亮，‹ › 可翻，右侧「20条/页」下拉。
- 前缀 `.finord-*` 追加到 globals.css 末尾(卡片/tabs/stats 9列/filters/表格/分页，含 1280/720 响应式降级)。`npx tsc --noEmit` 通过。

## 概览页改用真实后端接口
- 后端项目 `~/Desktop/xuanshiai`（FastAPI，远程 https://xhztest.xyz/api/v1 由 .env.local NEXT_PUBLIC_ADMIN_API_BASE_URL 指向，本地 8000 未运行）。
- admin-endpoints.ts 新增类型+端点：`AdminDashboardReport`(metrics/pending/member_gender/income_rank/trends，snake_case)、`AnnouncementPage/AnnouncementItem`、`AcademyCategory`、`AdminBootstrap`；`announcements(query)` → admin/announcements、`academyCategories()` → admin/academy/categories；`bootstrap`/`dashboard` 返回类型由 Record<string,unknown> 收紧。
- home/page.tsx 数据源改为 4 个接口：dashboard({from:min(本月1日,今天-14d),to:今天})(一次调用同时得 15 天趋势+本月增量)、bootstrap()(操作员名+短信余量)、announcements({page:1,page_size:5})(系统更新)、academyCategories()(使用帮助栏目)。
- 区域映射：7统计卡←metrics+本月trends求和；待审11项中 待牵线←pending.match_application、约见申请←pending.matchmaker_service、提现申请←pending.withdrawal、投诉举报←pending.report，其余无后端项为0；会员增长趋势←trends(7/15天tab切换slice)；**「会员登记来源占比」改为「收入构成占比」**←income_rank(product_type经PRODUCT_LABELS映射中文，中心显示总收入)；男女占比←member_gender；**光荣榜改为真实指标**(线上牵线successful_match_count/线上VIP/线下VIP/服务红娘/推广红娘)；耗材-短信←bootstrap sms.remaining_count(其余0)；系统更新←announcements(category为tag)；使用帮助←学苑栏目name。
- **调试登录兼容**：local-demo-token 模式下接口会 401 并被 adminApi 踢回登录页，故 `demo` state(getAdminToken()==="local-demo-token")时 useEffect 直接 return，state 初始化为截图演示数据(buildDemoReport/demoAnnouncements/demoGuides/9410条短信/shushu)；真实 token 时初始 0 由接口填充，接口失败保持 0。
- 空状态 `.ov-source-empty` 样式。`npx tsc --noEmit` 通过。

## 积分明细页面 + 发放积分弹窗
- 路由 `/system-credit-history`，重写为静态列表页。复用 `.finord-*`(card/filters/select/daterange/search-input/btn/table/pagination)，新增 `.crh-*`：`.crh-head/.crh-title/.crh-search-input/.crh-col-id/.crh-col-category/.crh-col-amount(.income蓝#3658f7/.expense紫#7b61d6)/.crh-col-time`。
- 结构：卡内标题「积分明细」+右上「发放积分」蓝钮 → 筛选条(全部支付分类▾/日期→日期/输入会员昵称/搜索蓝钮，无导出EXCEL) → 表格(ID/支付会员/支付事项/支付分类/支付金额±N金币着色/支付时间/操作"-") → 分页(‹ 1 › + 20条/页)。
- 数据 13 条截图真实(秋刀鱼+2200金币/毛毛-100金币/σπη'η收礼物+100/出现1+450金币 等，ID 13→1 递减)，补齐到 211 条 → 11 页。
- 发放积分弹窗：`.ptp-mask` + `.ptp-panel`(右侧 Drawer 1000px, ptp-slide-in 动画)，`.ptp-header`(左 ✕+标题「发放积分」，右 取消描边+确定发放蓝钮),`.ptp-body` 表单(发放对象 select 默认所有注册用户 / *发放数值 input+积分单位 / *发放理由 placeholder「不要超过20个字」maxLength=20)。`npx tsc --noEmit` 通过。

## 余额提现页面
- 路由 `/system-cashout-history`，重写为静态页。复用 `.finord-*`，新增 `.cs-*`。
- 结构：蓝色信息条 `.cs-info`(圆形 ℹ 图标 + 两段长说明文字) → 3 统计卡 `.cs-stats`(未申请提现总额15756.58元(绿¥)/已提现总额1009元(红☺)/提现中的总额0元(蓝✓)) → 卡内「余额提现」标题 → 筛选条(单选组 全部/待处理/已提现/提现中/拒绝提现 + 全部支付类型▾ + 账号昵称输入 + 搜索蓝钮 + 🗑清空数据描边钮) → 表格(提现账号昵称+ID:xxx/联系方式/申请提现金额/手续费/到账金额(done蓝)/提现状态(done绿·pending橙)/当前账号余额/时间(申请+到账两行)/支付到(方式+账号/姓名)/支付方式/操作(待处理→已完成转账|拒绝提现 蓝链；已提现→-)) → 分页 1 页。
- 数据 5 条截图真实：σπη'η ID548 已提现10元、出现1 ID54 已提现999元(手续费9.99到账989.01)、憨喜 ID506 三条待处理(74/380/80元)。`.cs-*`：info/info-body/info-ic/info-text/stats/stat(-icon green·red·blue)/(-meta/label/value)/filters/radios(-radio.active)/clear/nick/uid/phone/amount/fee/arrive(.done)/status(.done/.pending)/balance/time/payto/payname/paymethod/ops/op-sep。`npx tsc --noEmit` 通过。

## 统计报表页面
- 路由 `/finance-statistic`，重写为静态页。复用 `.finord-tabs`(两行 24 tab 圆角，与收入明细相同) 與 `.finord-table`，新增 `.fs-*`。
- 结构：两行 tab(全部选中蓝) → 卡内「统计报表」标题 → 统计时间行 `.fs-timeselect`(统计时间: + 日榜/月榜/年榜单选 + 2026-08-09→2026-09-09 date + ▦日历图标) → 表格(时间 | 成功付款笔数 | 收入金额 | 已退款)，全 0 数据显示红色 `.fs-red`(0笔/0元/0元, 0笔)。
- 数据 15 行截图真实(2026-09-09 → 2026-08-26 递减，全部为 0)，一页无分页。`.fs-*`：timeselect/label/radios(-radio.active)/daterange/cal/th-time/th-center/td-time/table/td-center/red。`npx tsc --noEmit` 通过。

## 合同管理页面
- 路由 `/e-contract-list`(电子合同三级菜单首项)，重写为静态空态页。复用 `.finord-table/.finord-filters/.finord-select/.finord-btn`，新增 `.ecl-*`。
- 面包屑四级：首页/财务管理/电子合同/合同管理(直接构造 items，getBreadcrumb 只支持三级)。
- 结构：蓝色信息条 `.ecl-notice`(标题「预知」+ 说明文字，含 合同发起/合同签署/合同管理/法律效力/使用费用 五段带<b>加粗小节) → 2 统计卡 `.ecl-stats`(已发起合同 0条(蓝文图标)/可用合同余量 150条(红文图标)+「在线充值」蓝钮 `.ecl-recharge`) → 卡内「合同管理」标题 → 筛选条(按会员昵称▾ select + 请输入 input + 搜索蓝钮 + 右侧 📃客户签署流程演示蓝描边 `.ecl-demo` margin-left:auto) → 空态表格(暂无数据 中央灰图标 `.ecl-empty` colSpan=7)。
- `.ecl-*`：notice/notice-body/notice-ic/notice-text/notice-title/stats/stat(-icon blue·red)/(-meta/label/value)/recharge/demo/empty(-inner/-icon/-text)。`npx tsc --noEmit` 通过。
- 合同配置(`e-contract-config/page.tsx`)重写为静态可交互：红色提示条(`.etc-alert`)+四级面包屑+蓝色须知条(`.ecl-notice`,4点说明含腾讯电子签官网)+卡片头部「合同配置」+两个blue小链接(`配置使用教程`/`客户签署流程演示`)+右侧「授权电子签」蓝钮+表单`腾讯电子签系统 ○开启○关闭(useState单选,默认关闭)`+「确定提交」蓝钮。`.ecc-*`：head/links/link/auth/form/row/label/radio(accent-color蓝)/submit。`npx tsc --noEmit` 通过。
- 运营工具菜单重排为含二级子菜单结构(AdminLayout `operate`组)：自由收款`/free-pay`→红娘喜讯▾(栏目配置`/xixun-menu`/喜讯管理`/xixun-list`/锦旗管理`/xixun-banner`)→搭子社群▾(栏目配置`/group-menu`/社群管理`/group-list`/报名管理`/group-signup`)→互动消息▾(功能设置`/interact-config`/内容设置`/interact-content`/消息记录`/interact-record`)→销售匹配库`/sales-match`→内容单页`/single-page`→超级获客▾(落地页`/customer-landing`/自由表单`/free-form`)→批量资料卡`/tool-lovecard`→会员分区`/tool-theme`→短信群发`/sms-group`→送礼物`/love-gift-wrap`→推文助手`/generate-tool`→吸粉二维码`/qrcode-wrap`→积分商城▾(礼品管理`/gift-list`/兑换管理`/gift-exchange`)。`npx tsc --noEmit` 通过。
- 自由收款(`free-pay/page.tsx`)重写为双tab空态页(useState切换)：顶部下划线tab`自由收款`/`收款订单`。Tab1自由收款：须知条(`.ecl-notice`,`自由在线收款解决方案`说明4段)+`您已经创建的项目:`标题+筛选条(收款类目:不限 + spacer + `＋添加收款类目`/`＋添加收款项目`蓝钮)+11列表格空态(ID/收款项目名称/收款类目/收款金额/推广红娘奖励/服务红娘奖励/合伙红娘奖励/在聚合页面/收款订单/查看/操作)。Tab2收款订单：通知条(客户在平台点击"立即支付"…)+筛选条(收款类目:不限 + 日期区间 + 按会员昵称输入框 + 搜索蓝钮)+9列表格空态(ID/下单时间/付款人/关联红娘/收款内容/金额/支付状态/支付时间/操作)。`.fp-*`：tab-bar/tab(.active下划线)/title/filters/spacer。`npx tsc --noEmit` 通过。

## 模板管理页面
- 路由 `/e-contract-template`(电子合同二级菜单)，重写为静态空态页。复用 `.ecl-notice`(须知条)/`.finord-card/.finord-table/.finord-btn`，新增 `.etc-*`。
- 面包屑四级：首页/财务管理/电子合同/模板管理。
- 结构：红色提示条 `.etc-alert`(✕ + 「电子签功能已关闭,请先打开电子签配置」) → 蓝色须知条(标题「须知」+ 3 条说明) → 卡内「模板管理」标题 + 右上两个蓝钮 `.etc-actions`(＋添加合同模板 / ↻刷新列表) → 空态表格(暂无数据 .ecl-empty colSpan=5)。
- 表格列：版本号/模板名称/模板ID/创建人/操作。`.etc-*`：alert/alert-x/alert-text/actions。`npx tsc --noEmit` 通过。

## 喜讯管理页面
- 路由 `/xixun-list`(红娘喜讯二级菜单首项，此前不存在，本次新建文件)。复用 `.ecl-notice/.finord-card/.finord-filters/.finord-select/.finord-btn/.finord-table/.mp-switch/.cs-op-sep`，新增 `.xl-*`。
- 面包屑四级：首页/运营工具/红娘喜讯/喜讯管理。
- 结构：蓝色须知条(标题「须知」+ 3 条说明：跟进回访收集反馈→作喜讯展示 / 鼓励成功客户提交反馈→工作人员编辑完善展示给公众号 / 超级红娘查看全平台、普通红娘仅本人) → 卡内筛选条(按状态分类▾ select + 按服务红娘筛选▾ select + 右侧「添加喜讯」蓝钮) → 13列表格(ID/添加时间/发布人/反馈方/交住方/相识时间/情感状态/服务红娘/红娘祝福/浏览次数/平台公开展示/链接·二维码/操作) → 分页 ‹1›。
- 数据 6 行截图真实(三土发布；芋头苍蝇×王八老鼠 1个月牵手成功·琴琴 / 茵比QQ糖×拨安 5个月恋恋生活·芸希老师 / Karo1314×久Lieb 12个月已办婚礼·苓琴 / 阿布×Iris 15个月已验证·琴琴 / 吴先生×兰小姐 2个月牵手成功·芸希老师 / 夏天×娜娜 6个月已订婚·芸希老师)，发布时间 2026-06-13/14，平台公开展示开关全开(蓝 `.mp-switch.on` 标签「开」)，浏览次数 3/1/2/0/0/2，链接列「查看」蓝链，操作「编辑/查看 | 删除」。
- `.xl-*`：filters(select 140px)/table(min-width 1180px)/time/publisher(white-space:pre-line)/blessing(max-width 240px)/ops。`npx tsc --noEmit` 通过。

## 锦旗管理页面
- 路由 `/xixun-banner`(红娘喜讯二级菜单，此前不存在，本次新建文件)。复用 `.ecl-notice/.finord-card/.finord-table/.mp-switch`，新增 `.xj-*`。
- 面包屑四级：首页/运营工具/红娘喜讯/锦旗管理。
- 结构：蓝色须知条(标题「须知」+ 3 条说明：锦旗在线制作功能输入赠语快速生成 / 引导会员制作赠送电子锦旗在平台展示 / 可在文印店制作为实物锦旗 + 蓝链「在线制作锦旗入口」) → 7列表格(赠送时间/制作人/赠送人/赠送给/赠送语/平台公开展示/操作) → 分页 ‹1›。
- 数据 10 行截图真实(制作人均为「用户\nFairy」两行；赠送人 杜杜&林林/吴先生@韩小姐/张女士@杨先生/小赵&小宋/王小姐&王先生/宋小姐&董先生/图图&壮壮/夏天&婉婉/小刘&小姚/徐先生&文小姐；赠送给 琴琴老师·芸希老师·南京红姐；赠送语如「邂逅真爱 感恩相伴/用心搭佳缘 温暖伴余生/结缘遇良人 感恩引路人」等)，时间 2026-06-12 17:49~18:02，平台公开展示开关全开(蓝 `.mp-switch.on` 标签「开」)，操作「下载保存 链接/二维码 删除」三个蓝链。
- `.xj-*`：entry/table(min-width 1000px)/time/maker(white-space:pre-line)/words(max-width 220px)/ops。`npx tsc --noEmit` 通过。

## 红娘喜讯-栏目配置页面
- 路由 `/xixun-menu`(红娘喜讯二级菜单，此前不存在，本次新建文件)。复用 `.ecl-notice/.finord-card`，新增 `.xm-*`。表单非空无表格。
- 面包屑四级：首页/运营工具/红娘喜讯/栏目配置。
- 结构：蓝色须知条(标题「须知」+ 2 条说明：基于用户反馈的成功案例展示平台解决"婚介不信任"难题 / 创新设置线上送锦旗功能,锦旗墙栏目集中展示,可自制实物悬挂门店) → 卡内「栏目设置」标题 → 各表单行.
- 字段行：栏目浏览(网址路径 https://www.xuanshiai.com/subpages/xixun/index + 二维码:查看 + 在线预览:点击这里,均为蓝链)/栏目标题 input=脱单喜讯/栏目描述 input=脱单喜讯/分享封面(○系统默认 ●自定义(300*300) radio,useState 默认 custom,选中显示橙色封面预览「脱单喜讯」+「上传图片」)/宣传头图(上 上传按钮 + 最佳尺寸:698像素x240像素)/喜讯分类(8 行:牵手成功·恋爱生活·已见父母·已订婚·已领证·已办婚礼·婚后生活·锦旗飘飘,每行 label:input+更换图标(灰)+上移(蓝)+下移(蓝),最后一行锦旗飘飘无下移)/祝福语管理(+添加按钮 + 15 条祝福语,每条带🗑删除,分隔用顶部边框)/确定提交蓝钮。
- `.xm-*`：title/row/label(88px)/content/inline/link/blue/input(360px)/radios/radio(accent-color蓝)/cover(120x120橙色渐变预览 cover-name/cover-upload)/upload-row/upload-btn/muted/cats/cat(96px label,300px input)/cat-icon(灰)/cat-moves/cat-move(蓝)/bless/bless-item(顶边框分隔)/bless-text/bless-del/add-btn/submit。`npx tsc --noEmit` 通过。

## 概览页图表动画修复
- 问题：概览页改数据驱动后，真实接口模式下首帧 `report=emptyReport`（空数据集），recharts 只在挂载时播放入场动画，接口返回后数据更新不触发入场动画，导致图表失去刚进页面的"动态效果"。（演示模式数据同步初始化，故曾有动画。）
- 修复 `home/page.tsx`：新增 `loaded` state（demo 模式初始 true，真实模式初始 false，dashboard fetch `.then` 置 true）；给 AreaChart 与两个 PieChart 加 `key={loaded ? "xxx-loaded" : "xxx-empty"}` 在数据到达时强制 remount 重播入场动画；Area/Pie 显式 `isAnimationActive animationDuration={600}`。`npx tsc --noEmit` 通过。

## 搭子社群-栏目配置页面
- 路由 `/group-menu`(搭子社群二级菜单首项，此前不存在，本次新建文件)。复用 `.xm-row/.xm-label/.xm-content/.xm-input/.xm-radios/.xm-radio/.xm-title/.xm-actions/.xm-submit/.finord-card`，新增 `.gm-*`。表单非空，无表格。
- 面包屑四级：首页/运营工具/搭子社群/栏目配置。
- 结构：卡内「栏目设置」标题 → 各字段行。
- 字段行：栏目名称(input=找搭子 + 蓝色信息 `.gm-info` "● 作为首页和分享标题。")/栏目描述(textarea `.gm-textarea`=年轻人的潮流新社交。放下手机，遇见真实的Ta + ".gm-info" "● 作为分享描述")/分享封面(○系统默认 ○自定义(300*300) radio,useState 默认 system)/首页头图(○系统默认 ○自定义(778*417) radio,useState 默认 system)/社群分类(蓝色信息条 `.gm-catbar` "● 请在"平台配置-基础数据-社群分类"中添加和管理 + 转入蓝链")/热门区域(＋添加热门区域 蓝钮 `.gm-add-hot`)/推广介绍/入群须知(文本)/入群协议(文本),后三个为富文本编辑器。
- 富文本编辑器 `.gm-editor`：`.gm-toolbar`(一组图标工具按钮+`.gm-tool-sep` 分隔线，toolbar 数组含 null 分隔) + `.gm-body` 内容区。推广介绍 body 为图片横幅 `.gm-banner`(左「邀请朋友一起加入」蓝色圆角徽标 + 「年轻人的潮流社交」大字,右 📢☺ 插画)；入群须知/入群协议 body 为 `.gm-body-text` 长文本(strong 标题段落 + <br> 换行)。
- `.gm-*`：info/info::before(蓝点)/textarea(636px)/catbar/catbar-dot/catbar-go/add-hot/editor(706px)/toolbar/tool/tool-sep/body/banner/banner-text/banner-badge/banner-title/banner-illus/banner-face/body-text/body-text p/body-text strong。`npx tsc --noEmit` 通过。

## 搭子社群-社群管理页面
- 路由 `/group-list`(搭子社群二级菜单，此前不存在，本次新建文件)。复用 `.ecl-notice/.finord-card/.finord-filters/.finord-search-input/.finord-btn/.finord-table/.finord-table-wrap/.finord-id/.finord-link/.cs-op-sep/.mp-switch`，新增 `.gml-*`。
- 面包屑四级：首页/运营工具/搭子社群/社群管理。
- 结构：蓝色须知条(标题「须知」+ 2 段说明：添加/管理社群,已满关闭在线入群通道显示"已满",下线不展示；推广红娘不分级别统一按社群设置的奖励金额给分成奖励,可在"推广红娘-分成明细"选"社群缴费"查询) → 卡内筛选条(输入社群标题关键词 input 220px + 「＋ 添加社群信息」蓝钮,无独立搜索/导出) → 11列表格(ID/社群标题/发起人/地区/分类/报名人数/上线?/状态/海报/二维码/创建时间/操作) → 分页 `‹ 1 › 10 条/页`。
- 数据 1 行截图真实(ID 1, 美食干饭搭子, 发起人空, 江苏省南京市建邺区, 美食搭子, 报名人数 1, 上线?开关"上线", 状态"邀请加入", 创建时间 2026-05-31 17:39:14, 操作 编辑|复制|删除)。
- `.gml-*`：filters/search(220px)/add(蓝钮)/table td 居中/count/switch-label/status/time/ops。`npx tsc --noEmit` 通过。

## 搭子社群-报名管理页面
- 路由 `/group-signup`(搭子社群二级菜单，此前不存在，本次新建文件)。复用 `.ecl-notice/.finord-card/.finord-filters/.finord-select/.finord-searchbox/.finord-search-label/.finord-search-input/.finord-btn/.finord-btn-primary/.finord-table/.finord-table-wrap/.finord-link/.finord-page`，新增 `.gsm-*`。
- 面包屑四级：首页/运营工具/搭子社群/报名管理。
- 结构：蓝色须知条(标题「须知」+「在这里可以查看到在您平台上报名入群的所有信息」) → 卡内筛选条(`所有社群`▾ select + `支付: 不限`▾ select + 按昵称 请输入 input 120px + 「搜索」蓝钮) → 10列表格(报名时间/昵称/手机/资料完善/实名认证/加入社群/第几次报名/在线支付/支付方式/单号/推广红娘/操作) → 分页右对齐 `‹ 1 ›`。
- 数据 1 行截图真实(2026-05-31 17:44:26, 别吻我橘子 166****8875 两行, 已完善, 已认证, 美食干饭搭子, 第几次报名 1 + 报名历史蓝链, 免费, 单号:-+方式:-, 推广红娘 -, 操作 查看资料|删除)。
- `.gsm-*`：filters(nowrap)/gap/search(120px)/search-btn/table td 居中/time/nick/phone/count/ops/pages(ml:auto 右对齐)。`npx tsc --noEmit` 通过。

## 互动消息-功能设置页面
- 路由 `/interact-config`(互动消息二级菜单，此前不存在，本次新建文件)。复用 `.ecl-notice/.ecl-notice-body/.ecl-notice-ic/.ecl-notice-text/.ecl-notice-title/.finord-card/.xm-row/.xm-label/.xm-content/.xm-radios/.xm-radio/.xm-actions/.xm-submit`，新增 `.ifs-*`。
- 面包屑四级：首页/运营工具/互动消息/功能设置。
- 结构：蓝色须知条「须知」+ 7 条说明(会员消息功能/打招呼定义 + 七大特色列表) → 卡片「功能设置」7 个字段行：
  1. 开启"会员消息系统" 开启/关闭 radio(默认开启) → 蓝条「开启状态下会员在线上平台才可以使用本功能」
  2. 非实名认证会员是否允许使用消息功能 允许/不允许(默认不允许) → 蓝条「若设为"不允许",系统将提示引导进行实名认证」
  3. 非VIP会员是否允许主动发送消息 允许/不允许(默认允许) → 蓝条「若设为"不允许",系统将提示引导会员开通办理VIP」
  4. 非VIP会员每天最多发多少条消息 input 3 条 → 蓝条「0 表示不限制,不含回复」
  5. VIP会员每天最多发多少条消息 input 10 条 → 蓝条「0 表示不限制,不含回复」
  6. 非VIP会员是否允许回复消息 允许/不允许(默认允许) → 蓝条「若设为"不允许",系统将提示引导会员开通办理VIP」
  7. 对方未回复之前最多发送消息条数 input 2 条 → 蓝条「0 表示不限制」
  → 底部「确定提交」蓝钮。
- `.ifs-*`：info(淡蓝圆角条 内蓝点)/inputline(70px 小输入框+条 单位)/input/unit。`npx tsc --noEmit` 通过。

## 互动消息-内容设置页面
- 路由 `/interact-content`(互动消息二级菜单，此前不存在，本次新建文件)。复用 `.ecl-notice/.finord-card/.finord-table-wrap/.finord-table/.finord-link/.mp-switch/.cs-op-sep`，新增 `.ic-*`。
- 面包屑四级：首页/运营工具/互动消息/内容设置。
- 结构：蓝色须知条「须知」+「您可以在这里自由添加、修改消息的内容主题、回复内容;好的创意内容将有助提升会员的互动性哦」 → 卡片头部「消息内容设置」+ 右侧「＋ 新建内容主题」蓝钮 → 表格(消息内容主题/显示排序/状态/操作)。
- 显示排序：首行仅「↓ 下移」，其余「↑ 上移 ↓ 下移」(蓝链)。状态：`.mp-switch` 开关 + 文字标签「启用」蓝/.「关闭」灰。操作：编辑 | 回复内容管理(n) | 删除。
- 数据 5 行截图真实(你好刚到你感觉很有眼缘/你看去过好多城市旅行/你好抱着认真找对象的心态/你好我看到了你资料/你看过你的资料，回复内容管理 2/0/0/3/3)。
- 新建内容主题弹窗(`.ic-mask`+`.ic-panel` 居中 560px)：标题「新建消息内容主题」+✕ → body textarea placeholder「输入主题内容,100字符以内」maxLength=100 → 底部 取消/确定。确定后插入列表首行。
- `.ic-*`：head/title/add/stats/topic/sorts/move/status(-label on·off)/ops/ops-sep/mask/panel(-head/-title/-x/-body/-textarea/-actions)/cancel/ok。`npx tsc --noEmit` 通过。

## 互动消息-消息记录页面
- 路由 `/interact-record`(互动消息二级菜单，此前不存在，本次新建文件)。复用 `.ecl-notice/.finord-card/.finord-table-wrap/.finord-table/.finord-link`，新增 `.ir-*`。
- 面包屑四级：首页/运营工具/互动消息/消息记录。
- 结构：蓝色须知条「须知」+ 2 段说明(消息记录功能含义 + 首次发送人/首次回复时间/最后互动时间定义) → 卡片「消息记录」筛选条(输入框 placeholder「请输入主动发送人昵称/编号/手机/姓名」+「搜索」蓝钮 + 3 单选排序 按首次发送时间默认/按首次回复时间/按最后互动时间) → 6列表格(首次发送人/发送给/首次发送时间/首次回复时间/最后互动时间/操作)。
- 首次发送人/发送给：头像(34px 灰块) + 姓名 + 编号 一行 + 下方灰色红娘名(李会强/张瑞/荷菱颖/冯琴等)。操作 = 蓝链「查看消息内容记录」→ 打开右侧 Drawer。
- 数据 8 行截图真实(秋刀鱼 B976071、出现1 B241050、出现 B237195、σπη'η B108658；发送给 1196/0哔吧啦/爱慕药/小Yang又困/毛毛等，时间 2026-07-09~2026-06-25，首次回复全"未回复")。
- 预览前台 Drawer(`.ir-mask`+`.ir-drawer` 右侧 440px)：顶栏 ✕+「预览前台」+「关闭」→ 消息中心(←+标题+用户图标) → 用户卡(头像1196+已实名徽标绿+26岁+166cm+详细>) → 时间 07-09 09:38 → 紫色气泡(你好，看过你的资料…) + ⏱等等Ta回复 → 底部条(💬系统已经给Ta发送短信提醒)。
- `.ir-*`：title/filters/search-input/search-btn/sorts/sort/table th 左对齐/person/avatar/person-info/name/id/dash/match/time/mask/drawer(-head/-x/-title/-close)/msg(-top/-back/-title/-tools)/user-card(-avatar/-info/-line/-name/-badge/-meta/-detail/-arrow)/chat-time/bubble(-wrap/-time)/system-tip/talk-ic。`npx tsc --noEmit` 通过。

## 销售匹配库页面
- 路由 `/sales-match`(运营工具下直接子菜单，导航 AdminLayout line68 已指向，此前不存在，本次新建文件)。复用 `.ecl-notice/.finord-card/.finord-filters/.finord-searchbox/.finord-search-label/.finord-select/.finord-search-input/.finord-btn/.finord-table/.ecl-empty`，新增 `.slm-*`。
- 面包屑三级：首页/运营工具/销售匹配库(getBreadcrumb("运营工具","销售匹配库"))。
- 结构：蓝色须知条(标题「须知」+ 3 段说明：眼缘库定义/使用步骤(销售红娘到店前为顾客创建专属匹配库→挑5-10位红娘推荐→引导加入眼缘清单)/特别注意事项(展示头像照片不受权限限制,建议使用后前端关闭)) → 卡内标题行 `.slm-head`(左：标题「销售匹配库」+「客户签署流程演示」下划线蓝链；右：「创建销售匹配库」「功能配置」蓝钮) → 筛选条(状态:不限▾ / 红娘:不限▾ / 「请输入」输入 box=240px placeholder「请输入关联会员的昵称/手机/姓名」 + 搜索蓝钮) → 空态表格 colSpan=12(暂无数据)。
- 表格列：ID/标题/关联账号/创建时间/创建人/是否到店/销售红娘/销售库前端/链接/二维码/红娘推荐人选/客户眼缘清单/操作。
- `.slm-*`：head/title-wrap/title/demo(下划线蓝链)/actions/filters(nowrap,search-input 240px)/table(min-width 1240px, th/td 居中, 标题/推荐人选/眼缘清单 三列左对齐)。`npx tsc --noEmit` 通过。

## 内容单页页面
- 路由 `/single-page`(运营工具下直接子菜单，导航 AdminLayout line69 已指向，此前不存在，本次新建文件)。复用 `.ecl-notice/.finord-card/.finord-filters/.finord-search-input/.finord-btn/.finord-table/.finord-link/.mp-switch/.finord-pagination/.finord-pages/.finord-page`，新增 `.sp-*`。
- 面包屑三级：首页/运营工具/内容单页(getBreadcrumb("运营工具","内容单页"))。
- 结构：蓝色须知条(标题「须知」+ 3 段说明：自定义单页自由创建无限自由内容页面可插入导航/关闭状态自动跳转平台首页/可通过"内容专辑"功能打造专属场景) → 卡内标题行 `.sp-head`(左标题「内容单页」+ 右「＋ 添加单页」蓝钮) → 筛选条(输入框 240px placeholder「请输入关键字」+ 搜索蓝钮) → 带复选框表格 → 分页右对齐 ‹ 1 ›。
- 表格列(11)：复选框/ID/页面标题(标题+✎编辑小图标)/仅相亲会员可浏览/仅实名会员可浏览/仅VIP会员可浏览/浏览明细(-)/链接·二维码(查看蓝链)/预览效果(预览蓝链)/启用·关闭/操作(复制/编辑/删除三行竖排蓝链)。
- 数据 3 行截图真实：ID 4 活动 / ID 3 本周活动 / ID 1 历史活动(三权限均"关闭"灰 switch + 灰色"关闭"文字，启用都"开"蓝 switch + 蓝色"开"文字)。
- 交互：表头全选 colSpan checkbox，三权限三列 + 启用关闭均可点击切换。`.mp-switch` 结构为 `<button class="mp-switch on"><span class="mp-switch-knob"></span></button>`(不含内部 label，右置外部文字)。
- `.sp-*`：head/title/filters(nowrap,search-input 240px)/table(min-width 1180px, th/td 居中, 第3列页面标题左对齐)/col-check(40px)/check(accent-color 蓝)/title-cell/edit-ic(灰✎ hover 蓝)/switch-wrap/switch-text(.on 蓝)/ops(竖排)/op-del/pagination(ml auto 右对齐)。`npx tsc --noEmit` 通过。

## 获客落地页页面
- 路由 `/customer-landing`(超级获客二级菜单首项，导航 AdminLayout line70 已指向，此前不存在，本次新建文件)。复用 `.ecl-notice/.finord-card/.finord-filters/.finord-search-input/.finord-btn/.finord-table/.finord-link/.finord-pagination/.finord-pages/.finord-page/.finord-page-size`，新增 `.cl-*`。
- 面包屑四级：首页/运营工具/超级获客/落地页(直接构造 items)。
- 结构：蓝色须知条(标题「须知」+ 4 段说明：落地页灵活用途/资料显示在客源线索或会员CRM中标注来自落地页/内置表单模式下手机号已存在库中无法再次提交(含蓝色下划线链接「效果预览」)/自由表单模式同手机号只允许提交一次不判断是否平台会员) → 卡内标题行 `.cl-head`(左标题「获客落地页」+ 右「＋ 创建落地页」蓝钮) → 筛选条(输入框 240px placeholder「请输入关键字」+ 搜索蓝钮) → 带复选框表格 → 分页 `‹ 1 ›` + 右侧「20 条/页 ▾」。
- 表格列(11)：复选框/ID/落地页标题/形式/数据位置/创建时间/浏览量/获取客源/推广红娘/链接·二维码/操作。
- 数据 1 行截图真实：ID 1, 你好6月, 单页·内置表单, 数据位置「会员资料」蓝色标签按钮 `.cl-badge`, 2026-06-12 14:42:38, 浏览量 4, 获取客源 总数：0 男：0 女：0, 推广红娘 -, 查看蓝链, 操作「预览/复制/编辑/删除」四行竖排蓝链。
- 交互：表头全选 + 行 checkbox(checked/selected 双 state 联动)。`.cl-*`：head/title/filters(nowrap,search-input 240px)/inline-link(蓝下划线)/table(min-width 1160px, th/td 居中, 第3列落地页标题左对齐)/col-check(40px)/check(accent-color 蓝)/badge(淡蓝标签)/ops(竖排)/pages。`npx tsc --noEmit` 通过。

## 自由表单页面
- 路由 `/free-form`(超级获客二级菜单第二项，导航 AdminLayout line70 已指向，此前不存在，本次新建文件)。复用 `.ecl-notice/.finord-card/.finord-filters/.finord-searchbox/.finord-search-label/.finord-select/.finord-search-input/.finord-btn/.finord-table/.ecl-empty`，新增 `.ff-*`。
- 面包屑四级：首页/运营工具/超级获客/自由表单(直接构造 items)。
- 结构：蓝色须知条(标题「须知」+ 1 段说明：查看所有通过"自由表单"填写登记的客户信息资料，可批量导出到表格/入库到客源线索/会员CRM) → 卡内标题行 `.ff-head`(左标题「自由表单」+ 右「⬆ 导出EXCEL」「▦ 字段管理」两个蓝钮) → 筛选条(输入框 200px placeholder「请输入称呼/手机号搜索」+ 搜索蓝钮 + 「性别:」不限▾ + 「请选择落地页」▾ 180px) → 空态表格 colSpan=8(暂无数据)。
- 表格列：ID/称呼/性别/手机号/更多信息/落地页/提交时间/操作(空态时无内容)。
- `.ff-*`：head/title/actions/filters(nowrap,searchbox inline,search-input 200px)/landing-wrap(.finord-select 180px)/table(min-width 900px, th/td 居中)。`npx tsc --noEmit` 通过。

## 批量资料卡页面
- 路由 `/tool-lovecard`(运营工具下直接子菜单，导航 AdminLayout line71 已指向，此前不存在，本次新建文件)。复用 `.ecl-notice/.finord-card/.finord-table/.finord-btn`，新增 `.tlc-*`。
- 面包屑三级：首页/运营工具/批量资料卡(直接构造 items)。
- **主页面**：蓝色须知条(标题「须知」+ 5 段说明：按条件快速筛选批量导出相亲海报/用途(推文朋友圈纸质册展示墙)/扫码引导公众号提粉/海报保存在服务器生成管理记录/海报名称以会员编号命名如5208001.png) → 卡内标题行 `.tlc-head`(左标题「批量资料卡」+ 右「＋ 新建批量生成」「🎨 模板样式总览」两个蓝钮) → 7列空态表格(创建时间/项目标题/二维码类型/二维码有效期/模板/数量/操作)。
- **新建批量生成 Drawer**(`.tlc-mask`+`.tlc-panel` 右侧 1150px, tlc-slide-in 动画, `.tlc-panel-head` 左✕+标题「新建批量生成」+右「☁ 拥抱此上传」蓝钮)：`.tlc-tabs`(自定义条件active下划线/按活动报名)。
  - 自定义条件 tab(完整表单)：
    - 条件条 `.tlc-condition`(★结果为年龄在18-80岁、身高在140-200厘米之间且相亲会员资料审核通过的相亲会员)
    - ＊项目标题 input(可输入)+蓝条「● 自定义文字，方便管理识别，如：30岁以上未婚女生」
    - 相亲状态 单选(不限/公开相亲/委托红娘/完全私密/已经脱单)
    - 注册时间 开始日期→结束日期
    - 会员性别 单选(不限/男/女)
    - 现居地/籍贯 单选chips布局(不限/指定区域)+蓝条「添加后生效，可添加多个区域」
    - 实名认证(不限/未实名认证/已实名认证)/学历认证(不限/已学历认证)/房产认证(不限/已房产认证)
    - 线上VIP(不限/普通会员/新人专享/心动专享/挚爱专享)/线下VIP(不限/是/否)
    - 身高范围 140 CM 至 200 CM+蓝条「● 将从您选择的范围中随机选择」
    - 年龄范围 18 岁 至 80 岁+同蓝条
    - 5 组标签 chips(全选+各选项，chipState Record 管理)：婚姻状态(未婚…丧偶)/学历(不限…博士)/收入(不限…年入百万)/工作(不限…健身教练)/会员标签(高颜值…公务员)
    - 服务红娘 chips(全选/芸希老师)
    - 蓝条「● 当前符合上述条件的会员共有 346 人」
    - 海报模板 tab(常规模板 active/4A打印专用)：`.tlc-tpl-grid` 7列单选模板卡，常规模板 24 个/4A 16 个，`makeTemplates` 用 TPL_COLORS 渐变占位 + avatar/line/qr 骨架，选中 `.active` 边框+左上角圆点
    - 海报头像 单选(会员头像/自定义通用头像)
    - 二维码类型 单选(普通H5二维码/公众号临时二维码/公众号永久有效二维码/指定二维码)+蓝条「扫码后直接到资料内容页」
    - 二维码描述文章 textarea 默认「微信扫码识别\n了解Ta更多资料」
    - 生成数量：单选「按相亲会员ID从小到大排序」+ 批次 `.tlc-batches` 单选(第1-50条…第501-546条/仅生成最新前50)
    - 底部 `.tlc-panel-footer`(确定提交蓝钮/直接下载描边钮)
  - 按活动报名 tab(占位)：条件条(选择已结束或已报名的活动批量生成海报)+＊选择活动 select(请选择活动/往期活动回顾/成功案例故事)
- **模板样式总览全屏**(`.tlc-overview-mask` 淡紫氛围+右上✕关闭)：中央「会员资料海报批量制作」大字 + 副标题「线下相亲墙/纸质资料册/发朋友圈/资料相册」+ `.tlc-ov-grid` 7列 40 个模板卡片。
- 封装子组件：RadioGroup(label/options/value/onChange/layout inline|chips)、ChipGroup(全选+多选)、TplCard。`.tlc-*` 共 40 余类。`npx tsc --noEmit` 通过。

## 会员分区页面
- 路由 `/tool-theme`(运营工具下直接子菜单，导航 AdminLayout line72 已指向，原为空 ListPage，本次重写为静态页)。复用 `AdminBreadcrumb/.ecl-notice/-body/-ic/-text/-title/.finord-card/.finord-btn/-primary/.finord-link/.mp-switch`，新增 `.tm-*`。
- 面包屑三级：首页/运营工具/会员分区(getBreadcrumb("运营工具","会员分区"))。
- 结构：蓝色须知条(标题「须知」+ 1 段说明：会员分区功能帮助快速建立主题会员聚合页,按性格特征分类集中展示,增强关注度流量导入,可通过专区创建互动活动) → 卡内标题行 `.tm-head`(左标题「会员分区」+ 右「＋ 添加分区」蓝钮) → 8列表格。
- 表格列：ID/分区名称/链接·二维码(查看蓝链)/创建时间/状态(蓝底标签`上架`+小号 mp-switch)/数据(自动筛选条件蓝链)/排序(↑上移 ↓下移蓝链,首行仅下移)/操作(配置条件|编辑|删除)。
- 数据 4 行截图真实：ID2 高颜值专区 2026-06-14 16:50:04(仅↓下移) / ID4 高收入专区 2026-06-17 16:06:39 / ID1 高学历专区 2026-06-30 16:39:28 / ID3 95后专区 2026-06-17 16:00:06，状态全上架。
- 交互：↑上移/↓下移交换行序、状态 mp-switch 点击切换上/下架(badge 文字同步)。`.tm-*`：head/title/table(min-width 1040px,th/td 居中)/cell-id/cell-time/status-wrap/badge(.on 蓝字淡蓝底/.off 灰字灰底)/switch-sm(scale .85)/moves/move/move:hover/ops/op-sep/op-del。`npx tsc --noEmit` 通过。

## 短信群发页面
- 路由 `/sms-group`(运营工具下直接子菜单，导航 AdminLayout line73 已指向，同时系统管理-短信系统也用此路由；原静态页已存在仅面包屑路径为「系统管理/短信系统/短信群发」，本次改面包屑为「运营工具/短信群发」+ 增加按钮交互与两个 Drawer)。复用 `AdminBreadcrumb/getBreadcrumb/.obc-notice/-i/-body/-t/-desc/.admin-card/.grp-*/.finord-btn/-primary/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body`，新增 `.sms-*`。
- 面包屑三级：首页/运营工具/短信群发(getBreadcrumb("运营工具","短信群发"))。
- 结构：黄色须知条(`.obc-notice`)1段 → 卡内标题行 `.grp-head`(左「短信群发」+ 右三蓝钮 `.grp-btn`：＋创建群发任务 / 💳短信充值 / ✏️提交群发模板) → 8列表格空态(colSpan=8 暂无数据)。
- 表格列：任务名称/创建时间/发送对象/手机号码清单/任务状态/发送统计/发送明细/操作。
- 按钮交互：创建群发任务→打开 Drawer1；短信充值→`router.push("/sms-record")`(跳短信系统-发送记录页,用户明确要求)；提交群发模板→打开 Drawer2。
- **Drawer1 创建群发任务**(`.sms-panel` 右侧 1100px，复用 tlc 骨架)：头部 ✕+标题 + 右侧「☁ 拥抱此上传」蓝钮/取消/确定提交。
  - 通知条 `.sms-notice`(标题「通知」蓝 + 2 段：提交内容由服务商二次编辑后至腾讯审核通过后下发 / 审核严格不得含营销性、婚恋相关字样、具体联系方式和网站)。
  - ＊任务名称 input placeholder「自定义任务名称,限制20字符」(useState)。
  - 发送对象 9 chips 单选(所有注册用户/最近1个月注册的会员/所有会员/所有男会员/所有女会员/所有VIP会员/所有推广红娘/指定活动报名名单/互动活动报名嘉宾)。
  - ＊短信模板 蓝条(`.sms-tpl-bar`)「从以下内置的短信模板中选择发送内容；欢迎提交您的短信群发内容创意，腾讯审核通过后即可在下面选择发送」+ `.sms-tpl-grid` 3列 21 个模板卡(◇ Q编号 +「短信内容:」+ 文案，选中 actiive 蓝边+✓)。
- **Drawer2 群发短信模板**(`.sms-panel-narrow` 右侧 820px)：头部 ✕+标题 + 右侧三钮。obc-notice 须知条 3 段(提交后微信告知技术工程师/审核严格不含营销婚恋字样/70字计费规则1汉字数字字母标点空格算1字) → ＊场景描述 input placeholder「发送该短信的目的描述」→ ＊模板内容 textarea placeholder「140字符内，请输入您想要群发的信息内容创意」maxLength=140 + `.sms-count`「当前 {n} 字。实际发送时，签名、变量长度会影响总字数」实时计数。
- `.sms-*`：panel(-narrow)/head-actions/upload-btn/cancel/notice(-head/-title)/form-row(-top)/form-label(96px)/input(-wide)/targets/chip(.active)/tpl-wrap/tpl-bar(-dot)/tpl-grid(3列,900降2列)/tpl(.active 蓝边)/tpl-head/-id/-cat/-check/-text/textarea-wrap/textarea/count。`npx tsc --noEmit` 通过。

## 送礼物页面
- 路由 `/love-gift-wrap`(运营工具下直接子菜单「送礼物」，导航 AdminLayout line74 已指向，原为 ListPage 空态，本次重写为双 tab 静态还原页)。复用 `AdminBreadcrumb/.ecl-notice/-body/-ic/-text/-title/.finord-card/.finord-btn/.finord-btn-primary/.finord-table/.finord-table-wrap/.finord-link`，新增 `.gift-*`。
- 面包屑三级：首页/运营工具/送礼物/礼物管理（或 赠送礼物，随当前 tab 动态切换，直接构造 items）。
- 结构：蓝色须知条(标题「须知」+ 4 段：送礼物增强互动性促活 / 礼物赠送需向平台支付费用收益归平台 / 赠送后对方立即收到短信提示 / 获赠方自动获得平台积分奖励可兑换礼品) → `finord-card` 顶部 `.gift-tabbar`(左 `.gift-tabs` 下划线 tab：礼物管理/赠送礼物；右仅礼物管理 tab 显示「💳 添加礼物」蓝钮)。
- **礼物管理 tab**：9列表格(ID/礼物名称/礼物单位/礼物图片/销量统计/所需积分/奖励积分/排序/操作)。礼物图片为 emoji 大号(.gift-thumb 30px)；销量「3查看」数字蓝+.gift-sales 灰；金币值 `.gift-coin` 蓝；排序 ↑上移/↓下移(首行仅下移，交互交换行序)；操作 编辑|删除。
- 数据 7 行截图真实：ID18 水晶球 颗 🔮 3查看 900/450 | ID17 啤酒 杯 🍺 800/400 | ID16 小彩礼 个 🧧 700/350 | ID15 小情书 个 💌 600/300 | ID14 甜甜圈 个 🍩 500/250 | ID13 麦旋风风筒 个 🍦 400/200 | ID11 彩虹糖 颗 🍬 300/150，操作 编辑|删除。
- **赠送礼物 tab**：`.gift-subtabs` 付款筛选 全部/已支付/未支付(下划线单选) → 12列表格(复选框/ID/赠送礼物/赠送数量/赠送人/消耗积分/实付金额/奖励积分/赠送对象/支付状态/赠送时间/操作)。赠送人/对象为 `.gift-person`(头像30px渐变圆 + 姓名 + 编号: 蓝灰)；消耗积分/奖励积分 `.gift-coin` 蓝、实付金额 `.gift-price` 红；支付状态 `.gift-status` 淡蓝标签「未支付」；赠送时间「—」；操作 删除。
- 数据 7 行截图真实(均为未支付)：ID33 水晶球 1颗 速发明老师 B634017 → 恰口奥立奥 G06087 | ID32 水晶球 1颗 秋刀鱼 B976071 → 恰口奥立奥 G066087 | ID31 水晶球 1颗 秋刀鱼 B245655 → 1196 G617884 | ID30/29 水晶球 1颗 秋刀鱼 B245655 → 乌龙茶6071 G74715 | ID28/27 宇宙火箭 1发 Sofia G410116 → 莓泥不行ya B165423。
- 交互：tab 切换、支付筛选、表头全选 + 行 checkbox(选中 state)、礼单上移/下移交换行序。`.gift-*`：tabbar/tabs/tab(.active 下划线蓝)/subtabs/subtab(.active)/table(min-width 1080px,表宽版 1280px, th/td 居中)/col-check(40px)/check(accent-color 蓝)/thumb(30px emoji)/sales(b 蓝)/coin(蓝)/price(红)/moves/move/ops/op-sep/op-del/person/avatar(-recv)/person-info/name/id/status/dash。`npx tsc --noEmit` 通过。

## 推文助手页面
- 路由 `/generate-tool`(运营工具下直接子菜单「推文助手」，导航 AdminLayout line75 已指向，原为 ListPage 空态，本次重写为静态还原页)。复用 `AdminBreadcrumb/.ecl-notice/-body/-ic/-text/-title/.finord-card`，新增 `.twt-*`。
- 面包屑三级：首页/运营工具/推文助手(直接构造 items)。
- 结构：蓝色须知条(标题「须知」+ 2 段：生成后点击"一键复制"粘贴到公众号推文编辑器或第三方(如135编辑器)使用 / 选项中留空或不勾选任何数据则默认为不限) → 卡片标题「推文助手」 → `.twt-wrap` 左表单右手机预览两栏。
- **左表单** `.twt-form`(每行 `.twt-row`+`.twt-label` 84px)：
  - 年龄范围：[18]岁 至 [70]岁(`.twt-num` 70px 居中)
  - 7 组 chips 单选(全选+多选)：会员性别(男/女)/婚姻状态(未婚/离异未育/离异不带孩/离异带女孩/离异带男孩/丧偶)/学历(不限/初中/技校/高中/大专/本科/硕士/博士)/工作(不限/私企员工/央企/国企...美容师/健身教练 21项)/服务红娘(芸希老师)/会员级别(普通会员/新人专享/心动专享/挚爱专享)/相亲状态(公开相亲/委托红娘/停止相亲/已经脱单)
  - 5 组 radio 单选：线下VIP(不限/是)/注册时间(不限/1天内/3天内/7天内/15天内/30天内)/实名认证(不限/已实名认证)/风格模版(模板1/2/3)/二维码类型(普通H5二维码/公众号二维码,含蓝条「① 扫码后直接到资料内容页」)
  - 现居地 select「请选择」
  - 指定会员 input placeholder「请输入」+ 蓝条「① 输入会员编号“*”逗号隔开」
  - 生成数量 radio：生成本页全部数据（50条/页）/ 仅生成最新的前 [50] 条(`.twt-num-sm` 56px)
  - 分页 `.twt-pager`：共 0 条 ‹ 1 › +「生成模版」蓝钮
- **右手机预览** `.twt-phone`：iPhone 外壳(`.twt-phone-frame` 280px 边框深黑圆角34 无边框) → `.twt-topbar`(黑底 状态栏 9:41 AM + 100% + 电池图标 + `.twt-notch` 刘海凹槽) → `.twt-screen`(白底,「‹ 返回」+「没有符合要求的数据」) → 框下方「📋 一键复制」蓝钮。
- 封装子组件：ChipRow(ChipDef 全选+多选)、RadioRow(RadioDef 单选,支持 inlineExtra)。`.twt-*`：card/title/wrap/form(表单左)/row/label(84px)/range/nums/num(-sm)/unit/arrow/input/select/info(蓝条)/options/radio(.active 蓝字)/chips/chip(.active)/pager(-total/-arrow/-cur)/submit/phone/phone-frame/topbar/status(-time/-right)/batt/notch/screen/back/empty/copy-btn。`npx tsc --noEmit` 通过。

## 吸粉二维码页面
- 路由 `/qrcode-wrap`(运营工具下直接子菜单「吸粉二维码」，导航 AdminLayout line76 已指向，此前文件不存在，本次新建)。复用 `AdminBreadcrumb/.ecl-notice/-body/-ic/-text/-title/.finord-card/.finord-btn/-primary/.finord-link/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body`，新增 `.fq-*`。
- 面包屑四级：首页/运营工具/吸粉二维码/二维码管理(直接构造 items)。
- 结构：蓝色须知条(标题「须知」+ 4 段说明：生成自定义微信分享+公众号引导关注二维码/使用示例每个渠道建专属二维码实时统计吸粉引流/未关注扫码后现公众号关注页/已关注扫码后直接在对话框发送分享内容) → `finord-card` 标题行 `.fq-head`(左「二维码管理」+ 右「＋ 添加二维码」蓝钮) → 11列表格 → 分页右对齐 ‹ 1 ›。
- 表格列：复选框/ID/分享封面/标识/分享内容/二维码有效期/生成时间/推送次数/带来关注/二维码/操作。
- 数据 1 行截图真实：ID1，分享封面 `.fq-cover` 红渐变方块(约会 启 小字)，标识 123，分享内容 3 行(标题: 南京单身 / 摘要: 给你发一个高颜值对象 / 链接: sdfghjk)，二维码有效期 `.fq-expired` 红标签「已过期」，生成时间 2026-07-03 11:40:10，推送次数 1，带来关注 0，二维码「查看」蓝链，操作 数据统计/延期/删除 三行竖排蓝链。
- 交互：表头全选 + 行 check(双 state 联动)，「＋ 添加二维码」打开右 Drawer。
- **添加二维码 Drawer**(`.fq-panel` 右侧 460px)：头 ✕+标题 + 右侧「☁ 抱抱此上传」蓝钮/取消/确定提交。
  - ＊分享封面 `.fq-upload-box`(+上传图片 虚线框 110x110)+蓝条「① 最佳尺寸：300像素x300像素」
  - ＊标识 input placeholder「不要超出50字符」+蓝条「① 自定义文字，仅用于方便区分和管理」
  - ＊分享标题 input placeholder「不要超出50字符」
  - ＊分享摘要 input placeholder「请输入」
  - ＊分享链接 input
  - 有效期 radio：临时二维码（30天后失效）/永久有效二维码(默认临时)
  - 底部「确定提交」蓝钮
- `.fq-*`：card/head/title/btn-plus/table-wrap/table(min-width 1180px, th/td 居中, 第4/5/6列 分享内容/有效期/标识 左对齐)/col-check(40px)/check(accent-color 蓝)/id/cover(红渐变方块)/ident/share(-title/-sub/-link)/expired(红标签)/time/num/ops/op/op-del/pager(-arrow/-cur)/panel(460px)/head-actions/upload-btn/cancel/form-row/form-label(88px)/content/upload-box(-plus/-text)/input(-wide 底边距)/info(蓝条)/options/radio(.active)/submit-row/submit。`npx tsc --noEmit` 通过。

## 礼品管理页面
- 路由 `/gift-list`(积分商城二级菜单首项，导航 AdminLayout line77 已指向；原为 ListPage 通用空态页，本次重写为静态还原页)。复用 `AdminBreadcrumb/.finord-card/.finord-table-wrap/.finord-table/.finord-btn/-primary/.finord-link`，新增 `.gl-*`。
- 面包屑四级：首页/运营工具/积分商城/礼品管理(直接构造 items)。
- 结构：卡片首行为筛选条(`.gl-filters`+`.gl-searchbox`：输入框 220px placeholder「请输入礼品标题关键字」+「搜索」蓝钮) → 标题行(`.gl-head` 左「礼品管理」+ 右「＋ 添加礼品」蓝钮) → 6列表格 → 分页右对齐 ‹ 1 ›。
- 表格列：ID/礼品标题(蓝链「指甲刀」)/性质(实物快递)/所需积分(蓝字100)/库存数(97)/操作(编辑|删除 三者分隔线 蓝链)。
- 数据 1 行截图真实：ID 1，所需积分 100，库存 97。
- `.gl-*`：card/filters/searchbox/search-input(220px)/search-btn/head/title/table(min-width 900px,th/td 居中,第2列礼品标题左对齐)/id/type/points(蓝字)/stock/ops/op/op-sep/pager(-arrow/-cur)。`npx tsc --noEmit` 通过。

## 兑换管理页面
- 路由 `/gift-exchange`(积分商城二级菜单次项，导航 AdminLayout line77 已指向；原为 ListPage 通用空态页，本次重写为静态还原页)。复用 `AdminBreadcrumb/.finord-card/.finord-table-wrap/.finord-table/.finord-btn/-primary/.finord-link`，新增 `.ex-*`。
- 面包屑四级：首页/运营工具/积分商城/兑换管理(直接构造 items)。
- 结构：卡片首行为标题行 `.ex-head`(左「兑换管理」无按钮) → 筛选条 `.ex-filters`(`.ex-searchbox`：select「按礼品搜▾」+ input placeholder「请输入」220px + 搜索蓝钮) → 7列表格 → 分页右对齐 ‹ 1 ›。
- 表格列：ID/申请人(可选头像)/兑换礼物(蓝链「指甲刀」)/积分(蓝字100)/申请兑换时间/状态(蓝标签「等待审核」/绿标签「兑换成功」)/操作(蓝链)。
- 数据 2 行截图真实：
  - 行 1：ID 2, 申请人「毛毛」无头像,姓名 毛毛 手机 18926072282 地址 南京市「建邺区(?)」, 兑换礼物 指甲刀, 积分 100, 2026-07-01 10:45:24, 状态「等待审核」蓝标签, 操作 兑换成功/兑换失败/删除。
  - 行 2：ID 1, 申请人头像大"G"渐变 + 灰小字昵称"Good Nl" + 姓名 李 手机 18856767690 地址 南京市建邺区新城科技园, 兑换礼物 指甲刀, 积分 100, 2026-06-30 17:59:59, 状态「兑换成功」绿色标签, 操作 删除。
- 封装子组件 `Applicant`：可选 hasAvatar/initial/nick + name/mobile/address 渲染头像块(圆形 40px 渐变 + 下方 11px 灰小字昵称) + 右侧姓名大字 + 三行姓名/手机/地址灰小字。
- `.ex-*`：card/head/filters/search-select(34px+右箭头 SVG)/search-input(220px)/search-btn/title/table(min-width 980px,th/td 居中,第2列申请人左对齐)/id/applicant(flex)/avatar-block(纵向)/avatar(40px 圆形渐变)/nick(11px 灰)/userinfo/name(14px 黑)/meta(12px 灰)/points(蓝)/time/status(-pending/-success/-failed 三态标签)/ops/op/pager(-arrow/-cur)。`npx tsc --noEmit` 通过。

## 自由收款 两个添加 Drawer
- 路由 `/free-pay`(运营工具下直接子菜单「自由收款」，导航 AdminLayout line65 已指向)。原静态页存在仅按钮无 Drawer，本次为按钮「＋ 添加收款类目」「＋ 添加收款项目」绑定 Drawer 打开逻辑。
- 复用 `AdminBreadcrumb/getBreadcrumb/.finord-card/.ecl-notice/-body/-ic/-text/-title/.finord-filters/.finord-searchbox/.finord-search-label/.finord-select/.finord-search-input/.finord-btn/-primary/.finord-table/.finord-table-wrap/.ecl-empty/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body`，新增 `.fp-*`。
- **主页面**：双 tab 「自由收款/收款订单」下划线切换；自由收款 tab 内蓝色须知条(标题「须知」+ 4 段：自由在线收款方案/创建任意支付项目发到微信群收款/每个项目聚合+展示两个独立页面/财务支付事项中称"线上收款")+ 标题行 `finord-section-title fp-title`「您已经创建的项目:」+ 筛选条(收款类目:不限▾ + spacer + 两个蓝钮)。表格 11 列空态。
- **「＋ 添加收款类目」Drawer** (`.fp-panel` 右侧 820px)：复用 tlc 骨架，头部 ✕+标题「添加收款类目」+ 右侧「☁ 抱抱此上传」/取消/确定提交。
  - ＊类目名称 input placeholder「不要超过10汉字」
  - 页面头图 占位框 130x80「+750*350」+ 蓝条「① 留空则不显示，多图轮换显示，最佳尺寸750*350，用于显示在聚合页面的头部」
  - 服务介绍 `.fp-editor`(toolbar 25 个 lucide 图标: H1/H2/H3/字体/粗/斜/下划线/删除线/左中右两端对齐/无序/有序/链接/图片/表情/视频/表单/分割线/全屏/撤销/重做/源码/引用；body contentEditable placeholder「请输入正文」) + 蓝条「① 可留空，此内容将作为本类目下所有收款项目的默认介绍」
  - 蓝色须知条 `.fp-notice`「须知」+ 「下面的设置应用到本类目的聚合页面。所有本类目下的收款项目独立页面」
  - 分享标题 input placeholder「请输入分享标题」+ 蓝条「① 留空则自动默认为类目名称」
  - 分享图标 占位框 90x90「+300*300」+ 蓝条「① 留空则自动默认以平台首页分享图标」
  - 分享描述 textarea placeholder「请输入描述」rows=3 + 蓝条「① 留空则自动默认以平台首页分享描述」
- **「＋ 添加收款项目」Drawer** (`.fp-panel` 右侧 820px)：头部 ✕+标题「添加收款项目」+ 右侧三钮。
  - ＊收款名称 input placeholder「不要超过10汉字」
  - ＊归集类目 select 占位
  - ＊收款金额 input「0」+ 右侧「元」+ 蓝条「① 客户在线支付的金额」
  - 推广红娘奖励 封装 `.fp-reward` RadioLine 子组件：单选 统一设置/按级别设置分成 + select「固定金额」+ input「0」+ 元 + 蓝条说明
  - 服务红娘奖励 select「固定金额」+ input「0」+ 元 + 蓝条
  - 合伙红娘分成 RadioLine + 蓝条
  - 分店分成 select + input + 元 + 蓝条(含特别提醒：服务红娘不分)
  - 收费介绍 radio：使用本类目默认统一内容 / 独立设置
  - 页面头图 占位框 + 蓝条「① 留空则不显示」
  - 聚合页面中的显示排序 input + 蓝条「① 数字越大显示越靠前」
- 注意：JSX 属性含英文双引号时改用模板字符串 backticks 包裹(如合伙红娘 info)。
- `.fp-*`：spacer/panel(820px)/head-actions/upload-btn/cancel/row(label+content)/label(96px)/content/input(-wide/-num 90px)/cover(130x80 dashed)/cover-sm(90x90)/info(蓝条)/notice(-title/-body)/editor(-toolbar/-tool/-body/-placeholder)/textarea/select(-wide 右箭头 SVG)/options/radio/reward/-reward-row/unit/amount。`npx tsc --noEmit` 通过。

## 喜讯管理 添加喜讯 Drawer
- 路由 `/xixun-list`(红娘喜讯二级菜单首项，导航 AdminLayout line59 已指向)。原静态页存在仅按钮无 Drawer，本次为按钮「添加喜讯」绑定 Drawer 打开逻辑。
- 复用 `AdminBreadcrumb/.ecl-notice/-body/-ic/-text/-title/.finord-card/.finord-filters/.finord-select/.finord-btn/-primary/.finord-table-wrap/.finord-table/.finord-id/.finord-link/.finord-pagination/.finord-pages/.finord-page/.mp-switch/-label/-knob/.cs-op-sep/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body`，新增 `.xx-*`。
- **「添加喜讯」Drawer** (`.xx-panel` 右侧 560px)：复用 tlc 骨架，头部 ✕+标题「添加/编辑喜讯」+ 右侧「☁ 抱抱此上传」/关闭/确定提交。
  - ＊反馈方 radio(库中会员/自定义，默认库中)+ input placeholder「输入昵称关键词」
  - ＊交住方 radio + input placeholder「输入昵称关键词」
  - ＊交往时间 input placeholder「只能填写数字」+「个月」单位
  - ＊情感状态 select placeholder「请选择情感状态」(牵手成功/已订婚/已办婚礼/已验证/恋恋生活)
  - ＊服务红娘 select placeholder「请选择服务红娘」(芸希老师/琴琴/苓琴)
  - ＊喜讯内容 textarea placeholder「支持段落排版」rows=6 + 下方「⬆ 图片上传/管理（支持移动排序、删除）」线框按钮
  - 显示排序 input + 蓝条「① 数字越大显示越靠前」
- 状态管理：useState addOpen/fbType/ptType，xl-table 之前已经存在并保留。
- `.xx-*`：panel(560px)/head-actions/upload-btn/cancel/row/label(88px)/content/options/radio/input(-wide/-num)/inline/unit/select(-wide 右箭头 SVG)/textarea/upload-row/upload-btn-line(线框按钮 hover 蓝)/info(蓝条)。`npx tsc --noEmit` 通过。

## 搭子社群-栏目配置 添加热门区域 Drawer
- 路由 `/group-menu`(搭子社群二级菜单首项，导航 AdminLayout line61 已指向)。原静态页存在仅按钮无 Drawer，本次为「＋ 添加热门区域」按钮绑定 Drawer 打开逻辑。
- 复用 `AdminBreadcrumb/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body/.finord-btn/-primary`，新增 `.gp-*`。
- **「＋ 添加热门区域」Drawer** (`.gp-panel` 右侧 540px)：复用 tlc 骨架，头部 ✕+标题「添加热门区域」+ 右侧「☁ 抱抱此上传」/取消/确定提交。
  - 区域选择行 `.gp-row`：顶部 `.gp-row-top` 左标签「＊可选择任意省级或以下区域」+ 右内联 `.gp-info-inline`「请选择您想要加入的终极区域名」+ 下方 input(空白)
  - 热门区域行：顶部「＊热门区域」标签 + 下方 select `.gp-select-wide`「请选择」(北京/上海/广州/深圳/杭州/南京)
- 状态管理：useState hotOpen。
- `.gp-*`：panel(540px)/head-actions/upload-btn/cancel/row/row-top(label+info-inline 横排)/label/info-inline(小蓝条)/content/input(100% 满宽)/select(-wide 右箭头 SVG)。`npx tsc --noEmit` 通过。

## 搭子社群-社群管理 添加社群信息 Drawer
- 路由 `/group-list`(搭子社群二级菜单次项「社群管理」，导航 AdminLayout line62 已指向)。原静态页存在仅按钮无 Drawer，本次为「＋ 添加社群信息」按钮绑定 Drawer 打开逻辑。
- 复用 `AdminBreadcrumb/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body/.finord-btn/-primary`，新增 `.gx-*`。
- **「＋ 添加社群信息」Drawer** (`.gx-panel` 右侧 560px)：复用 tlc 骨架，头部 ✕+标题「添加/编辑社群」+ 右侧「☁ 抱抱此上传」/关闭/确定提交。
  - ＊社群标题 input + 蓝条「最多30字」
  - ＊所在区域 select「请选择」
  - ＊社群分类 select「请选择社群分类」
  - ＊列表封面：占位框 90x90「+300*300」+ 「📷 从云端素材选择」线框按钮
  - ＊内容头图：占位框 110x36「+750*250」+ 「📷 从云端素材选择」线框按钮
  - 社群标签 「+ 添加标签」虚线按钮
  - 报名条件 radio：注册登录 / 完善资料（无需实名认证）/ 需实名认证
  - 收费金额 input +「元」+ 蓝条「0元为免费」
  - 推广奖励 input +「元」+ 蓝条「推广红娘名下会员按照平台流程成功加入本群的奖励金额；用户付费入群后将自动计入到推广红娘账号的余额中」
  - ＊社群发起 input placeholder「输入昵称关键词」+ 蓝条说明
  - 本群介绍 富文本编辑器(toolbar 20 个 lucide 图标)+ contentEditable body placeholder「请输入正文」+ 蓝条「本内容显示在支付完成入群费后弹出的页面」
  - 显示排序 input + 蓝条「数字越大显示越靠前」
- 状态管理：useState gxOpen/signupMode。
- `.gx-*`：panel(560px)/head-actions/upload-btn/cancel/row/label(88px)/content/input(-wide/-num)/info(蓝条)/select(-wide 右箭头 SVG)/cover-row/cover(虚线)/cover-square(90x90)/cover-wide(110x36)/pick-btn(线框)/add-tag(虚线)/options/radio/inline/unit/editor(-toolbar/-tool/-body/-placeholder)。`npx tsc --noEmit` 通过。

## 销售匹配库 创建/编辑 + 功能配置 Drawer
- 路由 `/sales-match`(运营工具下直接子菜单「销售匹配库」，导航 AdminLayout line68 已指向)。原静态页存在仅按钮无 Drawer，本次为「创建销售匹配库」「功能配置」两按钮绑定 Drawer 打开逻辑。
- 复用 `AdminBreadcrumb/getBreadcrumb/.ecl-notice/-body/-ic/-text/-title/.finord-card/.finord-filters/.finord-searchbox/.finord-search-label/.finord-select/.finord-search-input/.finord-btn/-primary/.finord-table-wrap/.finord-table/.ecl-empty/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body/.mp-switch/-label/-knob`，新增 `.sm-*`。
- **「创建销售匹配库」Drawer** (`.sm-panel` 右侧 560px)：复用 tlc 骨架，头部 ✕+标题「创建/编辑销售匹配库」+ 右侧「☁ 抱抱此上传」/取消/确定提交。
  - ＊页面标题 input placeholder「最多20文字」
  - 分享描述 input placeholder「请输入分享描述」
  - 客户性别 radio 男性/女性(默认男性)
  - 关联账号 input placeholder「请输入账号昵称」+ 右侧 inline radio「按昵称/按手机」+ 2 段蓝条说明(按昵称/按手机匹配逻辑+绑定账号权限)
  - 智能匹配 switch「关闭」+ 蓝条「开启本功能后，眼缘库中的智能匹配将按照下面的条件筛选显示(不包含脱单会员)」
  - ＊销售红娘 select「请选择销售红娘」
  - ＊选人上限 input「10」+ 蓝条「该客户最多可选择的眼缘人选的数量限制」
  - 状态 radio 已到店/未到店(默认未到店)
  - 温馨提示 textarea placeholder「请输入温馨提示」rows=4 + 蓝条「这是前端进入到该匹配库时页面中弹出的内容」
  - 底部「确定提交」蓝钮
- **「功能配置」Drawer** (`.sm-cfg-panel` 右侧 620px)：头部 ✕+标题「功能配置」+ 右侧三钮。
  - **头部宣传图**: radio 展示(默认)/隐藏 + 「710像素X120像素」尺寸说明 + 渐变占位图(我 你 她 他 的 采 文字) + 「⬆ 上传图片」线框按钮
  - **导航与功能控制** 4 行(嘉宾海选/红娘推荐/智能匹配/眼缘人选)：每行 name + `.sm-cfg-nav-tag` 淡蓝标签 + mp-switch 显示开关 + ↑上移/↓下移 按钮(可交换行序)
  - **会员资料页展示内容控制** 4 项(认证情况/红娘介绍+择偶要求/个人资料+个人介绍)：每项 name + mp-switch 显示开关(择偶要求/个人介绍右侧另起一项)
- 状态管理：useState createOpen/cfgOpen/gender/acctMode/status/smart/showBanner/navItems/toggles。
- `.sm-*`：panel(560px)/cfg-panel(620px)/head-actions/upload-btn/cancel/row/label(88px)/content/input(-wide/-num)/info(蓝条)/options/-inline/radio/acct-row/switch-row/switch-text/select(-wide 右箭头 SVG)/textarea/submit-row/submit/cfg-block/-block-title/radio-row/banner-size/banner(渐变占位 90px)/banner-btn(线框)/nav/-nav-row/-nav-name(80px)/-nav-tag(淡蓝标签)/-move(线框按钮)/-mat/-mat-row/-mat-name(-right)。`npx tsc --noEmit` 通过。

## 内容单页 添加/编辑单页 Drawer
- 路由 `/single-page`(运营工具下直接子菜单「内容单页」，导航 AdminLayout line69 已指向)。原静态页存在仅按钮无 Drawer，本次为「＋ 添加单页」按钮绑定 Drawer 打开逻辑。
- 复用 `AdminBreadcrumb/getBreadcrumb/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body/.finord-btn/-primary`，新增 `.ssp-*`。
- **「＋ 添加单页」Drawer** (`.ssp-panel` 右侧 600px)：复用 tlc 骨架，头部 ✕+标题「添加/编辑单页」+ 右侧「☁ 抱抱此上传」/取消/确定提交。
  - ＊页面标题 input placeholder「最多6汉字」
  - 跳转到指定页面 radio 是/否(默认否)
  - 页面内容 富文本编辑器(toolbar 22 个 lucide 图标: H/Tt/B/I/U/S/左中右对齐/无序有序列表/链接/图片/表情/引用/代码/更多/撤销/重做/分割线/全屏/清空)+ contentEditable body min-height 180px placeholder「请输入正文」
  - 分享图标 占位框 90x90「+ 上传图片」虚线框
  - 分享标题 input placeholder「请输入分享标题」
  - 分享摘要 textarea placeholder「请输入分享摘要」rows=3
  - 底部「确定提交」蓝钮
- 状态管理：useState addOpen/jumpTo。
- `.ssp-*`：panel(600px)/head-actions/upload-btn/cancel/row(-top)/label(96px)/content/input(-wide)/options/radio/editor(-toolbar/-tool/-body min 180px/-placeholder)/pick(90x90 虚线)/textarea/submit-row/submit。`npx tsc --noEmit` 通过。

## 获客落地页 创建落地页 Drawer
- 路由 `/customer-landing`(超级获客二级菜单首项「落地页」，导航 AdminLayout line70 已指向)。原静态页存在仅按钮无 Drawer，本次为「＋ 创建落地页」按钮绑定 Drawer 打开逻辑。
- 复用 `AdminBreadcrumb/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body/.finord-btn/-primary`，新增 `.cll-*`。
- **「＋ 创建落地页」Drawer** (`.cll-panel` 右侧 700px)：复用 tlc 骨架，头部 ✕+标题「创建获客落地页」+ 右侧「☁ 抱抱此上传」/取消/确定提交。
  - ＊标题 input placeholder「最多30汉字」
  - 表单形式 radio 内置表单/自由表单(默认内置) + 内联蓝链「效果预览」(仅自由表单显示)
  - 页面形式 radio 单页/引导页(默认单页) + 内联蓝链「预览」
  - 字段 15 项复选框(昵称/性别/头像/出生/职业/婚况/学历/身高/体重/收入/现居/籍贯/民族/手机/微信)分两行排列，昵称/性别/手机三项必填且不可取消(必填项配蓝字(必填))
  - ＊表单抬头 input placeholder「最多30汉字」
  - 头部图片 占位框 90x90「+ 上传图片」虚线 + 蓝条「推荐尺寸750*400，若不上传则默认为模板中的图片」
  - 头部自定义 富文本编辑器(toolbar 22 个 lucide 图标)+ contentEditable body + placeholder「请输入正文」
  - 分享图标 占位框「+ 上传图片」+ 蓝条「推荐尺寸300*300，在线裁剪」
  - 分享描述 input「请输入描述」
  - 数据位置 radio 保存到客源线索(默认)/保存到会员资料(并自动注册用户账号) + 蓝条「此处设置后不可修改」
  - 手机号码 radio 无需验证(手机号直接填写无需验证)/需验证(手机号码需要进行验证码验真息才可以提交)(默认需验证)
  - 默认分派 radio 待分派(默认)/分派给指定红娘 + 蓝条「可以选择多个红娘，系统会自动将本落地页获取来的客源随机循环分派给这些指定的红娘」
  - 推广红娘 input 240px placeholder「请输入用户账号」+ 蓝条「设置推广红娘后，通过该落地页获取的客源均将特定到该推广红娘名下」
  - 页面跳转 input placeholder「请输入https://」+ 蓝条「页面跳转是指客户在落地页成功提交资料后进入的页面，留空则跳转至平台首页；自由表单模式下，若留空的话则在提交后默认返回到平台首页」
  - 按钮文案 input default「立即加入」
  - 提交按钮背景色 64x32 紫色色块(.cll-color-swatch #6b5dd3)+ 蓝链「恢复默认」
- 状态管理：useState addOpen/formType/pageType/fields/dataPos/phoneVer/assignMode。toggleField 处理必填项不可取消。
- 字段数组常量 `FIELD_OPTIONS` 15 项；`REQUIRED_FIELDS` 3 项(昵称/性别/手机)。
- `.cll-*`：panel(700px)/head-actions/upload-btn/cancel/row(-top)/label(100px)/content/input(-wide)/options(-row 22px gap)/radio/link(蓝链)/fields(wrap gap 14x22)/check(.req 蓝字)/info(蓝条)/pick(90x90 虚线)/editor(-toolbar/-tool/-body min 120px/-placeholder)/color-row/swatch(64x32 圆角)。`npx tsc --noEmit` 通过。

## 自由表单 字段管理 Drawer
- 路由 `/free-form`(超级获客二级菜单次项「自由表单」，导航 AdminLayout line70 已指向)。原静态页存在仅按钮无 Drawer，本次为「▦ 字段管理」按钮绑定 Drawer 打开逻辑。
- 复用 `AdminBreadcrumb/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body/.finord-btn/-primary/.finord-link`，新增 `.ffm-*`。
- **「▦ 字段管理」Drawer** (`.ffm-panel` 右侧 700px)：复用 tlc 骨架，头部 ✕+标题「字段管理」+ 右侧「＋ 新建字段」蓝钮。
  - 蓝色须知条 `.ffm-notice`「① 称呼、性别、手机号码为系统中客户资料的同名字段一致，入库时会将同步的数据新建字段一旦在被落地页中添加使用，则不支持删除」
  - 5 列表格(列名：字段名称/引导文案/字段类型/必填/操作)，14 行真实数据(均为「内置」)
- 字段数据常量 `FIELD_ROWS` 14 项(称呼/头像/职业/学历/体重/现居/微信/性别/出生/婚况/身高/收入/籍贯/手机)，其中称呼/性别/手机为必填(置 (必填) 蓝字)。每行 引导文案/字段类型/必填(是/否 蓝/灰)/操作(编辑 蓝链)。
- 状态管理：useState fieldOpen。
- `.ffm-*`：panel(700px)/head-actions/body(padding)/notice(蓝条)/table-wrap/table(th/td 居中,第1列字段名称左对齐)/name(黑)/builtin(12px 灰)/req(12px 蓝)/yes(蓝)/no(灰)。`npx tsc --noEmit` 通过。

## 会员分区 添加分区 Drawer
- 路由 `/tool-theme`(运营工具下直接子菜单「会员分区」，导航 AdminLayout line72 已指向)。原静态页存在仅按钮无 Drawer，本次为「＋ 添加分区」按钮绑定 Drawer 打开逻辑。
- 复用 `AdminBreadcrumb/getBreadcrumb/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body/.finord-btn/-primary`，新增 `.tz-*`。
- **「＋ 添加分区」Drawer** (`.tz-panel` 右侧 560px)：复用 tlc 骨架，头部 ✕+标题「添加分区」+ 右侧「☁ 抱抱此上传」/取消/确定提交。
  - ＊分区名称 input + 蓝条「① 建议不要超过6个汉字，如：高颜值专区」
  - ＊宣传标语 input + 蓝条「① 建议不要超过20个汉字」
  - 进入页面弹窗 radio 关闭/启用(默认关闭)
  - 会员数据 radio 自动条件筛选/指定会员(可支持会员自助申请加入) + 蓝条「① 根据您设置的条件实时自动筛选出数据并显示在该分区中」
  - 头部背景 90x90「+ 上传图片」虚线框 + 「取消背景」线框按钮 + 蓝条「① 最佳尺寸：750像素×345像素，点击可重新上传」
  - 分区页模板 5 个模板卡(60x100 渐变占位 + 头像圆点)单选(默认第一张选中)
  - 分享标题 input
  - 分享摘要 input
  - ＊分享封面 90x90「+ 上传图片」虚线框 + 蓝条「① 最佳尺寸：300像素×300像素」
  - 是否上线 radio 暂不上线/上线(默认上线)
- 状态管理：useState addOpen/popup/dataMode/tplIdx/online。
- 模板渐变常量 `TZ_TPL_COLORS` 5 组双色调。
- `.tz-*`：panel(560px)/head-actions/upload-btn/cancel/row(-top)/label(96px)/content/input(-wide)/info(蓝条)/options/radio/bg-row/pick(90x90 虚线)/cancel-bg(线框按钮)/tpl-row/tpl(透明 input)/tpl-frame(60x100 渐变 占位 选中蓝边)/tpl-rows/-span/-avatar。`npx tsc --noEmit` 通过。

## 送礼物 添加礼物 Drawer
- 路由 `/love-gift-wrap`(运营工具下直接子菜单「送礼物」礼物管理 tab，导航 AdminLayout line74 已指向)。原静态页存在仅按钮无 Drawer，本次为「💳 添加礼物」按钮绑定 Drawer 打开逻辑。
- 复用 `AdminBreadcrumb/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body/.finord-btn/-primary`，新增 `.gi-*`。
- **「💳 添加礼物」Drawer** (`.gi-panel` 右侧 540px)：复用 tlc 骨架，头部 ✕+标题「添加礼物」+ 右侧「☁ 抱抱此上传」/取消/确定提交。
  - ＊礼物名称 input + 蓝条「① 不要超过8个汉字」
  - ＊礼物单位 input default「个」
  - ＊礼物图片 90x90「+ 上传图片」虚线框
  - ＊购买所需积分数 input「积分」+ 蓝条「① 礼物赠送方需要支出的积分」
  - ＊平台奖励积分 input「积分」+ 蓝条「① 礼物获赠方获得的平台奖励积分」
- 状态管理：useState addOpen。
- `.gi-*`：panel(540px)/head-actions/upload-btn/cancel/row/label(120px)/content/input(-wide/-num 120px)/info(蓝条)/pick(90x90 虚线)/inline/unit。`npx tsc --noEmit` 通过。

## 礼品管理 添加礼品 Drawer
- 路由 `/gift-list`(积分商城二级菜单首项「礼品管理」，导航 AdminLayout line77 已指向)。原静态页存在仅按钮无 Drawer，本次为「＋ 添加礼品」按钮绑定 Drawer 打开逻辑。
- 复用 `AdminBreadcrumb/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body/.finord-btn/-primary`，新增 `.gl-drawer-*` / `.gl-d-*` / `.gl-editor-*` / `.gl-info` / `.gl-err`。
- **「＋ 添加礼品」Drawer** (`.gl-drawer-panel` 右侧 560px)：复用 tlc 骨架，头部 ✕+标题「添加礼品」+ 右侧「☁ 抱抱此上传」/取消/确定提交。
  - ＊标题 input
  - ＊图片 90x90「+ 上传图片」虚线框 + 蓝条「① 最佳尺寸：750像素×750像素」
  - 性质 radio 实物快递(默认)/实物自取/虚拟物品
  - ＊积分 input default「100」
  - ＊库存 input default「99」
  - ＊须知 富文本编辑器 + contentEditable body placeholder「请输入兑换须知」+ 蓝条「① 限制200字」+ 红字错误提示「请输入兑换须知」
  - ＊介绍 富文本编辑器 + contentEditable body placeholder「请输入正文」+ 蓝条「① 限制500字」+ 红字错误提示「请输入礼品介绍」
  - 底部「确定提交」蓝钮
- 状态管理：useState addOpen/nature。
- 封装子组件 `GiftEditor` 复用 toolbar 22 个 lucide 图标 + 蓝条 + 红字错误信息(传入 placeholder/maxChars/errText)。
- 工具栏常量 `GL_TOOLBAR` 22 项(H/Tt/B/I/U/S/左中右对齐/无序/有序/引用/链接/图片/表情/代码/更多/撤销/重做/分割线/全屏/清空)。
- `.gl-drawer-*`：panel(560px)/head-actions/upload-btn/cancel/row(-top)/label(80px)/content/input(-wide)/info(蓝条)/options/radio/pick(90x90 虚线)/submit-row/submit/`.gl-editor`/editor-toolbar/editor-tool/editor-body(min 160px)/editor-placeholder/`.gl-info`/`.gl-err`(红字)。`npx tsc --noEmit` 通过。

## 全局：移除所有 Drawer 头部"☁ 抱抱此上传"按钮
- 用户要求去掉所有 Drawer 头部的"☁ 抱抱此上传"蓝钮。
- 涉及 11 个页面 12 个 Drawer 头：customer-landing/cll, free-pay/fp(DrawerHead 共享 1 个), group-list/gx, gift-list/gl, group-menu/gp, love-gift-wrap/gi, qrcode-wrap/fq, sales-match/sm(CreateMatch+FuncConfig 2 个), single-page/ssp, tool-theme/tz, xixun-list/xx。
- 全部移除后头部只保留「取消/关闭」「确定提交」按钮。
- `npx tsc --noEmit` 通过。

## 短视频-参数配置页面
- 路由 `/short-video-config`(短视频二级菜单首项「参数配置」，导航 AdminLayout line62 已指向；原为 ListPage 通用空态页，本次重写为静态还原页)。复用 `AdminBreadcrumb/getBreadcrumb/.finord-card/.finord-btn/-primary`，新增 `.svc-*`。
- 面包屑三级：首页/短视频/参数配置(getBreadcrumb("短视频","参数配置"))。
- **卡片 1：参数配置** 标题行 svc-head(左「参数配置」+ 右「确定提交」蓝钮) + 表单行：
  - 自定义栏目名称 input default「脱单加油站」
  - 栏目首页分享图片 占位框 110x110(`.svc-pick` 内含 svc-pick-preview 红色渐变方块+ svc-pick-btn「上传图片」文字按钮) + 右侧「分享效果预览」区(`.svc-share-preview` 小卡 240px：脱单干货 + 摘要 + 红角标)
  - 栏目首页分享标题 input default「脱单干货」
  - 栏目首页分享摘要 textarea default「了解我们，分享脱单干货和直播高光片段...」
  - 普通会员发布视频 radio 需要审核(默认)/无需审核 + 蓝条「① 在编辑中重新上传了视频，也是要平台重新审核」
  - 普通会员发布评论 radio 需要审核/无需审核(默认无需)
  - 认证会员发布视频 radio 需要审核(默认)/无需审核
  - 认证会员发布评论 radio 需要审核/无需审核(默认无需)
  - 免审核白名单 textarea + 蓝条「输入账号昵称，多个用逗号隔开；白名单的用户发布视频和评论不需要审核」
  - 其他配置(`.svc-other` flex inline)：[100] 次 + 自动上热门 + [7] 天内的视频视为新上视频
  - 现金打赏随机范围：[1] 元 至 [100] 元
  - 红包倒计时：[10] 秒 + 蓝条「① 可以设置一个最长时间，当视频播放时间超过设置的时候就可以领红包，不必等放完。若视频时长不足设置的时间则按照视频本身时长计」
  - ＊视频发布协议 富文本编辑器(toolbar 22 个字符按钮：H/B/Tt/f/I/U/S/≡/☰/≡/••/1./❝/😊/🖼/🎬/📝/—/↶/↷/⋯/⤢)+ contentEditable body 含 2 段协议内容(网络短视频内容审核基本标准/具体细则 + 常见问题列表)
  - 底部「确定提交」蓝钮
- **卡片 2：数据刷粉** svc-section-title「数据刷粉」+ 3 行(增加人气 100-999 + 执行 + 蓝条 / 增加点赞数 100-999 + 执行 + 蓝条 / 刷新发布时间 1-1000 + 执行 + 蓝条)
- **卡片 3：视频分类** svc-section-title「视频分类」+ 5 个 chip 标签(关于我们/脱单干货/活动瞬间/优质嘉宾/直播切片)每个带 ✎/×/⋮⋮ 三操作 icon + 右侧「＋ 添加分类」虚线按钮
- 状态管理：useState normalPost/normalComment/verifiedPost/verifiedComment。
- 协议内容常量 `AGREEMENT_CONTENT` 含完整审核标准/细则文本(详见代码字符串)。视频分类数组 `VIDEO_CATEGORIES` 5 项。工具栏 `TOOLBAR` 22 项字符按钮。
- `.svc-*`：card/head/title/submit-head(右蓝钮)/section-title/row(-top)/label(130px)/req(红星)/content/input(-wide/-sm)/textarea/info(蓝条)/options(-row)/radio/share-wrap/pick(110x110 虚线+红渐变预览)/pick-preview(70px 红渐变)/pick-btn(灰 hover 蓝)/share-preview/...-title/...-card/...-head/...-name/...-body/...-badge(红渐变小角标)/other/range/unit/tilde/editor(360px)/editor-toolbar/editor-tool(字符按钮)/editor-body(320px)/editor-text(白空格预格式化)/submit-row/submit/brush/input-brush/dash/brush-btn(执行)/tags-row/tag(蓝底胶囊)/tag-icons/-icon/add-tag(虚线蓝底)。`npx tsc --noEmit` 通过。

## 短视频-视频管理页面
- 路由 `/short-video-list`(短视频二级菜单次项「视频管理」，导航 AdminLayout line62 已指向；原为 ListPage 通用空态页，本次重写为静态还原页)。复用 `AdminBreadcrumb/getBreadcrumb/.finord-card/.finord-table-wrap/.finord-table/.finord-btn/-primary/.finord-link/.mp-switch/-knob`，新增 `.svl-*`。
- 面包屑三级：首页/短视频/视频管理(getBreadcrumb("短视频","视频管理"))。
- **主页面**：标题行 svl-head(左「视频管理」+ 右 svl-head-actions 两个蓝钮「＋ 添加视频」+「⟳ 一键刷新发布时间」)→ 筛选条 svl-filters(灰底 5+3 下划线 tab「全部/通过/待审/未通过」+「置顶/推荐/热门」+ 全部分类▾ select + 按标题搜 radio + 输入框 + 搜索蓝钮 + 按发布时间▾ select)→ 13 列表格(列：复选框/ID/封面/描述/浏览权限/关联内容/统计数据/打赏收入/红包/显示/审核/属性/操作)。
- 数据 4 行截图真实(脱单干货/活动瞬间/优质嘉宾/关于我们 4 个分类)：每行 封面(`.svl-cover` 60x80 粉渐变方块+分类名文字)+ 描述 4 行(蓝字文案+分类/时长/发布/时间 4 行灰小字)+ 浏览权限 select「必须先登录▾」+ 关联内容「自定义」+ 统计数据 3 行 + 打赏收入「0元」红字 + 红包「有红包」灰底+开关 + 显示开关 + 审核 select「通过▾」绿字 + 属性 3 个开关(置顶/推荐/热门) + 操作 编辑/预览视频/删除 三行蓝链。
- 交互：置顶/推荐/热门三个开关可点击切换(flags 状态管理)。
- **「＋ 添加视频」Drawer** (`.svl-add-panel` 右侧 560px)：复用 tlc 骨架，头部 ✕+标题「添加视频」+ 右侧「取消」/「确定提交」。
  - ＊发布账号 input「请输入账号昵称」
  - ＊上传视频 90x90「+ 上传视频」虚线
  - 视频封面 radio 系统自动截图(默认)/自定义上传
  - ＊视频分类 select「选择视频分类」
  - ＊视频描述 textarea「不要超出50汉字」rows=2
  - 关联链接 radio 不关联(默认)/自定义/关联相亲资料/关联平台活动/关联平台首页
  - 显示排序 input + 蓝条「① 数字越大显示越靠前」
  - 虚拟播放数 input「0」+ 蓝条「① 修改后将在此基数上累计计算」
  - 功能设置：评论 开关 + 打赏 开关 + 发布时间 ...(省略号)
  - 浏览权限 select「不限」
- 状态管理：useState statusTab/searchMode/addOpen/flags/cover/link。
- 数据数组 `videos` 4 行(VideoRow 接口)。状态 tab 数组 `STATUS_TABS` 4 项,标记 tab 数组 `FLAG_TABS` 3 项。
- `.svl-*`：card/head/title/head-actions/add-btn/refresh-btn/filters(灰底)/tabs/tab(.active 蓝边蓝字)/tab-sep/select(28px 右箭头 SVG)/time-select(100px)/radio(12px)/input(160px)/search-btn/table(min-width 1380px, th/td 居中,第4/7/12/13列左对齐)/col-check/check/id/cover(60x80 粉渐变)/desc(320px)/desc-text(蓝)/desc-meta(灰)/meta-blue(蓝)/perm-select/audit-select(绿)/stats/tip(红)/redpacket/-text/flags/flag-row/ops/op-del/`.svl-add-*`：panel(560px)/actions/cancel/row/label(96px)/content/input(-wide)/pick(90x90 虚线)/options(-row)/radio/select(-wide 右箭头 SVG)/textarea/info(蓝条)/func/-item/-dots。`npx tsc --noEmit` 通过。

## 短视频-红包记录页面
- 路由 `/short-video-red-packet`(短视频二级菜单第三项「红包记录」，导航 AdminLayout line62 已指向；原为 AdminSearchBar+DataTable 通用空态页，本次重写为静态还原页)。复用 `AdminBreadcrumb/getBreadcrumb/.finord-card/.finord-table-wrap/.finord-table/.finord-btn/-primary/.finord-link/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body`，新增 `.rpk-*`。
- 面包屑三级：首页/短视频/红包记录(getBreadcrumb("短视频","红包记录"))。
- **主页面**：标题 rpk-title「红包记录」+ 筛选条 rpk-filters(灰底 3 个下划线单选 tab「全部/未领完/已领完」+ 「按昵称搜▾」select + 输入框 200px + 搜索蓝钮)→ 10 列表格 → 分页右对齐 ‹ 1 ›。
- 表格列：ID/发红包时间/发红包的人/发给的视频/支付状态/红包金额/份数/是否均分/领取状态/操作。
- 数据 5 行截图真实(后台发放的 1.00 元红包，每行 5 个视频对应)：状态全「已支付」绿标 + 金额「1.00元」红字 + 份数 10/15/20 + 「否」+ 领取状态「未领完」蓝标 + 下方 11px 灰小字「还剩X份 0.XY 元」 + 操作「领取明细」蓝链(点击打开 Drawer)。
- **「领取明细」Drawer** (`.rpk-detail-panel` 右侧 460px)：复用 tlc 骨架，头部 ✕+标题「领取明细」。
  - 3 列表格：领取人 / 领取时间 / 领取金额
  - 1 行数据：领取人「6<em>xxxx</em> N」蓝字编号 + 2026-06-14 14:12:40 + 0.02元 红字
- 状态管理：useState tab/detailOpen。
- 数据数组 `rows` 5 行 (RpRow 接口)。
- `.rpk-*`：card/title/filters(灰底)/tabs/tab(.active 蓝下划线)/select(30px 右箭头 SVG)/input(200px)/search-btn/table(min-width 1100px, th/td 居中,第4列发给的视频左对齐)/id/time/sender/video(蓝)/status(-paid 绿/-pending 蓝 标签)/amount(红)/num/claim/-meta(11px 灰)/pager(-arrow/-cur)/`.rpk-detail-*`：panel(460px)/body(padding)/table(3列)/user(蓝+em 灰)/time/amount(红)。`npx tsc --noEmit` 通过。

## 短视频-评论管理页面
- 路由 `/short-video-comment`(短视频二级菜单第四项「评论管理」，导航 AdminLayout line62 已指向；原为 ListPage 通用空态页，本次重写为静态还原页)。复用 `AdminBreadcrumb/getBreadcrumb/.finord-card/.finord-table-wrap/.finord-table/.finord-btn/-primary/.finord-link`，新增 `.cmt-*`。
- 面包屑三级：首页/短视频/评论管理(getBreadcrumb("短视频","评论管理"))。
- 结构：标题 cmt-title「评论管理」+ 筛选条 cmt-filters(灰底 4 个下划线单选 tab「全部/通过/待审/未通过」+ 右侧全选复选框)→ 9 列表格 → 分页右对齐 ‹ 1 ›。
- 表格列：复选框/ID/评论内容(内容+原文双行)/点赞数/发布时间/IP/会员/审核 select/操作。
- 数据 3 行截图真实(ID 3,2,1,2026-06-12 18:03:42/17:29:43/17:28:41)：
  - 评论内容上下两段：上方 13px 黑字("太真实了😂😂"/"总结的太到位了，感觉我就是我这样😂") + 下方 11px 灰小字「原文：相亲一定要先见见面再聊天！！文字都是冷冰冰的，真实的见面才能拉近两颗心的距离 ❤」
  - 点赞数 0、IP 180.111.215.178 / 116.147.253.130、会员 不吃猪肉/出现1/出现1
  - 审核 select「通过▾」绿字
  - 操作 编辑/回复/删除 三行蓝链
- 交互：表头全选 + 行复选框双 state 联动(headChecked + rowChecked 数组)。
- 数据数组 `rows` 3 行 (CmtRow 接口)。状态 tab 数组 `STATUS_TABS` 4 项。
- `.cmt-*`：card/title/filters(灰底)/tabs/tab(.active 蓝下划线)/head-check/table(min-width 1180px, th/td 居中,第3/9列左对齐)/col-check/check/id/content(360px)/content-text(13px 黑)/content-source(11px 灰)/likes/time/ip/member/audit-select(26px 绿字)/ops/op-del/pager(-arrow/-cur)。`npx tsc --noEmit` 通过。

## 短视频-会员主页页面
- 路由 `/short-video-homepage`(短视频二级菜单第五项「会员主页」，导航 AdminLayout line62 已指向；原为 AdminSearchBar+DataTable 通用空态页，本次重写为静态还原页)。复用 `AdminBreadcrumb/getBreadcrumb/.finord-card/.finord-table-wrap/.finord-table/.finord-btn/-primary/.finord-link/.mp-switch/-knob`，新增 `.svh-*`。
- 面包屑三级：首页/短视频/会员主页(getBreadcrumb("短视频","会员主页"))。
- 结构：标题 svh-title「会员主页」+ 筛选条 svh-filters(input 220px placeholder「请输入会员昵称」+ 搜索蓝钮)→ 12 列表格 → 分页右对齐 ‹ 1 ›。
- 表格列：ID/会员昵称/微信号/主页简介/作品数/播放量/粉丝量/获赞量/获得打赏/认证/创建时间/操作。
- 数据 4 行截图真实(ID 4 扒姐说媒 14 作品 97648 播放 0 粉 7 赞 0元 / ID 3 查营家-扒姐助理 0/0/0/0 0元 / ID 2 不吃猪肉 0/0/1/0 0元 / ID 1 SG小Q-WS 0/0/0/0 0元)：
  - 微信号/主页简介显示「-」灰字
  - 获得打赏「0元」红字
  - 认证 mp-switch「关闭」灰
  - 操作 预览主页/编辑资料/删除 三行蓝链
- 数据数组 `rows` 4 行 (HomeRow 接口)。
- `.svh-*`：card/title/filters/input(220px)/search-btn/table(min-width 1180px, th/td 居中,第2/12列左对齐)/id/nick/empty(灰)/num/tip(红)/time/ops/op-del/pager(-arrow/-cur)。`npx tsc --noEmit` 通过。

## 短视频-打赏管理页面
- 路由 `/short-video-tip`(短视频二级菜单第六项「打赏管理」，导航 AdminLayout line62 已指向；原为 AdminSearchBar+DataTable 通用空态页，本次重写为静态还原页)。复用 `AdminBreadcrumb/getBreadcrumb/.finord-card/.finord-table-wrap/.finord-table/.finord-btn/-primary`，新增 `.svt-*`。
- 面包屑三级：首页/短视频/打赏管理(getBreadcrumb("短视频","打赏管理"))。
- 结构：标题行 svt-head(左「打赏管理」+ 右 svt-total「共收到打赏：0元」红字)+ 筛选条 svt-filters(按标题搜▾ select + 输入框 200px + 搜索蓝钮)→ 13 列表格(列：复选框/ID/打赏视频/打赏用户/受赏用户/打赏附言/打赏形式/打赏数额/打赏时间/支付方式/支付单号/状态/操作)→ 空态 colSpan=13 显示「📦 + 暂时无数据」灰色小盒子图标。
- 状态：当前无数据，预留完整 12 列表头 + 复选框 + 居中空态样式 `.svt-empty-inner`。
- 列配置数组 `columns` 12 项 key/label 形式用于渲染表头。
- `.svt-*`：card/head/title/total/total-num(红)/filters/select(30px 右箭头 SVG)/input(200px)/search-btn/table(min-width 1240px, th/td 居中)/col-check/check/empty(padding 60px 0! important)/empty-inner(纵向 flex 居中)/empty-icon(36px 灰)/empty-text(灰)。`npx tsc --noEmit` 通过。

## 商家联盟-运营方案页面
- 路由 `/active-alliance`(商家联盟二级菜单首项「运营方案」，导航 AdminLayout line61 已指向；原为 ListPage 通用空态页，本次重写为文档式静态还原页)。复用 `AdminBreadcrumb/getBreadcrumb/.finord-btn/-primary/.finord-link`，新增 `.aa-*`。
- 面包屑三级：首页/商家联盟/运营方案(getBreadcrumb("商家联盟","运营方案"))。
- 结构：居中标题「联盟商家使用指导」+ 5 个章节。
- **一、商家联盟系统的价值与应用** + 4 列手机预览图(约会去哪里列表/合作商家+专属优惠 tab/赛博跳动运动空间/商品信息详情)
- **二、商家与婚恋平台的业务逻辑** + 段落说明
- **三、商家入驻-商品发布流程** + 4 步骤编号列表 + 4 列手机预览图(确认订单/确认订单+支付方式/购买成功弹窗/订单管理)
- **四、客户下单-支付-消费流程** + 段落说明 + 5 列手机预览图(推广红包统计/在售商品/订单记录/核销记录/核销记录)
- **五、订单分成-结算-提现流程** + 步骤列表 + 添加/编辑商品表单 mock(完整字段:合作商家/商品名称/商品封面/商品价格/合作优惠价/商家结算66元/红包分成推广红娘6.6+服务红娘8.8,红框高亮商家结算和红包分成两块) + 购买限制 + 2 列手机预览图(财务明细余额46.40+积分0/余额40,958.62+积分563) + 步骤列表 + 须知条 + 订单管理 mock(8 个筛选+导出EXCEL按钮+10列订单表格 6 行真实数据) + 步骤列表 + 3 列手机预览图(申请提现输入8888/435/10000 红包超额限制)
- 封装子组件：
  - `PhoneFrame` 手机外壳(200x360 圆角 18px + 4px 顶部 bar + 屏幕区)接受 idx 调色板
  - `MockLine` 灰线占位 (接受 width/color)
  - `MockBlock` 灰块占位 (接受 height/color)
- 调色板常量 `PHONE_PALETTE` 4 组(粉/蓝/橙/青)用于不同章节手机框边框色。
- 整体为阅读型文档(无按钮交互),核心在内容呈现+手机预览排版。
- `.aa-*`:doc(white bg)/doc-title(20px 居中)/section/-title(15px 600)/-text(13px 行高1.9 首行缩进2em)/-list(有序 24px left padding)/phones-row(flex gap 14px wrap)/phone(200x360 圆角18px 边框)+/-bar(60x4 圆点)/-screen(白底 flex 1)/-line(6px 圆角)/-block(灰)/-title(12px)/-tabs/-tab(.active 蓝)/-item(28x28 thumb)/-thumb/-thumb-lg(70px)/-item-info/-counter/-meta/-section-h/-pay(1px 灰边)/-buy-success(绿)/-ok(蓝)/-price(红)/-grid(2列)/-grid-item/-tag-row/-tag(胶囊)/-stat(粉蓝)/-stat-row/-stat-cell/-radio-row/`.aa-form-mock`：表单 mock 容器/-head/-close/-title/-actions/-cancel/-submit/-body/-row/-label(96px)/-input/-range/-num(80px)/-num-inline(60px)/-unit/-dash/-info(蓝条 margin-left 96px)/-highlight(红框 2px solid #e6495f 圆角)/`.aa-info-box`：须知条(flex icon+text)/`.aa-orders-mock`：订单 mock 容器/-filters/-select(100px)/-input(130px)/-export/-table(10列 min 1100px)/-product/-thumb(36x36)/-status(-已支付/-已消费 绿/-未支付 红/-已取消 蓝 4 态)/-amount(红)/-verify(-done 绿/-todo 灰)。`npx tsc --noEmit` 通过。

## 商家联盟-功能配置页面
- 路由 `/merchant-alliance-config`(商家联盟二级菜单次项「功能配置」，导航 AdminLayout line61 已指向；原为 ListPage 通用空态页，本次重写为静态还原页)。复用 `AdminBreadcrumb/getBreadcrumb/.finord-card/.finord-btn/-primary/.finord-link`，新增 `.mac-*`。
- 面包屑三级：首页/商家联盟/功能配置(getBreadcrumb("商家联盟","功能配置"))。
- 结构：标题 mac-title「功能配置」+ 9 个配置行：
  - 栏目浏览：网址路径 `https://www.xuanshi.com/subpages/hezuo/index` + 二维码「查看」+ 在线预览「点击这里」
  - 栏目标题 input「优选合作商城」
  - 栏目描述 input「精选同城优质服务定制产品套餐，为您的约会提供愉快的消费」
  - 分享封面 radio 系统默认(默认)/自定义(300*300)
  - 宣传头图：「↑ 上传」线框按钮 + 灰字「最佳尺寸：700像素x240像素」
  - 5 张宣传图占位(200x70 灰底)：每行右侧 ↑上移 / ↓下移 / 🗑删除 蓝链(可上下移动+删除,交互通过 setBanners 交换+过滤)
  - 商家分类 5 行：分类名 input(推荐餐饮/新奇体验/休闲娱乐/生活服务/结婚) + 右侧「↑上传图标/↑上移/↓下移/🗑删除」四按钮(可上下移动+删除,首尾行 disabled)
  - 「＋ 增加分类」蓝钮
  - 购买须知(默认)大 textarea 含 3 行默认文案「成功购买后请凭消费券号至消费二维码前往商家消费 / 您的短信中将收到券号,可在"订单-中查看订单和二维码 / 消费过程中若遇到使用问题请及时联系我们介入沟通」
  - 底部「确定提交」蓝钮
- 状态管理：useState shareCover/banners/categories。moveBanner/moveCategory 通过数组交换+removeBanner/removeCategory 过滤实现。
- 数据常量 `BANNERS` 5 行 `{id, movable}`,`CATEGORIES` 5 行 `{id, name, movable}`,`DEFAULT_NOTICE` 3 行购买须知默认文案。
- `.mac-*`：card/title/row(-top)/label(96px)/content(左对齐 flex wrap)/text-muted(灰)/url/input(-wide)/info(12px 灰)/options/radio/upload-pill(30px 线框按钮)/banners(纵向 gap 10px)/banner-row(-list)/banner-frame(200x70 灰底)/banner-actions/btn(12px 蓝链)/cats/cat-row/cat-input(200x32 灰底)/cat-icons/icon-btn(12px 蓝链, disabled 灰)/add-cat(蓝底)/notice(100px min-height 满宽)/submit-row/submit。`npx tsc --noEmit` 通过。

## 商家联盟-商家管理页面
- 路由 `/merchant-management`(商家联盟二级菜单第三项「商家管理」，导航 AdminLayout line61 已指向；原为 ListPage 通用空态页，本次重写为静态还原页+绑定「＋ 添加商家」Drawer)。复用 `AdminBreadcrumb/getBreadcrumb/.ecl-notice/-body/-ic/-text/-title/.finord-card/.finord-table-wrap/.finord-table/.finord-btn/-primary/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body`，新增 `.mm-*`。
- 面包屑三级：首页/商家联盟/商家管理(getBreadcrumb("商家联盟","商家管理"))。
- **主页面**：蓝色须知条(标题「须知」+「您在这里可以添加、管理全部的合作商家，掌握商品、订单、销售数据情况」)→ 卡片首行 mm-head(左「商家管理」+ 右「＋ 添加商家」蓝钮 onClick 打开 Drawer)→ 筛选条 mm-filters(按商家分类▾ select 140px + 按商家名称 input 240px + 搜索蓝钮)→ 11 列表格 colSpan=11 空态「📦 + 暂无数据」灰色小盒子图标。
- 表格列(11)：ID/商家名称/分类/管理账号/创建时间/商品统计/销售额统计/核销员工/展示/链接·二维码/操作。
- **「＋ 添加商家」Drawer** (`.mm-drawer-panel` 右侧 560px)：复用 tlc 骨架，头部 ✕+标题「添加/编辑商家」+ 右侧「关闭」/「确定提交」。
  - ＊商家名称 input placeholder「请输入商家名称」
  - ＊商家封面 90x90「+300*300」虚线占位
  - 商家相册 「🖼 图片上传/管理(支持移动排序、删除)」线框按钮
  - ＊管理账号 input placeholder「输入昵称关键词」+ 蓝条「① 必须拥有推广红娘身份，一个推广红娘只能绑定一个商家管理账号」
  - ＊商家分类 select placeholder「请选择商家分类」
  - 特色标签「+ 添加标签」虚线按钮
  - ＊商家地址 「请选择」select(160px) + 「请输入详细地址」input 横排
  - ＊联系电话 input placeholder「请输入联系电话」
  - ＊营业时间 input placeholder「请输入营业时间」
  - 商家介绍 富文本编辑器(toolbar 20 个 lucide 图标:H/Tt/B/I/U/S/左中右对齐/引用/链接/图片/表情/代码/无序/有序/分割线/撤销/重做/全屏)+ contentEditable body placeholder「请输入正文」+ 蓝条「本内容显示在支付完成入群费后弹出的页面」
  - 显示排序 input + 蓝条「数字越大显示越靠前」
- 状态管理：useState addOpen。
- 工具栏常量 `TOOLBAR` 20 项。列配置数组 `columns` 11 项。
- `.mm-*`：card/head/title/filters/select(30px 右箭头 SVG, 140px)/input(240px)/search-btn/table(min-width 1180px, th/td 居中)/empty(padding 60px 0! important)/empty-inner/-icon(36px 灰)/-text(灰)/`.mm-drawer-*`：panel(560px)/head-actions/cancel/row(-top)/label(96px)/content/input-wide/input-num(100px)/pick(90x90 虚线)/pick-btn(线框)/info(蓝条)/select-wide(满宽 右箭头 SVG)/select-addr(160px)/add-tag(虚线)/addr-row(flex)/editor(240px)/editor-toolbar/editor-tool/editor-body(200px)/editor-placeholder。`npx tsc --noEmit` 通过。

## 商家联盟-商品管理页面
- 路由 `/merchant-product`(商家联盟二级菜单第四项「商品管理」，导航 AdminLayout line61 已指向；原为 ListPage 通用空态页，本次重写为静态还原页+绑定「＋ 添加商品」Drawer)。复用 `AdminBreadcrumb/getBreadcrumb/.ecl-notice/-body/-ic/-text/-title/.finord-card/.finord-table-wrap/.finord-table/.finord-btn/-primary/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body`，新增 `.mp-*`。
- 面包屑三级：首页/商家联盟/商品管理(getBreadcrumb("商家联盟","商品管理"))。
- **主页面**：蓝色须知条(标题「须知」+「您可以在这里添加、管理所有的合作销售的商品或服务」)→ 卡片首行 mp-head(左「商品管理」+ 右「＋ 添加商品」蓝钮 onClick 打开 Drawer)→ 筛选条 mp-filters(所有商家▾ select 140px + 按商品名称 input 240px + 搜索蓝钮)→ 9 列表格 colSpan=9 空态「📦 + 暂无数据」灰色小盒子图标。
- 表格列(9)：ID/商品名称/商家名称/销售价格/添加时间/销量统计/上架/链接·二维码/操作。
- **「＋ 添加商品」Drawer** (`.mp-drawer-panel` 右侧 560px)：复用 tlc 骨架，头部 ✕+标题「添加/编辑商品」+ 右侧「关闭」/「确定提交」。
  - ＊合作商家 input placeholder「输入关键词选择」
  - ＊商品名称 input placeholder「不要超过30汉字」
  - ＊商品封面 90x90「+800*800」虚线占位
  - ＊商品价格 横排(原价值[100]元 至 合作优惠价[88]元) + 蓝条「① 合作优惠价为客户下单实际支付所得价格」
  - 商家结算 [0] 元/份 + 蓝条「① 订单核销成功后，平台给予商家结算的结算金额，本金额会自动计入到商家账号的"余额"中，且均有明细账务。0元表示不分成」
  - 推广红娘分成 radio 统一设置(默认)/按级别设置分成 + select「固定金额」+ [0] 元/份 + 蓝条「① 订单核销成功后，平台可设置给予下单人所属于的推广红娘分成(以订单创建时归属关系为准),本金额会自动计入到红娘账号的"余额"中，且均有明细账务。0元表示不分成。特别提醒：客户仅需在平台一链接即可绑定归属关系，给予推广人订单分成，无需完善相亲会员资料」
  - 合伙红娘分成 radio 统一设置(默认)/按级别设置分成 + select「固定金额」+ [0] 元/份 + 蓝条「① 如果该付款客户有归属合伙红娘，将获得对应的金额分成，本金额会自动计入到合伙红娘账号的"余额"中」
  - 服务红娘分成 select「固定金额」+ [0] 元/份 + 蓝条「① 订单核销成功后，平台可设置给予下单人所属于的服务红娘分成，本金额会自动计入到红娘账号的"余额"中。0元表示不分成」
  - 购买限制 radio 每个账号只能购买[0]份(默认)/每个订单只能购买[0]份 + 蓝条「① 若一个订单购买多份，只能一次性核销，无法分次核销」
  - 购买须知 radio 使用默认(默认)/自定义
  - 商品介绍 富文本编辑器(toolbar 20 个 lucide 图标)+ contentEditable body placeholder「请输入正文」
- 状态管理：useState addOpen/promoteMode/partnerMode/limitMode/noticeMode。
- 工具栏常量 `TOOLBAR` 20 项。列配置数组 `columns` 9 项。
- `.mp-*`：card/head/title/filters/select(-sm 100px)/input(240px)/search-btn/table(min-width 1080px, th/td 居中)/empty/-inner/-icon/-text/`.mp-drawer-*`：panel(560px)/head-actions/cancel/row(-top)/label(96px)/content/input-wide/input-num(100px)/unit/pick(90x90 虚线)/price-row(横向)/price-label/info(蓝条)/split-row(横排)/radio/range/limit-row(横排)/options/editor(240px)/editor-toolbar/editor-tool/editor-body(200px)/editor-placeholder。`npx tsc --noEmit` 通过。

## 商家联盟-订单管理页面
- 路由 `/merchant-order`(商家联盟二级菜单第五项「订单管理」，导航 AdminLayout line61 已指向；原为 ListPage 通用空态页，本次重写为静态还原页)。复用 `AdminBreadcrumb/getBreadcrumb/.ecl-notice/-body/-ic/-text/-title/.finord-card/.finord-table-wrap/.finord-table/.finord-btn/-primary`，新增 `.mo-*`。
- 面包屑三级：首页/商家联盟/订单管理(getBreadcrumb("商家联盟","订单管理"))。
- 结构：蓝色须知条(标题「须知」+ 2 段说明：1.未支付/已支付未核销状态下的订单均可随时取消订单 / 2.已支付订单(无论是否核销)若需退款,请在"财务管理-收入明细"中操作订单不可删除)→ 卡片首行无显式标题(截图未显示)、两行筛选条 mo-filters(纵向 flex gap 12px)→ 10 列表格 colSpan=10 空态「📦 + 暂无数据」灰色小盒子图标。
- **第一行筛选 mo-filters-row**：全部商家▾ select 110px + 订单状态▾ select 110px + 核销状态▾ select 110px + 「按商品关键词」input 160px + 「按订单号」input 160px + 按昵称▾ select 110px + 「请输入」input 160px
- **第二行筛选**：核销时间开始 → 核销时间结束(两个 date input 130px) + 「⬇ 导出EXCEL」蓝钮
- 表格列(10)：ID/下单商品/商家/下单人/下单时间/订单状态/订单金额/方式·时间/核销状态/操作。
- 状态：当前无数据，仅保留完整表头 + 居中空态样式 `.mo-empty-inner`。
- 列配置数组 `columns` 10 项。
- `.mo-*`：card/filters(纵向)/filters-row(横向 flex wrap gap 10px)/select(30px 右箭头 SVG, 110px)/searchbox(内联 flex)/input(160px)/text-muted(12px 灰)/daterange(横向)/date(130px)/export-btn/table(min-width 1180px, th/td 居中)/empty(padding 60px 0! important)/empty-inner/-icon(36px 灰)/-text(灰)。`npx tsc --noEmit` 通过。

## 活动报名菜单结构重组
- 修改 `src/components/AdminLayout.tsx` line 60「活动报名」菜单结构：将原 5 项平铺 items（参数配置/活动管理/报名管理/互选活动/互选记录）改为 4 项（参数配置/活动管理/报名管理/互选活动(嵌套)）。
- 「互选活动」改为带 `children` 的子菜单组，包含「活动管理」(`/mutual-selection-list`) + 「互选记录」(`/mutual-selection-record`)，与搭子社群/红娘喜讯等使用相同的 `children` 嵌套语法。
- `npx tsc --noEmit` 通过。

## 活动报名-参数配置页面
- 路由 `/active-config`(活动报名二级菜单首项「参数配置」，导航 AdminLayout line60 已指向；原为 ListPage 通用空态页，本次重写为静态还原页)。复用 `AdminBreadcrumb/getBreadcrumb/.finord-card/.finord-btn/-primary`，新增 `.ac-*`。
- 面包屑三级：首页/活动报名/参数配置(getBreadcrumb("活动报名","参数配置"))。
- 结构：两个独立卡片(卡片间留 16px margin-bottom)。
- **卡片 1：活动分类** ac-card + 标题 ac-title「活动分类」+ 5 个 chip 标签(专场活动/会面小聚/相亲大会/政企联谊/免费活动)每个带 ✎/×/⋮⋮ 三操作 icon(交互：× 可删除)+ 右侧「＋ 添加分类」虚线蓝底按钮。
- **卡片 2：参数配置** ac-card + 标题 ac-title「参数配置」+ 4 个字段行：
  - 自定义栏目名称 input default「同城活动」
  - 自定义默认图一 上传图片占位(200x110 虚线)+ 蓝条「① 最佳尺寸：900×383(与公众号首图一致)」
  - 自定义默认图二 上传图片占位(110x110 虚线方块)+ 蓝条「① 最佳尺寸：300像素x300像素」
  - ＊用户协议须知 富文本编辑器(toolbar 20 个 lucide 图标)+ contentEditable body min-height 240px + 含文字「线下活动规则」标题
  - 底部「确定提交」蓝钮
- 状态管理：useState categories(默认 5 项),removeCategory 通过过滤实现删除。
- 数据常量 `CATEGORIES` 5 项,工具栏 `TOOLBAR` 20 项。
- `.ac-*`：card/title/tags-row(横向 wrap gap 10px)/tag(蓝底胶囊)/tag-icons/-icon(灰 hover 蓝)/add-tag(虚线蓝底)/row(-top)/label(130px)/content/input(-wide)/pick(虚线)/-wide(200x110)/-square(110x110)/info(蓝条)/editor(280px)/editor-toolbar/editor-tool/editor-body(240px)/editor-content/submit-row/submit。`npx tsc --noEmit` 通过。

## 活动报名-活动管理页面
- 路由 `/active-list`(活动报名二级菜单次项「活动管理」，导航 AdminLayout line60 已指向；原为 ListPage 通用空态页，本次重写为静态还原页+绑定「＋ 发布活动」+「👁 预览活动栏目」按钮+「链接/二维码」查看 Drawer)。复用 `AdminBreadcrumb/getBreadcrumb/.ecl-notice/-body/-ic/-text/-title/.finord-card/.finord-table-wrap/.finord-table/.finord-btn/-primary/.finord-link/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body/.mp-switch/-knob`，新增 `.al-*`。
- 面包屑三级：首页/活动报名/活动管理(getBreadcrumb("活动报名","活动管理"))。
- **主页面**：蓝色须知条(标题「须知」+ 3 段：本系统实现活动发布/招募海报/二维码/报名链接/在线报名/在线收款/短信通知/活动签到/报名信息一键导入会员库/财务管理等功能 / 活动分为四种状态:"报名中"、"已满额"、"报名截止"、"已结束"，仅状态为"报名中"中可以报名 / 活动为"审核通过"且"上线"才会在平台上显示)→ 卡片首行 al-head(左「活动管理」+ 右 al-head-actions 两个蓝钮「＋ 发布活动」+「👁 预览活动栏目」)→ 筛选条 al-filters(input 240px + 搜索蓝钮)→ 11 列表格。
- 表格列(11)：复选框+ID / 活动 / 链接·二维码 / 分享海报 / 签到码 / 活动状态 / 报名人数 / 创建时间 / 审核状态 / 上线 / 操作。
- 数据 3 行截图真实(ID 8 单身青年免费择偶竞争力评分(免费活动/持续进行中/荟希老师 六月)/报名中/男生27女生14 / 2026-07-01 13:31:24 / 通过 / 上线; ID 7 7.26一年内结婚专场(专场活动/7月26日14:00-17:00/荟希老师 六月)/活动结束/男生1女生1 / 2026-06-27 17:44:18 / 通过 / 上线; ID 6 7.19黒心媒婆大赛(专场活动/7月19日14:00-17:00/荟希老师 六月)/活动结束/男生0女生1 / 2026-06-27 17:39:41 / 通过 / 下线)。
- 表格元素：60x70 渐变方块封面 + 4 行 meta 灰小字(分类/时间/地点/管理)+ 活动状态 tag(报名中绿/活动结束灰)+ 报名人数两行(男生/女生)+ mp-switch 上线开关 + 操作 4 行(复制/编辑/名单展示/删除)蓝链。
- **「＋ 发布活动」Drawer** (`.al-drawer-panel` 右侧 920px)：复用 tlc 骨架，头部 ✕+标题「添加活动」+ 右侧「取消」/「确定提交」。Drawer 主体为「左侧菜单 + 右侧表单」双栏布局,al-drawer-body flex。
  - 左侧菜单 al-drawer-sidebar(120px 宽 灰底): 10 个章节按钮(基本信息/时间地点/活动费用/报名要求/人数限额/红娘奖励/活动详情/活动提醒/其他设置/管理设置),点击切换 activeSection,选中态蓝左 border + 白底。
  - 右侧表单 al-drawer-form(满宽)按 activeSection 渲染 10 个章节(每个章节标题带蓝左 border「| 章节名」):
    - **基本信息**: 活动名称 input / 活动分类 select / 活动发起 input / 封面大图 200x110 虚线 + 蓝条 900x383 / 封面小图 110x110 虚线 + 蓝条 300x300
    - **时间地点**: 活动时间 input + 灰字「建议格式示例:9月28号(周六)下午2点-6点」/ 活动地址 input / ＊报名截止 date / ＊活动结束 date + 蓝条「该时间后活动状态自动变更为已结束」
    - **活动费用**: 横排「活动费用 男生[0]元 女生[0]元」+ 蓝条「0表示免费...」+ 费用名称 input「报名费」
    - **报名要求**: radio 任何人都可以报名(默认)/ 相亲会员才可以报名 + 蓝条 + 4×3 grid 11 项必填字段(姓名/手机/性别/年龄/身高/婚况/单位/收入/学历/头像/身份证号)+ 必须实名认证 checkbox + 蓝条
    - **人数限额**: radio 限制男女人数(默认)/ 仅限制总人数 + 男生[0]人 女生[0]人 + 蓝条 + 报名人数 男生[0]人 女生[0]人 + 蓝条 + 隐藏报名数 mp-switch + 蓝条
    - **红娘奖励**: 横排「推广红娘奖励/服务红娘奖励/合伙红娘奖励」三组 [0]元 + 蓝条「用户会员报名本活动支付费用后...」
    - **活动详情**: 富文本编辑器(toolbar 20 个 lucide 图标)+ contentEditable body min-height 180px placeholder「请输入活动详情」
    - **活动提醒**: 富文本编辑器 + contentEditable body placeholder「请输入正文」+ 蓝条「本内容将会显示在报名成功后的提醒页面」
    - **其他设置**: 客服微信 input / 客服二维码 110x110 虚线占位 + 蓝条「留空则自动默认显示平台客服的微信号和二维码」/ 浏览人气[0] / 显示排序[0] + 蓝条「数字越大显示越靠前」/ 自定义分享 mp-switch + 蓝条
    - **管理设置**: ＊管理红娘 input placeholder「请输入服务红娘的账号昵称」+ 蓝条「可以设置多个红娘来管理...」/ 短信通知 input placeholder「请输入手机号」+ 蓝条「设置号码后,有人报名活动时...」
- **「链接/二维码」查看 Drawer** (`.al-link-panel` 右侧 540px)：复用 tlc 骨架，头部 ✕+标题「链接/二维码」+ 右侧「关闭」。body al-link-body 显示「链接地址」label + 链接 input「https://www.xuanshi.com/subpages/active/index」+ 「二维码」label + 手机预览 al-phone(200x460 圆角 18px 边框 + 顶部 4px bar)：
  - 顶部 banner(紫粉渐变 135deg):「同城活动」标题 + 「百聊不如一见」粉色药丸 + 「同城相亲 交流活动」+ 「线下活动/线上互选」粉/黄按钮
  - 二维码方块 70x70(8x8 黑白格子 CSS 模拟)
  - 「单身青年 免费择偶竞争力评分」粉色渐变宣传块
  - 「1年内结婚 专场脱单」粉色小宣传块
  - 7.26 一年内结婚专场 卡片(粉色封面+标题+标签)
  - 底部 4 tab bar(最新发布/会员专区/已订婚会员/脱单情报员)
- 状态管理：useState addOpen/linkOpen/activeSection/onlineMap/signupMode/limitMode/customShare。
- 工具栏常量 `TOOLBAR` 20 项,左侧菜单数组 `SIDEBAR_SECTIONS` 10 项,数据数组 `rows` 3 行 (Row 接口)。
- `.al-*`:card/head/title/head-actions/publish-btn/preview-btn/filters/input(240px)/search-btn/table(min-width 1380px, th/td 居中)/row-content(check+id)/check/id/activity(左对齐 flex)/cover(60x70 渐变)/-info/-name(13px 黑)/-meta(11px 灰 4行)/-meta-blue(蓝)/activity-status(-报名中 绿/-活动结束 灰)/count/-num(加粗)/time/audit(绿)/ops(纵向)/`.al-drawer-*`：panel(920px)/cancel/body(flex 0)/sidebar(120px 灰底右 border)/nav(满宽左对齐 active 蓝左 border 白底)/form(满宽 padding)/section-head(14px 蓝左 border 3px)/row(-top)/label(96px)/content/input-wide/select(右箭头 SVG)/date/text-muted(12px 灰)/info(蓝条)/info-block(蓝条 margin-left 96px)/cover-pick(虚线)/-wide(200x110)/-square(110x110)/fee-row(横排 wrap)/num(100x30)/unit/radio-row(22px gap)/radio/form-grid(4列 8px row 16px col)/req(蓝)/checkbox(蓝)/switch-row/editor(220px)/editor-toolbar/editor-tool/editor-body(180px)/editor-placeholder/`.al-link-*`：panel(540px)/body(16 22 22)/url(13px 灰)/url-input(12px)/qr-title(13px 灰)/qr(flex 居中)/phone(200x460 圆角 18px 2px border)/-bar(60x4)/-screen(白底 flex-1)/-banner(紫粉渐变 padding 10px)/-top(12px 居中)/-pill(白底紫字 10px)/-sub(9px 灰白)/-btn(flex)/-btn-pink(粉)/-btn-yellow(黄)/-qr(70x70)/-grid(8列)/-cell(nth-child 2n+3 3n+1 黑底)/-poster(粉渐变)/-text(9px)/-poster-small(粉渐变)/-card(横排 6px padding)/-cover(32x32 粉渐变)/-info/-name(10px)/-tag(8px 灰)/-tab-bar(顶 border)/-tab(9px 灰 flex-1 居中)。`npx tsc --noEmit` 通过。

## 活动报名-报名管理页面
- 路由 `/active-signupmanager`(活动报名二级菜单第三项「报名管理」，导航 AdminLayout line60 已指向；原为 ListPage 通用空态页，本次重写为静态还原页+绑定「修改报名」Drawer)。复用 `AdminBreadcrumb/getBreadcrumb/.finord-card/.finord-table-wrap/.finord-table/.finord-btn/-primary/.finord-link/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body`，新增 `.sm-*`。
- 面包屑三级：首页/活动报名/报名管理(getBreadcrumb("活动报名","报名管理"))。
- **主页面结构**：
  - 顶部 sm-activity-row 活动筛选 select「活动：全部」200px
  - sm-stats-row 9 列表格统计卡片(彩色顶部 border + 大数字 + 灰小字):57人报名总数/50人首次报名/9人待审/46人审核通过/2人未通过/2352元报名费/57人未签到/0人已签到/17人活动入库(9 列网格布局,各自颜色 #c4cad6 灰/#fa8c16 橙/#2bb673 绿/#52c41a 绿/#ff4d4f 红/#3658f7 蓝/#13c2c2 青/#722ed1 紫/#eb2f96 粉)
  - 卡片 sm-card 标题 sm-title「报名管理」+ 右侧 sm-head-actions 两个蓝钮「📇 导出报名资料卡片」+「⬇ 导出EXCEL」
  - 筛选条 sm-filters(纵向):第一行 sm-filters-row 12 个元素横排(性别/审核/支付/签到 4 个 select + 全部/首次报名 radio + 开始日期/结束日期 date + 按昵称搜 select + 请输入 input + 搜索蓝钮)
  - 9 列表格 5 行真实数据(ID 60-56):
    - 报名会员:50x60 灰头像(底部"男"蓝标)+ 姓名「刺猬 / 范浩然」+ 6 行 meta 灰小字(2006年(20岁) / 大专 / 5-8千元 / 未婚,报名: 单身青年免费择偶竞争力评分)+ 「会员」红粉渐变小角标
    - 实名认证 tag(已实名蓝/未实名灰)
    - 手机「13880942587」或「13151401977 / 420602199706********」
    - 第几次报名「第1次」+ 复制 蓝链
    - 在线缴费 status(免费绿/未支付红+98.00元)
    - 报名审核「待审」蓝字
    - 推广红娘「-」灰
    - 操作: 修改报名 蓝钮/查看资料 蓝链(已实名)或资料入库 蓝钮(未实名)/删除 蓝链(纵向排列)
- **「修改报名」Drawer** (`.sm-edit-panel` 右侧 460px)：复用 tlc 骨架，头部 ✕+标题「修改报名」+ 右侧「取消」/「确定提交」。
  - 头像 90x90 灰渐变占位 + 「📷 上传图片」线框按钮
  - ＊姓名 input 范浩然
  - ＊性别 radio 男(默认)/女
  - ＊年龄 input 20 +「岁」
  - ＊身高 input +「CM」
  - ＊手机 input 13880942587
  - 学历「大专」 + 修改 蓝链
  - 收入「5-8千元」 + 修改 蓝链
  - 婚况「未婚」 + 修改 蓝链
  - 单位 input
  - 身份证 200x120 灰渐变占位 显示「420602199706********」
- 状态管理：useState activity/signupTab/editOpen/editRow/name/gender/age/phone(后 4 个为受控输入传给 Drawer)。
- 数据常量 `STATS` 9 项统计,`rows` 5 行 (Row 接口)。状态 tab 字符串 "全部"/"首次报名" 受控。
- 工具栏复用 finord-btn/-primary。
- `.sm-*`:activity-row(顶部)/activity-select(200px 32px)/stats-row(9 列网格)/stat(70px min-height, border-top 3px)/-value(22px 600)/-unit(11px)/-label(12px)/card/head/title/head-actions/export-card-btn/export-btn/filters(纵向)/filters-row(横向 flex wrap)/select(30px 右箭头 SVG, 100px)/radio(12px)/text-muted(12px 灰)/date(130px)/input(200px)/search-btn/table(min-width 1280px, th/td 居中,第2/9列左对齐)/row-content/-check/-id/member(横排 flex)/avatar(50x60 灰底)/-gender(底部蓝字)/-info/-name(13px 黑)/-badge(10px 红粉渐变)/-meta(11px 灰 6 行)/realname(yes 蓝/ no 灰 2 态)/phone/times(纵向)/-link(11px)/pay(纵向)/-status(绿/red todo)/-amount(红)/audit(蓝)/cell-empty(灰)/ops(纵向)/edit-btn(3px 10px)/import-btn/op-del(蓝)/`.sm-edit-*`：panel(460px)/cancel/row(-top)/label(80px)/input-wide(满宽 34px)/input-num(100px 34px)/unit/inline(横排)/options/radio/link-row(横排)/avatar(纵向)/-img(90x90 灰渐变)/-btn(线框)/idcard-img(200x120 灰渐变+身份证号)。`npx tsc --noEmit` 通过。

## 互选活动-活动管理页面
- 路由 `/mutual-selection-list`(互选活动二级菜单首项「活动管理」，导航 AdminLayout line60 已指向；原为 ListPage 通用空态页，本次重写为静态还原页+绑定「＋ 创建活动」Drawer)。复用 `AdminBreadcrumb/getBreadcrumb/.ecl-notice/-body/-ic/-text/-title/.finord-card/.finord-table-wrap/.finord-table/.finord-btn/-primary/.finord-link/.mp-switch/-knob/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body`，新增 `.ms-*`。
- 面包屑四级：首页/活动报名/互选活动/活动管理(getBreadcrumb("活动报名","活动管理"))。
- **主页面**：蓝色须知条(标题「须知」+ 5 段大段说明)→ 卡片首行 ms-head(左「活动管理」+ 右「＋ 创建活动」蓝钮 onClick 打开 Drawer)→ 筛选条 ms-filters(input 240px + 搜索蓝钮)→ 10 列表格 1 行真实数据(ID 4 6月互选开始啦 / 2026-06-05 12:03:29 / 开始:2026-06-30 08:59 结束:2026-06-30 23:59 / 活动结束 / 男生1 女生1 / 添加/查看 / mp-switch 下线 / 查看 / 操作 群发短信/编辑/复制/删除)→ 分页 ‹ 1 ›。
- 表格元素：50x60 ms-cover 粉渐变方块显示「6月 互选 开始啦」+ 13px 黑字活动名称 + 11px 灰两行时间 + 活动状态 tag(活动结束灰) + 报名人数两行(男生/女生) + mp-switch 上线 + 操作 4 行蓝链。
- **「＋ 创建活动」Drawer** (`.ms-drawer-panel` 右侧 720px)：复用 tlc 骨架，头部 ✕+标题「添加活动」+ 右侧「取消」/「确定提交」。Drawer body 含 13 个字段行：
  - ＊活动标题 input + 蓝条「活动标题参考」
  - ＊封面图片 90x90 虚线 + 「从云端素材」线框按钮 + 蓝条 900×383
  - ＊活动时间 开始日期 → 结束日期 + 蓝条
  - 选择人数 [5] 次 + 蓝条
  - 显示报名人数 [0] + 蓝条
  - 活动费用 男生/女生/VIP会员三组 [0]元 + 蓝条
  - 推广/服务红娘奖励 [0]元 + 蓝条
  - 报名要求 3 checkbox(实名/头像/3张照) + 3 列 grid(年龄/学历/收入)
  - ＊活动介绍 富文本编辑器 + 蓝条 + 红字错误提示
  - 分享标题 input + 同步标题 checkbox
  - 分享描述 textarea
  - 分享图标 90x90 虚线 + 云端素材
  - 互选成功后 radio(显示双方微信信息已加/提示联系红娘推送微信号) + 2 个效果参考 蓝链
  - 进入嘉宾互选时弹出的须知 蓝条 box + 效果参考
  - 互选成功后添加微信页面的提示 蓝条 box + 红字错误提示
  - 底部「确定提交」蓝钮
- 状态管理：useState addOpen/online/syncTitle/realname/avatar/threePhoto/wechatInfo/redNote(8 个受控状态)。
- 工具栏常量 `TOOLBAR` 20 项。列配置数组 `columns` 10 项。
- `.ms-*`：card/head/title/head-actions/create-btn/filters/input(240px)/search-btn/table(min-width 1280px, th/td 居中,第2列左对齐)/row-content/-check/-id/activity(横排)/cover(50x60 粉渐变)/-mutual(粉红渐变)/-info/-name(13px 黑)/time/-block(11px 灰 两行)/status(灰)/count(11px 灰)/-num(加粗)/ops(纵向 4 行)/pager(-arrow/-cur)/`.ms-drawer-*`：panel(720px)/cancel/row(-top)/label(96px)/content/input-wide(满宽 34px)/info(蓝条)/required-text(红字)/pick(90x90 虚线)/-square(同)/cloud-btn(线框)/icon-row(横排)/daterange(横排 wrap)/date(130px)/text-muted(12px 灰)/fee-row(横排 wrap)/num(80x30)/unit/checks-row(22px gap wrap)/checkbox(14px)/-right(ml 12px)/form-grid(3列 12px row)/form-item/required(蓝)/select-inline(120px 右箭头 SVG)/editor(220px)/editor-toolbar/editor-tool/editor-body(180px)/editor-placeholder/link-row/textarea/radio-row(12px gap wrap)/radio(14px)/note-box(蓝条)/submit-row/submit。`npx tsc --noEmit` 通过。

## 互选活动-互选记录页面
- 路由 `/mutual-selection-record`(互选活动二级菜单次项「互选记录」，导航 AdminLayout line60 已指向；原为 ListPage 通用空态页，本次重写为静态还原页)。复用 `AdminBreadcrumb/getBreadcrumb/.ecl-notice/-body/-ic/-text/-title/.finord-card/.finord-table-wrap/.finord-table/.finord-btn/-primary`，新增 `.mr-*`。
- 面包屑四级：首页/活动报名/互选活动/互选记录(getBreadcrumb("活动报名","互选记录"))。
- 结构：蓝色须知条(标题「须知」+ 2 段说明:1.记录了所有活动参与者「选择心动嘉宾」的历史记录,可以让红娘掌握客户的心动意向,为精准服务和跟进提供重要参考;2.活动结束之后,若互选都选择了对方心动嘉宾,则被视为「互选成功」,可自行添加对方微信或者由红娘介入互推微信名片)→ 卡片首行 mr-title「互选记录」+ 筛选条 mr-filters(灰底胶囊:按活动筛选▾ select 130px + 按行为方▾ select 130px + 「输入会员昵称/编号/姓名」input 240px + 搜索蓝钮 + 状态 3 个单选「不选/未成功/已成功」受控 status state)→ 6 列表格 colSpan=6 空态「📦 + 暂无数据」灰色小盒子图标。
- 表格列(6)：时间/行为方/行为动作/行为对象/活动名称/互选结果。
- 状态：当前无数据，仅保留完整表头 + 居中空态样式 `.mr-empty-inner`。
- 列配置数组 `columns` 6 项。
- `.mr-*`：card/title/filters(横向 wrap)/select(30px 右箭头 SVG, 130px)/input(240px)/search-btn/status(横向 14px gap)/-label(13px 灰)/radio(14px)/table(min-width 980px, th/td 居中)/empty(padding 60px 0! important)/empty-inner/-icon(36px 灰)/-text(灰)。`npx tsc --noEmit` 通过。

## 分店管理-分站配置页面
- 路由 `/branch-config`(分店管理二级菜单首项「分站配置」，导航 AdminLayout line57 已指向；原为 ListPage 通用空态页，本次重写为静态还原页)。复用 `AdminBreadcrumb/getBreadcrumb/.ecl-notice/-body/-ic/-text/-title/.finord-card/.finord-table-wrap/.finord-table/.finord-btn/-primary`，新增 `.bc-*`。
- 面包屑三级：首页/分店管理/分站配置(getBreadcrumb("分店管理","分站配置"))。
- 结构：蓝色须知条(标题「须知」+ 4 段大段说明:分站是按会员现居地划分/默认显示所有地区/自动跳转 IP 识别/分站模式支持全部模式和指定区域)→ **卡片 1 分站模式** bc-mode(左 label「分站模式」+ radio「全国模式/指定地区」+ 右侧「确定切换」蓝钮 margin-left auto)→ **卡片 2 分站列表** 含「+ 添加分站」灰按钮(禁用态 灰底灰字 cursor not-allowed)→ 7 列表格 colSpan=7 空态「📦 + 暂无数据」灰色小盒子图标。
- 表格列(7)：分站地区/显示名称/链接/显示排序/链接·二维码/自动跳转/操作。
- 状态：当前无数据，仅保留完整表头 + 居中空态样式 `.bc-empty-inner`。
- 状态管理：useState mode,2 个单选受控("全国模式"/"指定地区")。
- 列配置数组 `columns` 7 项。
- `.bc-*`：card/mode(横向 flex gap 22px)/mode-label(14px 黑 500)/mode-options(横向)/radio(14px)/confirm-btn(margin-left auto 22px padding)/list-head(flex-end)/add-btn(灰底禁用态)/table(min-width 1080px, th/td 居中)/empty(padding 60px 0! important)/empty-inner/-icon(36px 灰)/-text(灰)。`npx tsc --noEmit` 通过。

## 分店管理-门店管理页面
- 路由 `/mendian-list`(分店管理二级菜单次项「门店管理」，导航 AdminLayout line57 已指向；原为 ListPage 通用空态页，本次重写为静态还原页)。复用 `AdminBreadcrumb/getBreadcrumb/.ecl-notice/-body/-ic/-text/-title/.finord-card`，新增 `.md-*`。
- 面包屑三级：首页/分店管理/门店管理(getBreadcrumb("分店管理","门店管理"))。
- 结构：蓝色须知条(标题「须知」+ 4 段大段说明:多门店分店系统+3 个子项加粗标签"平台与分店间的分成与结算/分店红娘的分成与结账/分店店长权限"+分店管理红娘+2 个加粗"提示")→ 卡片 md-card 标题 md-title「门店管理」+ md-empty 居中空态「该功能需要升级为"旗舰版"后使用」灰字。
- 状态：无交互(静态空态),无需 useState。
- `.md-*`：card/title(empty 14px 600 25272d)/empty(320px min-height flex 居中 60px 0 padding)/empty-text(14px 灰)。`npx tsc --noEmit` 通过。

## 分店管理-分店红娘页面
- 路由 `/branch-matchmaker-list`(分店管理二级菜单第三项「分店红娘」，导航 AdminLayout line57 已指向；原为 ListPage 通用空态页，本次重写为静态还原页+绑定「＋ 添加红娘」Drawer)。复用 `AdminBreadcrumb/getBreadcrumb/.ecl-notice/-body/-ic/-text/-title/.finord-card/.finord-table-wrap/.finord-table/.finord-btn/-primary/.finord-link/.mp-switch/-knob/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body`，新增 `.bm-*`。
- 面包屑三级：首页/分店管理/分店红娘(getBreadcrumb("分店管理","分店红娘"))。
- **主页面**：蓝色须知条(标题「须知」+ 4 段说明:加粗标签"分店 店长红娘/分店 普通红娘"+ 1 段普通说明+ 加粗"注意"标签)→ 卡片首行 bm-head(左「红娘管理」+ 右「＋ 添加红娘」蓝钮 onClick 打开 Drawer)→ 筛选条 bm-filters(全部门店▾ select 110px + 请输入红娘称呼/手机 input 240px + 搜索蓝钮)→ 7 列表格 colSpan=7 空态「📦 + 暂无数据」灰色小盒子图标。
- 表格列(7)：红娘/手机·微信/隶属门店/锁定/前台展示/菜单权限/操作。
- **「＋ 添加红娘」Drawer** (`.bm-drawer-panel` 右侧 540px)：复用 tlc 骨架，头部 ✕+标题「添加/编辑服务红娘」+ 右侧「关闭」/「确定提交」。
  - ＊账号绑定 input placeholder「请输入已注册用户的昵称」+ radio「按昵称(默认)/按手机」+ 蓝条「① 如果查询不到账号...」
  - 红娘头像 + 微信二维码 横排(各 90x90「+ 上传图片」虚线)
  - ＊红娘称呼 input「请输入红娘称呼」+ 岗位描述 input「如:电话邀约、匹配牵线」(横排)
  - ＊红娘口号 select「请选择」+ 「自定义输入」蓝链(横排)
  - ＊微信号 input「请输入微信号」+ ＊手机号 input「请输入手机号」(横排)
  - ＊红娘角色 select「请选择红娘角色」
  - 分成级别 radio「不参与(默认)/允许」+ 蓝条「① 分店的红娘分成将由分店自行在分店平台中设置与结算」
  - 修改联系方式 radio「不允许(默认)/允许」+ 蓝条「① 若设置为"不允许"则红娘在其红娘平台中无法修改客户的手机号码和微信(包括客源线索、会员CRM)」
  - 定时锁定 mp-switch「关闭(默认)」+ 蓝条「① 开启定时锁定后,到了时间后该账号自动锁定」
  - 排序值 input + 蓝条「数字越大显示越靠前」
- 状态管理：useState addOpen/accountMode/commission/editContact/timedLock(5 个受控状态)。
- 列配置数组 `columns` 7 项。
- `.bm-*`：card/head/title/head-actions/add-btn/filters/select(30px 右箭头 SVG, 110px)/input(240px)/search-btn/table(min-width 1080px, th/td 居中)/empty(padding 60px 0! important)/empty-inner/-icon(36px 灰)/-text(灰)/`.bm-drawer-*`：panel(540px)/cancel/row(-top)/label(96px)/content/input-wide(满宽 34px)/-num(100px 34px)/acct-row(横排 flex)/radio(14px)/info(蓝条)/pick-row(横排 gap 14px)/pick(纵向)/-btn(90x90 虚线)/two-col(横排 gap 10px)/-wide(flex 1)/-select-wide(select)/radio-row(22px gap)/switch-row(gap 10px)。`npx tsc --noEmit` 通过。

## 分店管理-分店报表页面
- 路由 `/branch-report-list`(分店管理二级菜单第四项「分店报表」，导航 AdminLayout line57 已指向；原为 ListPage 通用空态页，本次重写为静态还原页)。复用 `AdminBreadcrumb/getBreadcrumb/.ecl-notice/-body/-ic/-text/-title/.finord-card/.finord-table-wrap/.finord-table`，新增 `.br-*`。
- 面包屑三级：首页/分店管理/分店报表(getBreadcrumb("分店管理","分店报表"))。
- 结构：蓝色须知条(标题「须知」+「以下是根据当前分店下所有红娘名下的客源、会员所统计的数据」)→ **9 个统计卡片 br-stats-row 9 列网格**:
  - 第 1 个特殊卡片 br-stat-special(蓝底「暂无门店」+「切换分店」蓝边白底按钮链接)
  - 0份 客源线索 / 0人 会员资料 / 0次 成功线上牵线 / 0人 线上VIP / 0次 线下VIP
  - 0元 成功安排见面(右上「排名 0」红字)/ 0元 线上分成(右上「排名 0」红字)/ 0元 线下业绩(右上「排名 0」红字)
  → 10 列表格 colSpan=10 空态「📦 + 暂无数据」灰色小盒子图标。
- 表格列(10)：月份/新增男会员/新增女会员/新增客源线索/新增线上VIP会员/新增牵线申请/新增线下约会/新增线下VIP会员/线上分成/线下业绩。
- 状态：当前无数据,仅保留完整表头+居中空态样式。
- 数据常量 `STATS` 9 项,首项 special 标记。
- `.br-*`:stats-row(9列网格)/stat(70px min-height)/-value(22px 600)/-unit(11px)/-label(12px)/-rank(右上 10px 红字 absolute)/-special(蓝底)/-special-link(蓝边白底)/card/table(min-width 1280px, th/td 居中)/empty(padding 60px 0! important)/empty-inner/-icon(36px 灰)/-text(灰)。`npx tsc --noEmit` 通过。

## 分店管理-分成明细页面
- 路由 `/branch-distribution-list`(分店管理二级菜单第五项「分成明细」，导航 AdminLayout line57 已指向；原为 ListPage 通用空态页，本次重写为静态还原页)。复用 `AdminBreadcrumb/getBreadcrumb/.ecl-notice/-body/-ic/-text/-title/.finord-card/.finord-table-wrap/.finord-table/.finord-btn/-primary`，新增 `.bd-*`。
- 面包屑三级：首页/分店管理/分成明细(getBreadcrumb("分店管理","分成明细"))。
- 结构：**5 卡片统计行 bd-stats-row(1.2fr repeat(4, 1fr) 网格)**:
  - 第 1 个特殊卡片 bd-stat-special(横向 flex 左对齐:🏪 + 蓝字「暂无门店」+ 右侧 bd-stat-switch-btn「切换分店」白底灰字线框按钮)
  - 4 个红字「¥0」+ 灰字(累计分得/本月分成/上月分成/待结算余额)
  → 蓝色须知条(标题「须知」+ 3 段:报表说明+分店分成+实时提现)→ 卡片首行 bd-head(左「线上分成明细」+ 右「⬇ 导出Excel(当前分店)」蓝钮)→ 筛选条 bd-filters(请选择红娘▾ select 130px + 请选择事件▾ select 130px + 开始日期 → 结束日期 dateRange + 搜索蓝钮)→ 8 列表格 colSpan=8 空态「📦 + 暂无数据」灰色小盒子图标。
- 表格列(8)：ID/时间/分店名称/红娘/消费会员/分成·奖励事件/消费金额/分成金额。
- 状态：当前无数据,仅保留完整表头+居中空态样式。
- 数据常量 `STATS` 4 项。
- `.bd-*`:stats-row(1.2fr+4fr 网格)/stat(70px min-height)/-value(22px 600 红字 #e6483d)/-num(同规格)/-label(12px 灰)/-special(横向 flex 左对齐)/-header(横排 gap 8px)/-special-icon(18px)/-special-text(14px 蓝 500)/-switch-btn(26px 白底灰字线框 hover 蓝)/card/head/title/export-btn(7px 18px)/filters(横向 wrap)/select(30px 右箭头 SVG, 130px)/daterange(横排)/text-muted(12px 灰)/date(130px)/search-btn/table(min-width 1180px, th/td 居中)/empty(padding 60px 0! important)/empty-inner/-icon(36px 灰)/-text(灰)。`npx tsc --noEmit` 通过。

## 合伙红娘-功能配置页面
- 路由 `/love-partner-config`(合伙红娘二级菜单首项「功能配置」，导航 AdminLayout line59 已指向；原为 ListPage 通用空态页，本次重写为静态还原页)。复用 `AdminBreadcrumb/getBreadcrumb/.ecl-notice/-body/-ic/-text/-title/.finord-card/.finord-btn/-primary`，新增 `.lp-*`。
- 面包屑三级：首页/合伙红娘/功能配置(getBreadcrumb("合伙红娘","功能配置"))。
- 结构：卡片 lp-card 标题 lp-title「功能配置」+ 蓝色须知条 lp-notice(标题「合伙人介绍」+ 6 段大段说明+ 1 个加粗"特别提醒")→ ＊合伙人介绍文案 + lp-editor 富文本编辑器(toolbar 21 个 lucide 图标)+ contentEditable body 内嵌 lp-banner(粉紫渐变 135deg 圆角 12px 含紫粉渐变文字"寻婚恋事业合伙人" + 白色半透药丸"携手共创,共享婚恋市场蓝海!" + 右侧 lp-banner-image 110x110 粉红径向渐变圆形)+ 底部 lp-editor-text 段落"作为婚恋服务行业的佼佼者...赢得了市场的广泛赞誉。"→ 当合伙人同时是自己团队中的推广红娘时 lp-row lp-row-top(标签 130px + lp-content 含 lp-options-label 灰小字+ 2 个 lp-radio「享有(默认)/不享有」+ lp-submit-row「确定提交」蓝钮)。
- 状态管理：useState shareBonus,1 个 radio 单选受控。
- 工具栏常量 `TOOLBAR` 21 项(包含 lucide-react 的 H/Tt/B/I/U/S/左中右对齐/无序/有序/链接/图片/表情/代码/引用/分割线/撤销/重做/全屏/标题2)。
- `.lp-*`：card(18 26 24)/title/notice(margin-bottom 16px)/row(-top)/label(130px)/content/editor(320px)/editor-toolbar/editor-tool/editor-body(280px)/editor-text(margin 12px 0 0)/banner(粉紫渐变 135deg 圆角 12px padding 18px 24px min-height 180px flex 纵向 gap 8px 左对齐)/-title(22px 700 紫粉渐变 background-clip text)/-pill(白色半透 rgba 0.7 紫字 14px 18px padding 6px 18px 圆角 18px)/-image(absolute 右上 110x110 粉红径向渐变 圆 50%)/options(横排 gap 14px wrap)/-label(13px 灰)/radio(14px)/submit-row/submit。`npx tsc --noEmit` 通过。

## 合伙红娘-分成配置页面
- 路由 `/love-partner-bonus-config`(合伙红娘二级菜单次项「分成配置」，导航 AdminLayout line59 已指向；原为 ListPage 通用空态页，本次重写为静态还原页+绑定「编排配置」Drawer)。复用 `AdminBreadcrumb/getBreadcrumb/.finord-card/.finord-table-wrap/.finord-table/.finord-link/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body/.finord-btn/-primary`，新增 `.lpb-*`。
- 面包屑三级：首页/合伙红娘/分成配置(getBreadcrumb("合伙红娘","分成配置"))。
- **主页面**：卡片 lpb-card 标题 lpb-title「合伙人级别分成配置」+ 7 列表格 3 行真实数据(ID 1-3 级别1-3 初级/中级/战略合伙人 + 按比例自动计算 35/40/45% + 数量 1/0/0 + 自动升级条件 默认/累计业绩>=10000元且有效相亲会员>=100人/累计业绩>=30000元且有效会员>=500人)+ 「编排配置」蓝链点击打开 Drawer。
- 表格列(7)：ID/分成级别/级别名称/分成模式/合伙人数量/自动升级条件/操作。
- **「编排配置」Drawer** (`.lpb-drawer-panel` 右侧 720px)：复用 tlc 骨架，头部 ✕+标题「编辑配置」+ 右侧「关闭」/「确定提交」。
  - ＊分成级别名称 input default「初级合伙人」+ 蓝条「① 不要超过4个汉字」
  - ＊会员注册奖励 横排「男会员[1.00]元/人 女会员[1.00]元/人」+ 蓝条
  - ＊推广红娘纳入分成 [0.00]元/人 + 蓝条
  - ＊会员消费分成 radio「不分成(默认)/给予分成」+ 蓝条
  - 分成模式 radio「自定义固定金额(默认)/按比例自动计算」+ [35]% input + 「更新数据」蓝钮 + 蓝条
  - 分成金额 3×N 网格 lpb-bonus-grid 22 项(资料审核费/推广展示/资料置顶套餐1-6/牵线套餐1-7+9-10/单次牵线服务/爆灯/新人专享/心动专享/臻爱专享/牵线套餐5)每项「名称 input[0.00]元」+ 底部「① 修改比例后下次开方可查看金额变化」蓝条 margin-top 12px
- 状态管理：useState editOpen/consumeMode/splitMode,2 个 radio 单选受控。
- 数据常量 `levels` 3 项真实数据,`BONUS_ITEMS` 22 项套餐。
- `.lpb-*`：card/title/table(min-width 1180px, th/td 居中,第6/7列左对齐)/`.lpb-drawer-*`：panel(720px)/head-actions/cancel/row(-top)/label(110px)/content/input-wide(满宽 34px)/info(蓝条)/-block(margin-top 12px)/fee-row(横排 wrap)/num(90x30)/unit(13px 灰)/radio-row(22px gap)/radio(14px)/mode-row(12px gap wrap)/update-btn(5px 14px)/bonus-grid(3列 10px row 14px col)/bonus-item(flex)/bonus-name(12px 灰 min-width 90px)。`npx tsc --noEmit` 通过。

## 合伙红娘-合伙人管理页面
- 路由 `/love-partner-list`(合伙红娘二级菜单第三项「合伙人管理」，导航 AdminLayout line59 已指向；原为 ListPage 通用空态页，本次重写为静态还原页+绑定「＋ 添加合伙人」Drawer)。复用 `AdminBreadcrumb/getBreadcrumb/.ecl-notice/-body/-ic/-text/-title/.finord-card/.finord-table-wrap/.finord-table/.finord-btn/-primary/.finord-link/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body`，新增 `.lpl-*`。
- 面包屑三级：首页/合伙红娘/合伙人管理(getBreadcrumb("合伙红娘","合伙人管理"))。
- **主页面**：蓝色须知条(标题「须知」+ 8 段说明:合伙人定义/团队业绩+分佣标准/会员消费分成/独立管理中心/推广红娘与合伙人并存/付费与人工两种开通形式/推广红娘注册申请流程/独立邀请二维码 + 加粗定义「团队成员/红娘业绩/团队有效会员/累积分成」4 项)→ 卡片首行 lpl-head(左「合伙人管理」+ 右「＋ 添加合伙人」蓝钮 onClick 打开 Drawer)→ 9 列表格 1 行真实数据(ID 2 出现1/富婆爱1/初级合伙人/2026-06-28 14:30:17/5人 名单蓝链/0元红字/2（男女2）/2元 [明细]蓝链/操作 合伙人中心+编辑+删除)→ 分页 ‹ 1 ›。
- 表格列(9)：ID/合伙人/合伙级别/创建时间/团队成员/团队业绩/团队有效会员/累积分成/操作。
- 表格元素：lpl-avatar 36x36 灰方块「?」+ 姓名 2 行(账号+团队) + lpl-level 绿标签(初级合伙人 12px 绿字淡绿底)/团队成员「5人 + 名单」蓝链/团队业绩「0元」红字/累积分成「2元 + [明细]」红字+蓝链/操作 3 行横排 蓝链(合伙人中心+编辑+删除)。
- **「＋ 添加合伙人」Drawer** (`.lpl-drawer-panel` 右侧 540px)：复用 tlc 骨架，头部 ✕+标题「添加/编辑合伙人」+ 右侧「关闭」/「确定提交」。
  - 蓝色须知条 5 段简版(无标题 lpl-drawer-notice):1.合伙人需先在平台上注册账号/2.推广红娘可同时成为合伙人/3.归属团队后默认关系保留/4.无归属团队自动加入/5.一个推广红娘账号只允许隶属一个合伙团队
  - ＊用户账号 input「请输入账号昵称」+ 蓝条「① 服务红娘不能成为合伙人」
  - ＊团队名称 input「请输入团队名称」+ 蓝条「① 请输入合伙人团队的名称,如:校园红娘小队」
  - ＊分成级别 radio 初级合伙人(默认)/中级合伙人/战略合伙人 + 蓝条「① 不同级别的享有不同的分成标准,请在分级配置中自行设定相关标准」
- 状态管理：useState addOpen/level,1 个 radio 单选受控(3 选项)。
- 数据数组 `partners` 1 行真实数据。
- `.lpl-*`：card/head/title/head-actions/add-btn/filters/table(min-width 1280px, th/td 居中,第2列左对齐)/partner(flex)/avatar(36x36 灰方块)/-info(左对齐)/team(灰)/level(绿标签)/time/performance(红)/bonus(红)/-link(ml 4px)/ops(gap 8px)/op-del(蓝)/pager(-arrow/-cur)/`.lpl-drawer-*`：panel(540px)/cancel/notice(margin-bottom 18px)/notice-text p(12px 行高 1.8)/row/label(96px)/content/input-wide(满宽 34px)/info(蓝条)/radio-row(22px gap wrap)/radio(14px)。`npx tsc --noEmit` 通过。

## 合伙红娘-团队关系页面
- 路由 `/love-partner-relation`(合伙红娘二级菜单第四项「团队关系」，导航 AdminLayout line59 已指向；原为 ListPage 通用空态页，本次重写为静态还原页+绑定「人工绑定团队关系」Drawer)。复用 `AdminBreadcrumb/getBreadcrumb/.ecl-notice/-body/-ic/-text/-title/.finord-card/.finord-table-wrap/.finord-table/.finord-btn/-primary/.finord-link/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body`，新增 `.lpr-*`。
- 面包屑三级：首页/合伙红娘/团队关系(getBreadcrumb("合伙红娘","团队关系"))。
- **主页面**：蓝色须知条(标题「须知」+ 4 段 4 种加入团队方式说明 + 1 段加粗"每个推广红娘只可以加入1个合伙人团队"+ 1 段非正常状态说明)→ 卡片首行 lpr-head(左「合伙人团队关系」+ 右「人工绑定团队关系」蓝钮 onClick 打开 Drawer)→ 筛选条 lpr-filters(按隶属合伙人搜▾ select 130px + 请输入 input 200px + 搜索蓝钮)→ 8 列表格 5 行真实数据(ID 6 lemon/富豪爱1/2026-08-23 21:49:21/0/0元/正常/移出团队+变更团队;ID 5 是胖春本人没错/2026-07-11 16:39:26/0/0元/正常;ID 4 越可名/2026-07-11 16:39:14/0/0元/正常;ID 3 Sofia/2026-07-05 14:53:18/2/0元/正常;ID 1 σ^^ η'/2026-06-30 16:17:55/0/0元/正常)→ 分页 ‹ 1 ›。
- 表格列(8)：ID/推广红娘/隶属合伙人(团队)/加入团队时间/发展会员数量/团队业绩贡献/团队关系状态/操作。
- 表格元素：lpr-time 灰、lpr-performance 红字(0元)、lpr-status 绿字(正常)、操作 2 行蓝链(移出团队+变更团队)。
- **「人工绑定团队关系」Drawer** (`.lpr-drawer-panel` 右侧 460px)：复用 tlc 骨架，头部 ✕+标题「人工绑定团队关系」+ 右侧「关闭」/「确定提交」。
  - 蓝条提示 lpr-warn「① 若推广红娘已经隶属于其他合伙人团队,本操作会将其从原有团队中移除并绑定到您指定的团队中」
  - ＊推广红娘 input placeholder「请输入推广红娘的账号昵称」+ 蓝条「① 请填写推广红娘的账号昵称」
  - ＊绑定到 select placeholder「请选择一个团队」
- 状态管理：useState bindOpen(无受控 radio)。
- 数据数组 `relations` 5 行真实数据。
- `.lpr-*`：card/head/title/head-actions/bind-btn/filters/select(30px 右箭头 SVG, 130px)/input(200px)/search-btn/table(min-width 1180px, th/td 居中)/time(灰)/performance(红)/status(绿)/ops(gap 8px)/pager(-arrow/-cur)/`.lpr-drawer-*`：panel(460px)/cancel/warn(蓝条)/row/label(80px)/content/input-wide(满宽 34px)/info(蓝条)/select-wide(满宽 34px 右箭头 SVG)。`npx tsc --noEmit` 通过。

## 合伙红娘-分成明细页面
- 路由 `/love-partner-bonus-details`(合伙红娘二级菜单第五项「分成明细」，导航 AdminLayout line59 已指向；原为 ListPage 通用空态页，本次重写为静态还原页+绑定「＋ 录入一笔分成」Drawer)。复用 `AdminBreadcrumb/getBreadcrumb/.finord-card/.finord-table-wrap/.finord-table/.finord-btn/-primary/.tlc-mask/.tlc-panel/.tlc-panel-head/-head-left/-x/-title/.tlc-panel-body`，新增 `.lpbd-*`。
- 面包屑三级：首页/合伙红娘/分成明细(getBreadcrumb("合伙红娘","分成明细"))。
- **主页面**：卡片首行 lpbd-head(左「合伙人(红娘)分成明细」+ 右「＋ 录入一笔分成」蓝钮 onClick 打开 Drawer)→ 筛选条 lpbd-filters(请选择事件▾ select 130px + 开始日期 → 结束日期 dateRange + 请输入合伙人昵称 input 200px + 搜索蓝钮)→ 7 列表格 2 行真实数据(ID 2 2026-07-15 14:36:24 富豪爱1/Sofia/会员注册奖励 蓝标签/相亲会员Rasim(Q847150)-女-会员注册奖励/1元红字;ID 1 2026-07-11 11:29:57 富豪爱1/Sofia/会员注册奖励/相亲会员Thera(Q824771)-女-会员注册奖励/1元)→ 分页 ‹ 1 ›。
- 表格列(7)：ID/时间/合伙人/团队推广红娘/分成类型/分成事件/分成金额。
- 表格元素：lpbd-time 灰、lpbd-type 蓝标签(会员注册奖励)、lpbd-event 灰、lpbd-amount 红字 500(1元)。
- **「录入一笔分成」Drawer** (`.lpbd-drawer-panel` 右侧 540px)：复用 tlc 骨架，头部 ✕+标题「录入一笔分成」+ 右侧「确定提交」蓝钮。
  - lpbd-info-block 蓝条「① 添加记录后会自动生成分成明细、余额明细,分成金额会计入到红娘账号余额中」
  - ＊合伙人红娘 input「请输入合伙人红娘名称」
  - ＊购买账号 input「请输入购买账号昵称」
  - ＊分成事件 select「请选择分成事件」
  - ＊消费事件 select「请选择消费事件」
  - ＊分成金额 lpbd-amount-row(横排 input + 元 + 「获取验证码」蓝钮)
  - ＊短信验证码 input + lpbd-info 蓝条「① 短信将发送至admin绑定的手机号」
  - lpbd-submit-row「确定提交」蓝钮
- 状态管理：useState addOpen(无受控 radio,仅控制 Drawer 开关)。
- 数据数组 `details` 2 行真实数据。
- `.lpbd-*`：card/head/title/head-actions/add-btn/filters/select(30px 右箭头 SVG, 130px)/daterange(横排)/text-muted(12px 灰)/date(130px)/input(200px)/search-btn/table(min-width 1280px, th/td 居中)/time(灰)/type(蓝标签)/event(灰)/amount(红 500)/pager(-arrow/-cur)/`.lpbd-drawer-*`：panel(540px)/info-block(蓝条)/row/label(96px)/content/input-wide(满宽 34px)/info(蓝条)/select-wide(满宽 34px 右箭头 SVG)/amount-row(横排)/num(100x34)/unit/get-code-btn(7px 14px)/submit-row(右对齐)/submit(8px 22px)。`npx tsc --noEmit` 通过。

## 账号管理-添加账号按钮 Drawer
- 路由 `/reg-user-all`(平台账号二级菜单首项「账号管理」，导航 AdminLayout line52 已指向；原页面使用 Tailwind CSS 实现居中 Modal 弹窗，本次为「添加账号」按钮新增独立的右侧 Drawer 弹窗，遵循截图样式)。复用现有 Tailwind 风格，新增 state + 提交函数 + 独立 Drawer 渲染块。
- 修改 `src/app/(admin)/reg-user-all/page.tsx`：
  - import 引入 `Eye, EyeOff, lucide-react`
  - 新增 `AddForm` 类型 + `initialAddForm` 常量：{ nickname: string; phone: string; password: string }
  - 新增 state：`addOpen`、`addForm`、`showAddPwd`、`addSaving`
  - 新增 `addValid` 检查：昵称+手机非空+密码≥8位
  - 新增 `submitAdd` 函数：调用 adminEndpoints.createAdminAccount，传入 phone 作为 username、nickname 作为 display_name，其余字段传默认值（SELF/null/[]）
  - 新增 `closeAdd` 函数：关闭并重置表单
  - 「添加账号」按钮 onClick 改为 `setAddOpen(true)`（原为 setDialog("create")）
- **新增 Drawer 渲染块** (`.reg-add-drawer-panel` 右侧 460px，全屏 mask)：
  - 头：✕ + 标题「添加账号」+ 右侧「取消」（outline 灰边按钮）+「确定提交」（primary 蓝钮，加载中文案变为「提交中…」）
  - body 字段（4 个）：
    - ＊昵称 input placeholder「3-28字符，最多14个汉字」+ maxLength={28}
    - ＊手机 input placeholder「请输入手机号」
    - ＊密码 input type=password/showAddPwd 切换 + Eye/EyeOff lucide 图标切换显示 + placeholder「请输入密码（至少8位）」
    - 头像：两个线框按钮「↑ 上传图片」「🖼 从云端素材选择」
- - mask 点击关闭 Drawer（onClick={closeAdd}），panel 上 stopPropagation 阻止冒泡。
- 修改 `src/app/globals.css` 末尾：
  - 新增 `@keyframes reg-slide-in { from { transform: translateX(100%); } to { transform: translateX(0); } }`
  - 新增 `.reg-add-drawer-panel { animation: reg-slide-in 0.25s ease-out; }`
- TS1005 问题：`<span aria>↑</span>` 中无效属性 `aria`，用 python 脚本替换为 `<span>↑</span>` / `<span>🖼</span>`。
- `npx tsc --noEmit` 通过。

## 客源线索-线索管理「智能录入」按钮 Drawer
- 路由 `/love-customer-list`(客源线索二级菜单首项「线索管理」，导航 AdminLayout line53 已指向)。原「▣ 智能录入」按钮（line285）无 onClick 绑定，本次新增右侧 Drawer 弹窗。复用 Tailwind 风格 + 新增独立 globals.css 前缀。
- 修改 `src/app/(admin)/love-customer-list/page.tsx`：
  - 新增 state：`smartOpen`
  - 「▣ 智能录入」按钮 onClick 改为 `setSmartOpen(true)`
  - 在 modal 渲染块之前新增 smartOpen Drawer 渲染块
- **Drawer 结构（右侧 720px，全屏 mask 点击关闭）**：
  - 头：✕ + 标题「智能录入」+ 右上「取消」「确定提交」两个按钮
  - body：
    - 蓝色须知条「💡 须知」+ 说明「腾讯AI智能体+Deepseek大模型 实现信息识别...」
    - 2 个 radio「文字识别录入 参考模版」「图片识别录入 参考模版」
    - textarea placeholder「复制粘贴到这里」+ 「✦ 开始智能填写到下面信息中」蓝钮
    - ☑ checkbox「将本内容自动存储到「原始登记信息存储」字段中」(默认勾选)
    - 「基本资料」标题 + 12 个标签 chips（高颜值/高收入/985毕业/211毕业/事业单位/双一流/海归/身材好/博士/央企企/银行金融/公务员）+ 「标签管理」蓝链
    - 10 个来源 chips（抖音/微信群/本地群/身边朋友/同行群/朋友圈/朋友介绍/QQ/其他渠道/落地页，「微信群」默认选中，蓝色态）+「管理来源」蓝链
    - 3 列字段网格（18 个字段）：手机/昵称(含自动生成按钮)/姓名/性别(radio 男/女)/生日(date)/属相(select)/身高(cm)/体重(kg)/婚况/家乡/现居/学历/职业/收入/购房/购车/吸烟/喝酒
- 状态管理：useState smartOpen(无受控 radio,仅控制 Drawer 开关)。
- 新增 `src/app/globals.css` 前缀 `.smart-*`:
  - `@keyframes smart-slide-in` + `.smart-drawer-panel` 滑入动画
  - drawer mask/panel/head/head-x/head-actions/cancel/submit/body/notice(-title)/radio-row/radio/radio-link/textarea/ai-btn/check-row/section-title/chips/chip(.active)/chip-mgr/grid/field(-label/-with-btn)/auto-btn/input-with-unit/input-unit/radio-inline
- 头像、昵称字段用 input-with-btn 横排(自动生成)；身高/体重用 input-with-unit 横排(cm/kg 单位)。
- `npx tsc --noEmit` 通过。

## 客源线索-线索管理「数据报表」按钮 Drawer
- 路由 `/love-customer-list`(客源线索二级菜单首项「线索管理」)。原「数据报表」按钮（line287）为 `<Link href="/love-customer-statistics">` 跳转链接，本次改为 onClick 触发右侧 Drawer 弹窗，遵循截图样式。
- 修改 `src/app/(admin)/love-customer-list/page.tsx`：
  - 新增 state：`statsOpen`、`statsTab`(7 个 tab 受控)
  - 「数据报表」按钮 onClick 改为 `setStatsOpen(true)`（原为 Link 跳转）
  - 在 smartOpen Drawer 之前新增 statsOpen Drawer 渲染块
- **Drawer 结构（右侧 760px，全屏 mask 点击关闭，slide-in 动画）**：
  - 头：✕ + 标题「数据报表」+ 右上「关闭」按钮
  - body：
    - 蓝色须知条「💡 须知」+ 1 段说明「为您统计了线索库中运营人员（推广红娘）的获客、销售红娘跟进、分派、客户意向等数据」
    - 7 个 tab（蓝下划线切换）：客户跟进统计(默认激活) / 客户意向统计 / 客户来源统计 / 客源状态统计 / 客户增量统计 / 客源分流统计 / 推广红娘获客统计
    - 「客户跟进统计」表格：表头 9 列（红娘/名下客源/从未跟进/超3天未跟进/超7天未跟进/超15天未跟进/超30天未跟进/跟进总数/本月跟进次数），1 行真实数据（芸希老师 / 1 / 1 / 0 / 0 / 0 / 0 / 0条 / 0条）
    - 其他 tab 简化显示通用 2 列占位（红娘/数据 0）
- 状态管理：useState statsOpen + statsTab(7 个 enum 受控)。
- 新增 `src/app/globals.css` 前缀 `.stats-*`:
  - `@keyframes stats-slide-in` + `.stats-drawer-panel` 滑入动画
  - drawer mask/panel/head/head-x/close/body/notice(-title)/tabs/tab(.active 下划线)/th(-center/-right)/td(-center/-right)
- `npx tsc --noEmit` 通过。

## 客源线索-线索管理「添加客源」按钮悬浮下拉菜单
- 路由 `/love-customer-list`(客源线索二级菜单首项「线索管理」)。原「+ 添加客源」按钮（line281-287）为单一触发 `open("create")` 的按钮，本次改为悬浮下拉容器，鼠标悬浮显示 3 个菜单项（单条录入/10条快录/批量导入）。
- 修改 `src/app/(admin)/love-customer-list/page.tsx`：
  - 新增 state：`addMenuOpen`
  - 把按钮包入 `<div className="lead-add-wrap">` + `onMouseEnter/onMouseLeave` 控制悬浮状态
  - 下拉菜单 `lead-add-menu` 含 3 个 `.lead-add-menu-item` 按钮：「单条录入」点击后调用 `open("create")` + `setAddMenuOpen(false)`，「10条快录」「批量导入」点击后关闭菜单
- 新增 `src/app/globals.css` 前缀 `.lead-add-*`：
  - `.lead-add-wrap` (relative + inline-block)
  - `.lead-add-menu` (absolute top calc(100%+6px) + 圆角 + 阴影 z-index 30 + 顶部小三角箭头::before)
  - `.lead-add-menu-item` (居中蓝字 hover 浅蓝底)
- `npx tsc --noEmit` 通过。

## 客源线索-线索管理「单条录入」全屏 Drawer
- 路由 `/love-customer-list`(客源线索二级菜单首项「线索管理」)。用户点击「+ 添加客源」按钮悬浮下拉菜单中的「单条录入」时，弹出截图样式的右侧全屏 Drawer，标题为「添加客源线索」。
- 修改 `src/app/(admin)/love-customer-list/page.tsx`：
  - 新增 state：`createOpen`
  - 「+ 添加客源」按钮 onClick 改为 `setCreateOpen(true)`（原为 open("create")）
  - 下拉菜单「单条录入」onClick 改为 `setCreateOpen(true) + setAddMenuOpen(false)`（原为 open("create")）
  - 在 statsOpen Drawer 之前新增 createOpen Drawer 渲染块
- **全屏 Drawer 结构（右侧 920px max-width 92vw）**：
  - 头：✕ + 标题「添加客源线索」+ 右上无按钮
  - body：
    - 来源 chips（10 项 + 管理来源蓝链，「抖音」默认选中蓝色态）
    - 标签 chips（12 项 + 管理标签蓝链）
    - 3 列字段网格（11 行 32 个字段）：
      - 手机/微信/昵称(含自动生成按钮)
      - 性别(radio 男/女,男默认选中)/姓名/出生(date 1990-01-01)
      - 婚况/职业/收入
      - 身高(cm 单位)/体重(kg 单位)/学历
      - 家乡(江苏省/南京市)/现居(江苏省/南京市)/购房
      - 购车/吸烟/喝酒
      - 民族/学校(含⊕选择按钮)/单位(含⊕选择按钮)
    - 描述 textarea(限500字)
    - 蓝色须知条「● 本内容仅红娘和后台可见，在入库到"会员CRM"后，显示在该客户的"会员资料-自我介绍"页中的"客源描述"中」
    - 照片：上传图片占位框 96×96
    - 分派 radio 不分派(默认)/智能分派
    - 确定提交 蓝钮
- 状态管理：useState createOpen(无受控 radio,仅控制 Drawer 开关)。
- 新增 `src/app/globals.css` 前缀 `.lc-create-*` 30 余类:
  - `@keyframes lc-create-slide-in` + `.lc-create-panel` 滑入动画
  - drawer mask/panel/head/head-x/body/form/row(-2)/chips/chip(.active)/chip-mgr/field(-label/-with-btn)/auto-btn/input-with-unit/input-unit/radio-inline/notice(-dot)/upload/assign/submit-row/submit
- `npx tsc --noEmit` 通过。

## 客源线索-线索管理「10条快录」全屏 Drawer
- 路由 `/love-customer-list`(客源线索二级菜单首项「线索管理」)。用户点击「+ 添加客源」按钮悬浮下拉菜单中的「10条快录」时，弹出截图样式的右侧全屏 Drawer，标题为「极速录入」。
- 修改 `src/app/(admin)/love-customer-list/page.tsx`：
  - 新增 state：`quickOpen`
  - 下拉菜单「10条快录」onClick 改为 `setQuickOpen(true) + setAddMenuOpen(false)`
  - 在 createOpen Drawer 之前新增 quickOpen Drawer 渲染块
- **全屏 Drawer 结构（右侧 920px max-width 92vw）**：
  - 头：✕ + 标题「极速录入」+ 右上「取消」「确定提交」两按钮
  - body：
    - 蓝色须知条「💡 须知」+ 「可以一次性极速录入10条客源线索」
    - 1 行字段组（横向 flex wrap）：
      - 称呼 input(100px, 请输入)
      - 自动生成 虚线灰按钮
      - 性别 radio (男/女,男默认选中)
      - 手机 input(170px, 灰底 placeholder「手机和微信至少填写1个」)
      - 微信 input(170px, 灰底 placeholder「手机和微信至少填写1个」)
      - 描述 input(130px, 请输入描述)
      - 线索来源 select(120px, 5 项抖音/微信群/本地群/朋友圈/落地页)
      - 删除 蓝按钮
    - 「+ 再加一条」虚线灰底线框按钮
- 状态管理：useState quickOpen(无受控 radio,仅控制 Drawer 开关)。
- 新增 `src/app/globals.css` 前缀 `.lc-quick-*` 30 余类:
  - `@keyframes lc-quick-slide-in` + `.lc-quick-panel` 滑入动画
  - drawer mask/panel/head/head-actions/cancel/submit(:disabled 灰态)/body/notice(-title)
  - row/cell(-label)/input(.w-name/.w-desc/.w-mobile/.w-wechat)/select(.w-source 满宽下拉 SVG)
  - auto(虚线灰底 hover 蓝)/gender/del(红字)/add(虚线灰底 hover 蓝)
- `npx tsc --noEmit` 通过。

## 客源线索-线索管理「批量导入」跳转新页面
- 用户点击「+ 添加客源」按钮悬浮下拉菜单中的「批量导入」时，跳转到「客源批量导入」全屏页面（独立路由 `/love-customer-batch-import`，原页面不存在，本次新建）。
- 新建 `src/app/(admin)/love-customer-batch-import/page.tsx`：
  - 引入 `useRouter` (next/navigation) + `useState` + `AdminBreadcrumb` + `getBreadcrumb`
  - 状态管理：`auditStatus` (active/pending 受控) + `dupMode` (skip/append 受控)
  - 面包屑三级：首页/客源线索/客源批量导入
- **页面布局（白色卡片 padding 28px 32px）**：
  - 标题行「开始您的批量导入」+ 右上「返回上一页」蓝链 (onClick router.push("/love-customer-list"))
  - 介绍文字「本应用可以帮助您将客源信息快速批量导入到客源线索中」
  - 4 步骤纵向布局（每步骤带蓝色圆圈数字图标）：
    - **第一步**：下载Excel模版蓝钮
    - **第二步**：蓝条说明「❗ 特别提醒」+ 4 条 ol 列表（手机号/微信唯一性/数据对应分类/默认填充/注册时间）
    - **第三步**：上传XCL文件占位框（圆形蓝色↑图标 + 文字「点击或拖拽上传XCL文件」）
    - **第四步**：导入设置
      - 1. 本次导入的客源审核状态默认为：● 有效 / ○ 待核
      - 2. 留空项/无法匹配项默认值设置（4 列网格）：来源 select / 分派红娘 select / 推广红娘 input / 标签 select
      - 3. 昵称重复处理方案：● 不导入 / ○ 仍然导入 + 蓝条 tip「重复昵称后系统自动加上序号，例如：爱笑小天使_2 爱笑小天使_3」
      - 「执行导入」蓝钮
- 修改 `src/app/(admin)/love-customer-list/page.tsx`：
  - import 引入 `useRouter` (next/navigation)
  - 在组件内新增 `const router = useRouter()`
  - 「批量导入」菜单项 onClick 改为 `router.push("/love-customer-batch-import") + setAddMenuOpen(false)`
- 新增 `src/app/globals.css` 前缀 `.lcbi-*` 20 余类:
  - card/head/back/intro/step(-head/-dot/-desc)/notice(-title)/btn-primary
  - upload(-icon/-text hover蓝边)
  - row(-block)/radio/radio-group/tip
  - grid(4列)/field(-label)/input(+focus border-blue)/select(SVG下拉箭头)
  - action
- `npx tsc --noEmit` 通过。

## 客源线索-线索管理「简洁模式设置」按钮 Drawer
- 路由 `/love-customer-list`(客源线索二级菜单首项「线索管理」)。原简洁模式左侧齿轮按钮「⚙」（line359）仅在 viewMode === "simple" 时显示且无 onClick 绑定，本次新增 Drawer 弹窗。
- 修改 `src/app/(admin)/love-customer-list/page.tsx`：
  - 新增 state：`simpleSetOpen`、`simpleFields`(20 项字段)、`fixTop`、`fixBottom` + `moveSimple`(数组交换实现前移/后移)
  - 「⚙」按钮 onClick 改为 `setSimpleSetOpen(true)`
  - 在 quickOpen Drawer 之前新增 simpleSetOpen Drawer 渲染块
- **右侧 Drawer 结构（560px max-width 92vw）**：
  - 头：✕ + 标题「列表设置」+ 右上「取消」「确定提交」两按钮
  - body：
    - 顶部 tip 行：左侧「勾选要显示的列，并可以任意排序」+ 右侧两个固定设置「固定前 [0-5] 列 / 固定后 [0-5] 列」select
    - 3 列表格：是否显示 / 字段名称 / 排序
    - 20 行真实数据（标记/客源ID/称呼/姓名/性别/出生/婚况/学历/身高/职业/来源/录入/跟进/状态/审核/意向/标签/入库状态/描述/操作），每行 checkbox + 字段名 + 「前移 后移」蓝链（首行前移 disabled，末行后移 disabled）
- 状态管理：useState simpleSetOpen + simpleFields(20 项) + fixTop(0-5) + fixBottom(0-5)，`moveSimple` 通过数组交换实现移动。
- 新增 `src/app/globals.css` 前缀 `.lc-set-*` 20 余类:
  - `@keyframes lc-set-slide-in` + `.lc-set-panel` 滑入动画
  - drawer mask/panel/head/head-actions/cancel/submit/body/tip
  - fix(-label)/select(SVG 下拉箭头)
  - table thead-th(.check-cell/.name-cell/.sort-cell)/tbody-td/-check input/.sort-cell/.move(:disabled 灰态 hover 下划线)
- `npx tsc --noEmit` 通过。

## 概览页-3个图表动态效果调整
- 路由 `/home`(概览首页)。用户反馈进入页面或刷新页面时，3 个图表（会员增长趋势/收入构成占比/会员男女占比）的入场动画太快。
- 修改 `src/app/(admin)/home/page.tsx`：
  - 用 python 全局替换所有 `animationDuration={600}` 为 `animationDuration={1800}`（3 处）
  - 涉及图表：会员增长趋势 AreaChart（line329）/ 收入构成占比 PieChart（line342）/ 会员男女占比 PieChart（line370）
- 动画时长从 600ms 延长到 1800ms，让入场动画更平滑自然。
- 关键 state 保留：`loaded` state + `key={loaded ? "X-loaded" : "X-empty"}` 强制 remount，确保真实接口模式下也能播放完整入场动画。
- `npx tsc --noEmit` 通过。

## 会员CRM-资料管理「添加资料」右侧 Drawer
- 路由 `/love-user-list`(会员CRM二级菜单首项「资料管理」，导航 AdminLayout line54 已指向)。原 createOpen 渲染使用居中 Modal (max-w-[620px])，本次改为截图样式的右侧 Drawer。
- 修改 `src/app/(admin)/love-user-list/page.tsx`：
  - 扩展 `memberForm` 新增字段：real_name / code / wechat / wechat_same / status(默认"1") / source(默认"1") / account_mode(默认"auto")
  - `createMember` API body 增加 real_name/wechat/status/source 字段，POST 后重置全部字段
  - 替换原 `<Modal title="添加会员资料">` 渲染块为 Drawer 容器（onClick mask 关闭 + panel stopPropagation）
- **右侧 Drawer 结构（720px max-width 92vw）**：
  - 头：✕ + 标题「添加资料」+ 右上「取消」「确定提交」两按钮（提交中变为「提交中…」）
  - body（8 个区块）：
    - 账号 radio：自动生成账号(默认)/ 绑定已有账号 / 自定义创建
    - 蓝色须知条「● 系统将以下发资料库中的手机号作为注册手机，将昵称作为客户登录账号（如：爱笑小天使），默认密码 abc123」
    - 昵称 input(3-28字符，最多14个汉字) + 右侧「作为登录账号」灰小字
    - 编号 input(留空表示系统自动生成) + 右侧「建议留空自动生成」灰小字
    - 姓名 input(3-30字符，最多15个汉字)
    - 性别 radio：男(默认)/ 女
    - 2 列网格：手机 input + 右侧「微信和手机至少填写一项」灰小字 / 微信 input + 右侧「☐ 微信同号」checkbox
    - 状态 radio：公开相亲(默认)/ 委托红娘 / 完全私密 / 停止相亲 / 已经脱单
    - 蓝色须知条「● 在平台中向所有人公开显示基本信息，相亲会员可进入主页查看详细资料（不含联系方式）」
    - 来源 radio：后台添加(默认)/ 父母登记
    - 蓝色须知条「● 请谨慎选择，一经提交，无法修改」
- 状态管理：memberForm.account_mode(3 个 radio 受控)/ status(5 个 radio 受控)/ source(2 个 radio 受控)/ wechat_same(checkbox 受控)。
- 新增 `src/app/globals.css` 前缀 `.member-add-*` 20 余类:
  - `@keyframes member-add-slide-in` + `.member-add-panel` 滑入动画
  - mask/panel/head/head-x/head-actions/cancel/submit(:disabled 灰态)/body
  - row/label/field-wrap/row-2(2列)/field(-label/-with-side)/side
  - radio-group/radio(.active)/check/notice(-dot)
- `npx tsc --noEmit` 通过。

## 会员CRM-资料管理「智能录入」按钮 Drawer
- 路由 `/love-user-list`(会员CRM二级菜单首项「资料管理」，导航 AdminLayout line54 已指向)。原「智能录入」按钮（line439）onClick 错误指向 `setCreateOpen(true)`（与「添加资料」按钮共用），本次绑定独立的智能录入 Drawer。
- 修改 `src/app/(admin)/love-user-list/page.tsx`：
  - 新增 state：`smartOpen`
  - 「智能录入」按钮 onClick 改为 `setSmartOpen(true)`
  - 在 createOpen Drawer 之后渲染 smartOpen Drawer 块
- **Drawer 结构（720px，复用 `.smart-drawer-*` 全局样式）**：
  - 头：✕ + 标题「智能录入」+ 右上「取消」「确定提交」两按钮
  - body：
    - 蓝色须知条「💡 须知」+ 腾讯AI智能体+Deepseek大模型说明
    - 2 个 radio「文字识别录入 参考模版」「图片识别录入 参考模版」
    - textarea placeholder「复制粘贴到这里」+ 「✦ 开始智能填写到下面信息中」蓝钮
    - ☑ checkbox「将本内容自动存储到「原始登记信息存储」字段中」
    - 蓝色提示「📍 系统将以下发资料库中的手机号作为注册手机，将昵称作为客户登录账号（如：爱笑小天使），默认密码 abc123」
    - 「基本资料」+「择偶要求」双 section 标题
    - 3 列字段网格（18 个字段）：手机/昵称(含自动生成按钮)/姓名/性别(radio 男/女)/生日(date)/属相(select)/身高(cm)/体重(kg)/婚况/家乡/现居/学历/职业/收入/购房/购车/吸烟/喝酒
- 状态管理：useState smartOpen(无受控 radio,仅控制 Drawer 开关)。
- 复用 `src/app/globals.css` 已有的 `.smart-drawer-*` 前缀 30 余类，无需新增样式。
- `npx tsc --noEmit` 通过。

## 会员CRM-资料管理「简洁模式设置」+ 30 列简化表格
- 路由 `/love-user-list`(会员CRM二级菜单首项「资料管理」)。用户希望会员CRM-资料管理页面在点击「简洁」后出现齿轮设置按钮以及表格的列变化，模仿线索管理的简洁模式。
- 修改 `src/app/(admin)/love-user-list/page.tsx`：
  - 新增 state：`simpleSetOpen`、`simpleFields`(30 项字段数组)
  - 在简洁按钮前添加 `view === "compact" && (...)` 渲染「⚙」按钮（onClick setSimpleSetOpen）
  - 修改 thead：根据 view 显示不同列（compact 模式 31 列：标记/资料ID/编号/头像/昵称/姓名/性别/出生/婚况/学历/身高/职业/体重/购房/民族/现居/老家/收入/标签/宗教/推广人/跟进人/客户意向/跟进情况/审核/状态/来源/登记时间/最后登录/操作，professional 模式保留原有 10 列）
  - 修改 tr 主体：用 `{view === "compact" ? (<>compact 30 列</>) : (<>professional 10 列</>)}` 三元表达式渲染
  - compact 模式列字段：复选框 / ID / 编号(G+id) / 36×36 头像 / 昵称 / 真实姓名 / 性别标签(蓝男/粉女) / 出生年(岁) / 婚况 / 学历 / 身高CM / 职业 / 体重KG / 购房 / 民族 / 现居 / 老家 / 收入 / 标签 / 宗教 / 推广人 / 跟进红娘 select / 客户意向 select / 跟进情况(上次+未跟进) / 审核 select(免审核资质) / 状态(公开相亲/已冻结) / 来源(自己注册) / 登记时间 / 最后登录 / 操作(详细/跟进/超管)
  - 表格最小宽度：compact min-w-[2400px]，professional 保留原 min-w-[1250px]
  - colSpan 改为 `view === "compact" ? 31 : 10`
  - 扩展 Member 类型添加字段：real_name/wechat/weight/tags/religion/ethnicity/house_status/last_login_at（tags 类型与 MemberDetailWorkspace 保持一致为 string[] | null）
  - 在 smartOpen Drawer 之前新增 simpleSetOpen Drawer 渲染块（复用 `.lc-set-*` 全局样式）
- 关键修复：
  - Python 脚本重构 tr 时 JSX 三元表达式结构错乱：用 `</> 包裹 professional 模式 + `) : ( <>` 包裹 compact 模式 + `</> 包裹 professional` 修复
  - Member.tags 类型从 `string | null` 改为 `string[] | null` 与 MemberDetailWorkspace 类型保持一致
## 平台配置 7 页全量接接口（UI 不变）
- 范围：`platform-config-basic` / `platform-navconfig` / `platform-page` / `power-config` / `platform-content` / `platform-base` / `platform-payconfig` 七个页面原先全是静态 useState，本次全部接入后台配置域接口，UI 不做视觉改动。
- 方案：复用后端 `/admin/configs/{namespace}` 快照机制（GET/PATCH+version 乐观锁+审计）+ `/admin/common/upload` 图片上传；新增前端库 `src/lib/platform-config.ts`（`useConfigDomain` Hook 深合并/防抖/ref 防版本竞态、`pickAndUploadImage`、`showConfigToast` 右下轻提示、`asStr/asObject` 等）；改动 700~900ms 自动保存，"确定提交"按钮同时保留为手动落库。
- 命名空间映射：basic→platform_basic(基础文案+8个图片位真实上传)；运营模式→platform_operation；引导页→platform_register_guide；资料登记→platform_register_fields；私密字段→platform_private_fields；筛选→platform_filter_config；导航→platform_navigation；平台布局→platform_layout(含统计栏基数受控、活动分类下拉受控)；权限→platform_permissions(browse/view/state/line/other 分组，保留原运行时限额键)；内容-文案→platform_content、自定义页面→platform_custom_pages、会员中心状态→platform_member_states；基础数据→platform_base_data.sections(29组字典增删改)；收费→platform_pay(含牵线套餐/VIP/置顶套餐等)。
- 后端 `app/services/admin_config.py` DEFAULT_CONFIGS 增加 6 个新域并扩展既有 platform_* 默认结构（合并缺失键，不覆盖存量值）。
- 交付文档：`docs/平台配置-接口接线与分级配置总结.md`（六级分级：基础/展示/内容/权限/交易/数据）。
- 注意：管理员需 `platform.config.read/write` 权限（`*` 超级管理自动放行）；导航"一键切换风格/更换颜色"、云端素材库弹窗等纯视觉演示控件仍保留为占位。
- `npx tsc --noEmit` 通过。
