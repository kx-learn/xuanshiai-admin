# 2026-09-19

## 全后台功能清单（按菜单分级）成文

- 产出文件：`docs/全后台功能清单-按菜单分级-2026-09-19.md`
- 口径：以 `src/components/AdminLayout.tsx` 代码真实菜单树为一级/二级依据，逐页读取 `src/app/(admin)/**/page.tsx` 抽取功能点与数据落点。
- 清点结论：20 个菜单分组 / 113 个菜单入口（去重页面 112）/ 121 个页面文件 / 51 个配置域命名空间。
- 数据落点分布：`adminEndpoints` 业务接口 59 页、`adminApi` 直调 21 页、`useConfigDomain` 配置域 34 页、静态纯 UI 7 页。
- 关键观察：
  1. 业务页已全部接线，仅 7 个页面无数据层（4 刻意静态 + 2 说明页 + 1 重定向页）。
  2. `/sms-group` 被挂了两处菜单（运营工具·短信群发 / 系统管理·短信系统·短信群发），是同一页面，改动会同时影响两个入口。
  3. `src/lib/admin-endpoints.ts` 第 1 行 import 的 `getAdminToken` 已无引用（tsconfig 未开 `noUnusedLocals`，不影响构建）。
  4. `src/lib/platform-config.ts` 是所有配置页统一底座（`useConfigDomain` + `uploadAdminImage` + 深浅合并 + 自动保存防抖）。

## 约定提醒

- 上一轮用户明确要求「不准更改文件」；本轮已在 Agent 模式下重新授权写入。涉及 `src/` 代码的改动仍应先确认。
- 临时的 `_mtimes.txt` 等排查产物已清理，不要留在仓库根目录。
