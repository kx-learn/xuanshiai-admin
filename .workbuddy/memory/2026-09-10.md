# 2026-09-10

- 移除登录页（src/app/login/page.tsx）临时自动登录补丁：useEffect 写死 `local-demo-token` 跳转 /home、`debugLogin` 函数及"调试登录"按钮，删除 globals.css 中 `.lg-debug` 样式。
- 清理所有依赖 `local-demo-token` 的演示数据死代码：
  - src/app/(admin)/home/page.tsx：demo state、buildDemoReport、demoAnnouncements、demoGuides、DEMO_PENDING、DEMO_HELP_ROWS 及相关分支
  - src/app/(admin)/reg-user-all/page.tsx：mockAccounts 及 load() 中的演示分支
  - src/app/(admin)/reg-user-log/page.tsx：mockLogs 及演示分支
  - src/components/AdminLayout.tsx：me() 前的 demo token 分支
- 真实登录流程恢复：loginAdmin() -> adminEndpoints 调用，`npx tsc --noEmit` 通过。
- 注意：若浏览器 localStorage 残留 `local-demo-token`，现在会走真实接口并 401 跳回登录页，属预期行为。
- 新增通用确认弹窗 `src/components/ConfirmDialog.tsx`（props: open/title/message/confirmText/cancelText/onConfirm/onCancel），样式类 `cfm-*` 加在 globals.css（chgp-* 之后）。AdminLayout 退出登录改为先弹「提示 / 您确定要退出登录吗？」，确定后才 logoutAdmin() 跳转 /login。
- 登录页 `src/app/login/page.tsx` 增强：① 点击扫码框后切换为密集清晰二维码（`denseQrCells()` 29x29，类 `.lg-qr-svg.is-ready` 深色填充），移除「点击扫码」浮层；② 「手机登录」tab 渲染手机号 + 验证码表单（`lg-code-row` / `lg-code-btn`），含 60s 倒计时。注意：项目无手机登录/SMS 接口，submitPhone 目前仅做校验并提示「手机登录暂未开放」。
- globals.css 隐藏密码框浏览器原生眼睛（`.lg-input::-ms-reveal/::-ms-clear` 及 `::-webkit-credentials-auto-fill-button`），避免与自定义 `.lg-eye` 重复。
- 修复首页两个环形图被裁切：`.ov-donut` 容器仅 104px 宽但 Pie `outerRadius=80`（直径 160），被 `.ov-card{overflow:hidden}` 裁掉左右各约 28px。改为 ResponsiveContainer `height={104}`、Pie `innerRadius={34} outerRadius={50}`（直径 100，左右各留 2px）。home/page.tsx 两处（来源占比 / 男女占比）。约定：环形图半径必须 ≤ 容器宽/2。
- 首页「耗材余量」卡片只有 2 项（实名/短信），补全为 4 项（实名 786次 / 短信 取 bootstrap header.sms.remaining_count / 合同 150次 / 婚况 1次，art 依次 card/chat/card/chat）。依据：docs/live-admin-home-api-capture.md:425-430 的 authorization.quotas 定义 real_name/sms/e_contract/marital_status 四项。注意：除短信外均无后端接口（AdminBootstrap 仅 sms_remaining_count），暂用固定值。
- 首页两个环形图卡片（ov-mid-source / ov-mid-gender）改为 flex 纵向布局 + body flex:1，内容垂直居中，消除被同行最高卡片拉伸后的底部空白。globals.css 加 `.ov-mid-source,.ov-mid-gender{display:flex;flex-direction:column}` 与 `.ov-mid-source>.ov-card-body,.ov-mid-gender>.ov-card-body{flex:1 1 auto}`。
- 环形图进场动画改为「从无 → 从 12 点位置顺时针扫一整圈」：home/page.tsx 新增 pieSweep 状态（0→1，requestAnimationFrame + easeOutCubic，1600ms，依赖 chartsReady && loaded），两个 Pie 改为 `startAngle={90} endAngle={90 - 360 * pieSweep} isAnimationActive={false}`。要点：Recharts 角度增大为逆时针，故顺时针需从 90 递减到 -270；startAngle==endAngle 时无渲染=「从无」。Recharts 自带 Pie 动画不是此效果，必须用受控 endAngle。
- 抽象扫过动画为可复用 hook：新增 `src/hooks/useSweepAngle.ts`（参数 active、duration=1600ms，返回 0→1 的 eased 进度）。home/page.tsx 改用此 hook 替换原来的 pieSweep state + useEffect。
- 把环形图扫过动画推广到所有 donut/pie 图：DashboardCharts.tsx（数据报表里的男女占比饼图）也用 useSweepAngle，并把 Pie 改为受控 startAngle/endAngle（Recharts isAnimationActive=false）。DashboardCharts.tsx 同时给 AreaChart 加 `.ustat-chart` 类并 isAnimationActive=false，给 Bar 加 `.ustat-bar-fill` 类。
- 折线/面积图加「线条描线 + 面积渐显」动画：globals.css 新增 @keyframes chart-line-draw（stroke-dashoffset 2000→0）和 chart-area-fade（opacity 0→1，0.9s delay）。`.ov-mid-trend .recharts-area-curve` 用 1.4s ease-out 描线 `.15s` 延迟；`.ov-mid-trend .recharts-area` 0.8s 渐显 0.9s 延迟。`.ustat-chart` 同等待遇（DashboardCharts 折线）。
- 类添加 `.ustat-donut-ring`（环形图扫过）、`.ustat-bar-fill`（条形图 scaleX 0→1）、`.ustat-chart`（折线/面积图描线渐显）于 globals.css，并把上述 prefers-reduced-motion 兼容全部归零。
